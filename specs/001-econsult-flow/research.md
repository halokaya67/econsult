# Platform research record

Generated on 2026-09-11 from the research workflow that preceded the specification. Nine topics were each researched by a read-only agent that had to open its sources and mark unopened recall as unverified; a second agent then tried to refute every claim and checked every named package against Expo Go for SDK 57 (the brief requires a clean clone to run with `npx expo start`). A completeness critic then listed gaps, contradictions and anything that would break Expo Go, and one follow-up round researched the gaps. Nothing here was edited by hand; the specification cites this record.

A rendered copy lives at `artifacts/research.html`.

> **Since then.** This is the research as it stood before the specification; the spec accepted some of it and overruled the rest, and the build then overturned one more. Where the app differs from the recommendations below:
>
> - `automaticallyAdjustKeyboardInsets` was adopted and then removed: iOS reports the out-of-process photo picker's keyboard frame too, which blanked the message step on a real iPad, so the screen scaffold listens for the keyboard itself and ignores foreign frames.
> - The Dutch sample copy here — "Volgende", "Versturen", the usage strings and the offline hint — was not used: the app is English, matching the brief's data.
> - The live-region recipe for errors and for the offline banner was dropped: an error is spoken once by the focus move to the field, and the banner has no live region; only the network provider announces, once per transition.
> - The service layer shipped as `src/api/{contracts,transport,services}.ts` with `src/api/fake/{fakeTransport,fixtures}.ts` in TypeScript, not as `src/services/*` with JSON fixtures, and the query client sits in `src/api/queryClient.ts`.
> - No environment layer shipped: no `.env`, no `.env.example` and no `EXPO_PUBLIC_*` variable is read anywhere.
> - Developer settings are `src/app/dev-settings.tsx`, a modal opened from a visible link gated on `isDevelopmentBuild()`, in English — not a hidden route behind a triple tap.
> - expo-status-bar was dropped from the keep list after all, and expo-device was kept rather than dropped: it is the source of `Device.isDevice`.
> - The flow lives at `src/app/econsult/`, a real path segment, rather than in a route group.
> - The dependency and jest sketches differ from what shipped: `@types/react ~19.2.2` and `@types/jest ^29.5.14`, a jest block with a setup file, coverage collection and seven thresholds, no `test:watch`, and a `typecheck` script that regenerates the typed routes before `tsc`.

## Topics

- [image-picker](#topic-image-picker) — Photo capture and library selection with expo-image-picker on Expo SDK 57 (Expo Go, iOS simulator / Android emulator, fetch+FormData upload)
- [network-offline](#topic-network-offline) — Offline detection and slow/failing request handling for the e-consult send flow in Expo SDK 57 (Expo Go compatible)
- [template-audit](#topic-template-audit) — Expo SDK 57 'default' template (create-expo-app 4.0.0): what it ships, what to keep, and what to strip for a small production-quality e-consult form app
- [keyboard-safe-area](#topic-keyboard-safe-area) — Keyboard avoidance and safe areas for the e-consult flow in Expo SDK 57 / RN 0.86 (Expo Go only)
- [accessibility](#topic-accessibility) — Accessibility APIs in React Native 0.86.3 / Expo SDK 57 for a VoiceOver- and TalkBack-usable e-consult form flow (labels, error announcement, focus management, touch targets, font scaling, testing)
- [fake-backend](#topic-fake-backend) — Faking three REST services in an Expo SDK 57 e-consult app (hand-rolled fake module vs msw/native vs local HTTP server), EXPO_PUBLIC_* env vars, and a __DEV__ settings panel
- [expo-router-flow](#topic-expo-router-flow) — Linear multi-step e-consult flow with expo-router ~57.0 Stack: file layout, screen options, back-prevention on confirmation, draft state sharing, typed routes, unstable_settings, Dynamic Type in the native header, and SDK 57 template/entry resolution
- [data-fetching](#topic-data-fetching) — Data fetching + form state for a 4-step e-consult flow on Expo SDK 57 / React 19.2 with React Compiler enabled (TanStack Query v5 vs plain hooks, draft state across expo-router screens, zod validation)
- [testing-lint](#topic-testing-lint) — Testing, linting and type-checking setup for the Expo SDK 57 / RN 0.86.3 / React 19.2.3 / TypeScript 6.0 e-consult app
- [Completeness critic](#completeness-critic)
- [followup:1](#followup-1) — Uploading an expo-image-picker photo as multipart/form-data in Expo SDK 57, where expo/fetch is the global fetch (three transport options compared)
- [followup:2](#followup-2) — TanStack Query v5 paused-mutation semantics vs. an advisory offline flag for the e-consult send path (React Native / Expo SDK 57)
- [followup:3](#followup-3) — Expo Go-compatible photo downscaling for the e-consult attachment: expo-image-manipulator (SDK 57) pipeline to produce a ≤1 MB, ~1600px JPEG from an expo-image-picker asset
- [followup:4](#followup-4) — Clean-clone install integrity for the combined Expo SDK 57 e-consult dependency set: does `git clone && npm install && npx expo start` survive the union of all recommended packages under npm 11 without --legacy-peer-deps
- [followup:5](#followup-5) — Draft survival and in-flight send across backgrounding / process death (Expo SDK 57, RN 0.86.3): AppState semantics, expo/fetch under iOS suspension, photo cache-URI durability, and a minimal AsyncStorage draft-persistence pattern
- [followup:6](#followup-6) — Dutch app language + screen-reader pronunciation (VoiceOver/TalkBack) in Expo SDK 57 / RN 0.86.3, and what works in Expo Go vs only in a prebuild/EAS binary

## Topic: image-picker

*Photo capture and library selection with expo-image-picker on Expo SDK 57 (Expo Go, iOS simulator / Android emulator, fetch+FormData upload)*

### Recommendation

Add expo-image-picker with `npx expo install expo-image-picker` (resolves to 57.0.x) — it is Expo Go compatible on SDK 57, so nothing about the photo step forces a custom development build; that is the single biggest reason to pick it over expo-camera-based custom UI for this case study. Add the config plugin entry to app.json next to expo-router/expo-splash-screen with Dutch usage strings (e.g. cameraPermission: "Sta toe dat de app de camera gebruikt om een foto bij uw e-consult te voegen", photosPermission: "Sta toe dat de app uw foto's gebruikt om een foto bij uw e-consult te voegen", microphonePermission: false since you never record video) — the plugin only takes effect for prebuild/EAS builds, but including it documents intent and keeps the project buildable. In code, use the hooks (`const [cameraPermission, requestCameraPermission] = useCameraPermissions()`) so permission state is reactive and you can render a distinct "permission denied → open Settings" state rather than a silent no-op, which matters for elderly users. Call `launchImageLibraryAsync({ mediaTypes: ['images'], allowsEditing: false, quality: 0.6 })` and `launchCameraAsync({ mediaTypes: ['images'], quality: 0.6 })`; pass `mediaTypes` as a string array (not the deprecated MediaTypeOptions enum) and always set quality explicitly, because the default 1.0 produces multi-MB JPEGs that are hostile on poor connections. Guard results with `if (result.canceled) return;` then take `result.assets[0]` and keep `{ uri, width, height, fileName, fileSize, mimeType }` in state for the preview (render with expo-image, already a dependency, and give the preview an accessibilityLabel plus a clearly labelled "Foto verwijderen" button rather than an icon-only X). For the two-call send, POST the e-consult JSON first, then upload with `const body = new FormData(); body.append('photo', { uri: asset.uri, name: asset.fileName ?? 'foto.jpg', type: asset.mimeType ?? 'image/jpeg' } as any); await fetch(url, { method: 'POST', body })` — do not set Content-Type yourself, let RN generate the multipart boundary, and note the `as any` cast is needed because RN's DOM-typed FormData signature does not describe the `{uri,name,type}` part. Handle the platform asymmetry explicitly: on the iOS simulator the camera button cannot work, so detect and show a friendly "camera niet beschikbaar in de simulator" hint and demo the flow from the photo library, while the Android emulator's virtual scene camera (with an imported JPEG of, say, a rash or a wound) lets you record the real capture path for your demo video. Finally, wire `getPendingResultAsync()` on Android mount so a low-memory MainActivity kill during capture does not silently drop the patient's photo, and treat the attachment upload as independently retryable — the e-consult itself is already created after call one, so the offline/error state should say "bericht verstuurd, foto nog niet" rather than implying total failure.

**Packages involved:** expo-image-picker, expo-image

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (expo-image-picker 57.0.17) — The npm `latest` dist-tag for expo-image-picker is 57.0.17 (the SDK 57 line, alongside sdk-56 → 56.0.25 and sdk-55 → 55.0.24), it declares peerDependency `expo: *` and its only dependency is `expo-image-loader: ~57.0.1`, so `npx expo install expo-image-picker` in this project resolves to 57.0.x.  
   Source: https://registry.npmjs.org/expo-image-picker and https://registry.npmjs.org/expo-image-picker/57.0.17
2. **verified** (Expo SDK 57 docs) — The SDK 57 docs platform table lists Android, iOS, Web AND Expo Go as supported, so photo capture/library picking needs no custom development build — `npx expo start` + Expo Go is enough.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/ and /versions/latest/sdk/imagepicker/
3. **verified** (expo sdk-57 branch) — The launch API is `launchCameraAsync(options: ImagePickerOptions = {}): Promise<ImagePickerResult>` and `launchImageLibraryAsync(options: ImagePickerOptions = {}): Promise<ImagePickerResult>`, plus `getPendingResultAsync(): Promise<ImagePickerResult | ImagePickerErrorResult | null>` for recovering results after Android kills MainActivity during camera capture.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-picker/src/ImagePicker.ts
4. **verified** (expo sdk-57 branch) — Permissions expose both imperative functions — `requestCameraPermissionsAsync()`, `getCameraPermissionsAsync()`, `requestMediaLibraryPermissionsAsync(writeOnly = false)`, `getMediaLibraryPermissionsAsync(writeOnly = false)` — and the hooks `useCameraPermissions()` / `useMediaLibraryPermissions({ writeOnly })`, both built with Expo's `createPermissionHook`.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-picker/src/ImagePicker.ts
5. **verified** (Expo SDK 57) — `ImagePickerResult` is a union `ImagePickerSuccessResult | ImagePickerCanceledResult` of the shape `{ canceled: boolean; assets: ImagePickerAsset[] | null }`, so the correct guard is `if (result.canceled) return;` then `result.assets[0]`.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/ and sdk-57 ImagePicker.types.ts
6. **verified** (expo sdk-57 branch) — `ImagePickerAsset` carries `uri: string`, `width`/`height: number` (documented as possibly 0), `fileName?: string | null` (null when only limited photo permission was granted), `fileSize?: number` (bytes), `mimeType?: string` (null when undetermined), plus `type` ('image' | 'video' | 'livePhoto' | 'pairedVideo' | null), `base64`, `exif`, `assetId`, `duration` and `pairedVideoAsset`.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-picker/src/ImagePicker.types.ts and the SDK 57 API reference
7. **verified** (expo sdk-57 branch / expo-image-picker CHANGELOG) — `mediaTypes?: MediaType | MediaType[] | MediaTypeOptions` where `MediaType = 'images' | 'videos' | 'livePhotos'` with `@default 'images'`; the old `MediaTypeOptions` enum (`.All/.Images/.Videos`) is deprecated — pass `mediaTypes: ['images']` instead.  
   Source: sdk-57 ImagePicker.types.ts + CHANGELOG entry '## 16.0.0 — 2024-10-22: ImagePicker.MediaTypeOptions have been deprecated. Use a single MediaType or an array of MediaTypes instead.'
8. **verified** (expo sdk-57 branch) — `quality?: number` is 0–1 with `@default 1.0` (raised from 0.2 in 16.0.0), and iOS ignores it for .png/.bmp images picked from the library — for a poor-connection GP portal you should set quality explicitly (e.g. 0.5–0.7) rather than rely on the default.  
   Source: sdk-57 ImagePicker.types.ts, SDK 57 API reference, expo-image-picker CHANGELOG 16.0.0
9. **verified** (Expo SDK 57 docs) — `allowsEditing?: boolean` has `@default false`, is Android+iOS only, is mutually exclusive with `allowsMultipleSelection`, gives crop+rotate on Android but crop only on iOS, and the docs' Known Issues state that on iOS a higher-resolution camera-roll image can yield a wrong cropped-rectangle value.  
   Source: sdk-57 ImagePicker.types.ts and https://docs.expo.dev/versions/latest/sdk/imagepicker/ (Known issues)
10. **verified** (Expo SDK 57 docs) — The config plugin entry is `["expo-image-picker", { photosPermission, cameraPermission, microphonePermission, colors: { cropToolbarColor }, dark: { colors } }]`; it writes iOS `NSPhotoLibraryUsageDescription`, `NSCameraUsageDescription` and `NSMicrophoneUsageDescription`, with defaults like "Allow $(PRODUCT_NAME) to access your camera", and passing `false` for cameraPermission/microphonePermission blocks the matching Android permission.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/ (Configuration in app config)
11. **verified** (Expo SDK 57 docs / expo-image-picker CHANGELOG) — On Android the library auto-adds `CAMERA`, `READ_EXTERNAL_STORAGE` and `WRITE_EXTERNAL_STORAGE` to the manifest; `READ_MEDIA_IMAGES` / `READ_MEDIA_VIDEO` were deliberately removed in 16.0.0, so no extra Android permission config is required in app.json.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/ (Permissions → Android) and CHANGELOG 16.0.0
12. **verified** (Expo SDK 57 docs) — The Expo docs state plainly that taking photos needs real hardware — "The iOS Simulator does not have one, so use a physical device to test this button" — so the camera path cannot be demoed on the iOS simulator and the UI must degrade gracefully there.  
   Source: https://docs.expo.dev/versions/latest/sdk/imagepicker/
13. **verified** (Android Studio emulator docs, read 2026-09-11) — The Android Emulator does provide a camera: Google documents a virtual scene camera ("You can use the virtual scene camera in a virtual environment to experiment with augmented reality (AR) apps made with ARCore") and you can feed it your own PNG/JPEG via Extended controls → Camera → Virtual scene images → Add image, which makes the emulator the practical way to exercise `launchCameraAsync` end-to-end.  
   Source: https://developer.android.com/studio/run/emulator-use-camera
14. **verified** (react-native 0.86-stable) — React Native 0.86 still ships its own FormData polyfill typed as `FormDataValue = string | {name?: string, type?: string, uri: string}`; `getParts()` turns `value.name` into `content-disposition; filename="..."` and `value.type` into the part's `content-type`, and the source's own example is `var photo = { uri: uriFromCameraRoll, type: 'image/jpeg', name: 'photo.jpg' }; body.append('photo', photo);` — so `fetch(url, { method: 'POST', body })` with an `asset.uri` works without reading the file into JS.  
   Source: https://raw.githubusercontent.com/facebook/react-native/0.86-stable/packages/react-native/Libraries/Network/FormData.js

### Open uncertainties

- I did not read the source of createPermissionHook this run, so the exact tuple returned by useCameraPermissions() (commonly [permission, requestPermission, getPermission]) is recalled from training, not verified — check node_modules/expo-modules-core after install.
- I did not verify whether Expo Go on SDK 57 uses its own bundled Info.plist usage strings (meaning your Dutch plugin strings only appear in a prebuild/EAS build, not in Expo Go). This is very likely but unverified — worth a sentence in the README either way.
- expo-image-picker 58.0.0 landed in the changelog on 2026-09-10 (SDK 58 line, one day before today), so if you run `npx expo install` against a non-pinned registry state, confirm it still resolves to the 57.x line for SDK 57.
- The Android docs page I opened describes the virtual scene camera and image import but does NOT state that a newly created AVD defaults its back camera to VirtualScene; my belief that it does is unverified — check the AVD's Advanced Settings before relying on it in a demo.
- I did not verify the default value of mediaTypes on the Web platform, nor confirm fileSize/mimeType are reliably populated on Android for camera captures (docs mark both fields optional); validate empirically before showing file size in the UI.
- The docs' Android permission list (CAMERA/READ_EXTERNAL_STORAGE/WRITE_EXTERNAL_STORAGE) predates the READ_MEDIA_IMAGES removal note in the changelog; I did not open the plugin source to confirm exactly which permissions the SDK 57 plugin writes today.
- fetch + FormData gives no upload progress events in React Native; for a spinner tied to real bytes you would need XMLHttpRequest.upload.onprogress or expo-file-system's upload API, neither of which I verified this run.

### Adversarial verification

**Verdict:** partly-wrong

Claims the verifier refuted or corrected:

- **Claim:** Fact 5: "ImagePickerResult is a union ImagePickerSuccessResult | ImagePickerCanceledResult of the shape { canceled: boolean; assets: ImagePickerAsset[] | null }"  
  **Correction:** Wrong as TypeScript: the sdk-57 source declares boolean *literal* members — `ImagePickerSuccessResult = { canceled: false; assets: ImagePickerAsset[] }` and `ImagePickerCanceledResult = { canceled: true; assets: null }` — and it is precisely those literals (not `canceled: boolean`) that make the digest's own `if (result.canceled) return;` guard narrow `result.assets` to non-null; describing it as `canceled: boolean` would make the recommended code a type error.  
  Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-picker/src/ImagePicker.types.ts
- **Claim:** Fact 6 presents a complete enumeration of ImagePickerAsset fields (uri, width, height, fileName, fileSize, mimeType, type, base64, exif, assetId, duration, pairedVideoAsset) and says mimeType is "null when undetermined".  
  **Correction:** Incomplete and internally inconsistent: sdk-57 also declares `file?: File`, documented as "Supported platforms: Web. The web File object ... can be used to upload to a server with FormData" — directly relevant since this project targets web too — and `mimeType` is typed `mimeType?: string` (optional, not nullable) even though the prose docs say "or null if could not be determined", so `asset.mimeType ?? 'image/jpeg'` is right in practice but TypeScript will not force the fallback.  
  Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-picker/src/ImagePicker.types.ts + https://docs.expo.dev/versions/latest/sdk/imagepicker/
- **Claim:** Uncertainty 3: "expo-image-picker 58.0.0 landed in the changelog on 2026-09-10 ... confirm npx expo install still resolves to the 57.x line."  
  **Correction:** Unsupported by the cited changelog and the risk is not real: the sdk-57 CHANGELOG's highest entry is `## 57.0.17 — 2026-09-11` with no 58.0.0 entry (58.0.0 exists on npm only under the `next` dist-tag, `latest` is 57.0.17), and more decisively `npx expo install` resolves through the installed expo 57.0.22's bundledNativeModules.json, which pins `"expo-image-picker": "~57.0.17"` — so it cannot drift to the 58.x line regardless of registry state.  
  Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-picker/CHANGELOG.md + https://registry.npmjs.org/expo-image-picker + node_modules/expo/bundledNativeModules.json
- **Claim:** Uncertainty 7: "for a spinner tied to real bytes you would need XMLHttpRequest.upload.onprogress or expo-file-system's upload API, neither of which I verified."  
  **Correction:** Now verified and stronger than stated: SDK 57's expo-file-system main API documents `File.createUploadTask(url, options)` ("Creates an upload task for this file without starting it") and `File.upload(url, options)`, and expo-file-system is itself "Included in Expo Go" — so an Expo Go-compatible progress-capable upload path exists without dropping to XHR; only the legacy `FileSystem.uploadAsync` is deprecated behind `expo-file-system/legacy`.  
  Source: https://docs.expo.dev/versions/v57.0.0/sdk/filesystem/
- **Claim:** Uncertainty 2: "I did not verify whether Expo Go on SDK 57 uses its own bundled Info.plist usage strings (meaning your Dutch plugin strings only appear in a prebuild/EAS build)."  
  **Correction:** Substantially resolved in the digest's favour: the Expo permissions guide states "Permissions in standalone and development builds require native build-time configuration before they can be requested using runtime JavaScript code. This is not required when testing projects in the Expo Go app", confirming the plugin's Dutch strings are a prebuild/EAS-only artifact and that Expo Go will show its own prompts — the README caveat the digest suggested is correct and should be written as fact, not hedge.  
  Source: https://docs.expo.dev/guides/permissions/
- **Claim:** Uncertainty 4 (residual, NOT refuted): "the Android docs page does not state that a newly created AVD defaults its back camera to VirtualScene."  
  **Correction:** Confirmed as a genuine gap — the page I opened documents the virtual scene camera verbatim ("You can use the virtual scene camera in a virtual environment to experiment with augmented reality (AR) apps made with ARCore") and the Extended controls > Camera > Virtual scene images > Add image flow for PNG/JPEG, but says nothing about AVD back-camera defaults; treat the digest's caveat as standing and check AVD Advanced Settings before the demo.  
  Source: https://developer.android.com/studio/run/emulator-use-camera

Expo Go compatibility of every package named:

| Package | In Expo Go | Config plugin | Dev build needed | Note |
|---|---|---|---|---|
| expo-image-picker | yes | no | no | The SDK 57 API reference header reads "Android, iOS, Web, Included in Expo Go", and the installed expo 57.0.22 pins it in bundledNativeModules.json as "expo-image-picker": "~57.0.17", so `npx expo install expo-image-picker` + `npx expo start` works in Expo Go with no dev build; the config plugin is optional (it only writes Info.plist usage strings and blocks Android permissions during prebuild/EAS, and docs/guides/permissions says build-time permission configuration "is not required when testing projects in the Expo Go app"). |
| expo-image | yes | no | no | SDK 57 docs header reads "Android, iOS, tvOS, Web, Included in Expo Go"; the only plugin prop is the optional iOS-only `disableLibdav1d`, and the package is already a dependency at ~57.0.5, matching bundledNativeModules.json exactly. |
| expo-image-loader | yes | no | no | Sole runtime dependency of expo-image-picker 57.0.17 (`"expo-image-loader":"~57.0.1"`) and itself version-mapped in the installed expo 57.0.22 bundledNativeModules.json, so it ships inside Expo Go with the picker and needs no separate setup. |
| expo-file-system | yes | no | no | Only implied by the digest's uncertainty about upload progress, not by the recommendation; SDK 57 docs read "Android, iOS, tvOS, Included in Expo Go", the config plugin is opt-in ("You can configure expo-file-system using its built-in config plugin if you use config plugins"), and SDK 57 exposes `File.upload(url, options)` and `File.createUploadTask(url, options)` in the main API (legacy `FileSystem.uploadAsync` is deprecated to `expo-file-system/legacy`). |


## Topic: network-offline

*Offline detection and slow/failing request handling for the e-consult send flow in Expo SDK 57 (Expo Go compatible)*

### Recommendation

Use expo-network (`npx expo install expo-network`) and nothing else. Both candidates run in Expo Go, so the deciding factors are payload and privacy: expo-network is a first-party Expo module already aligned to SDK 57, adds its own Android permissions, needs no config plugin, and — critically for a GP portal — never phones a third party, whereas NetInfo's `isInternetReachable` repeatedly HTTP-probes `https://clients3.google.com/generate_204` by default, which you would have to repoint at a practice-owned endpoint before it is defensible under GDPR. NetInfo's only real advantage (an actual internet probe rather than link state) is one you should not depend on anyway.

Shape it as a two-layer design, because the SDK-57 platform split makes a network flag an advisory hint, not a truth:

Layer 1 — the banner (advisory). Wrap `useNetworkState()` in a `useIsOffline()` hook that returns offline only when `isConnected === false || isInternetReachable === false`, and treats `undefined`/unknown as online. Fail-open matters here: an elderly user on a flaky 3G connection must never be locked out of sending by a state the module could not determine. Render a persistent banner above the form — large type that respects Dynamic Type, an icon plus Dutch text (not colour alone), `accessibilityRole="alert"` with `accessibilityLiveRegion="polite"` on Android, and an `AccessibilityInfo.announceForAccessibility` call on the offline→online and online→offline transitions so VoiceOver/TalkBack users hear the change without hunting for it. Note the iOS caveat explicitly in your write-up: since `isInternetReachable` mirrors `isConnected` on iOS, a captive-portal or dead-router Wi-Fi will read as online there while Android would correctly report unreachable.

Layer 2 — the send path (authoritative). Keep the send button mounted and focusable but `disabled` when offline, with `accessibilityState={{ disabled: true }}` and an `accessibilityHint` that says why and what to do ("Geen internetverbinding — uw bericht wordt bewaard op dit scherm"); removing the button strands screen-reader users with no explanation. Because the flag can be wrong, the request itself is the real check: wrap both calls in a small `fetchWithTimeout(url, init, ms)` that creates an `AbortController`, arms `setTimeout(() => controller.abort(), ms)` (no `AbortSignal.timeout` available on this RN version), and clears it in `finally`. Use roughly 15s for the create call and 30–45s for the attachment upload. In the catch, branch on `err.name === 'AbortError'` (timed out — "Het duurt te lang, probeer het opnieuw") versus a `TypeError` from fetch (connection failed) versus a non-2xx status, and drive a single `sendState` machine: `idle | sending | error | sent`.

Retries, with no queueing. Manual retry as the primary affordance: a large "Probeer opnieuw" button in the error state that re-runs only the step that failed. Add automatic retry sparingly and only where it is safe — at most two attempts with ~1s then ~3s backoff plus jitter. The create call is not idempotent, so either send an `Idempotency-Key` header (a UUID generated once per composed e-consult and reused across retries) or do not auto-retry it at all; say which you chose in the README. The attachment upload is keyed to the returned e-consult id and is therefore safe to auto-retry. Handle the partial-failure case deliberately, since it is the most interesting thing about a two-call flow: if create succeeds and upload fails, go to the confirmation screen with the e-consult marked sent and a separate inline "foto niet verzonden — opnieuw proberen" affordance. Never re-post the e-consult to recover a failed photo.

Testing story for the README. iOS simulator: it uses the Mac's network stack, so switching the Mac's Wi-Fi off is the fastest offline test, and Network Link Conditioner (Additional Tools for Xcode) gives the slow/lossy profiles for timeout and spinner behaviour. Android emulator: `adb emu gsm data off` / `adb emu gsm data home` for offline, and `adb emu network delay gprs` + `adb emu network speed edge` to exercise the sending state and the 15s abort.

Cost callout for the case study: zero. expo-network is bundled in Expo Go, so a clean clone plus `npx expo install expo-network` and `npx expo start` works on both simulators with no prebuild, no EAS build, and no native config.

**Packages involved:** expo-network, react-native, expo

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (Expo SDK 57 docs (v57.0.0)) — expo-network is listed as supported on Android, iOS, tvOS and Web and is explicitly marked "Included in Expo Go", so it needs no custom development build.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/network/
2. **verified** (expo-network 57.0.2) — The npm `latest` tag for expo-network is 57.0.2 with wildcard peer deps on `expo` and `react`, installed via `npx expo install expo-network`.  
   Source: https://registry.npmjs.org/expo-network/latest + docs.expo.dev v57 install snippet
3. **verified** (Expo SDK 57 docs (v57.0.0)) — expo-network exports `useNetworkState()` (auto-subscribing hook with cleanup), `getNetworkStateAsync()`, `addNetworkStateListener()` (returns an EventSubscription with `remove()`), `isAirplaneModeEnabledAsync()` and `getIpAddressAsync()`, where NetworkState is `{ isConnected?: boolean; isInternetReachable?: boolean; type?: NetworkStateType }`.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/network/
4. **verified** (Expo SDK 57 docs (v57.0.0)) — On iOS the docs state that for `isInternetReachable` "this value will always be the same as isConnected" — i.e. iOS gives you link state only, never a real internet probe.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/network/
5. **verified** (Expo SDK 57 docs (v57.0.0)) — On Android `isInternetReachable` is true only when the active network has internet capability, confirmed/validated internet access and a usable connection state (VPN additionally requires non-zero downstream bandwidth), so iOS and Android semantics genuinely differ.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/network/
6. **verified** (Expo SDK 57 docs (v57.0.0)) — expo-network automatically adds the Android `ACCESS_NETWORK_STATE` and `ACCESS_WIFI_STATE` permissions and needs no config plugin or manual native setup.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/network/
7. **verified** (netinfo 12.0.1 / Expo SDK 57 docs) — @react-native-community/netinfo is at 12.0.1 with peer `react-native: >=0.59`, and Expo's own SDK 57 page for it reports `inExpoGo: true` ("Included in Expo Go"), so it too would run under `npx expo start` without a dev build.  
   Source: https://registry.npmjs.org/@react-native-community/netinfo/latest + https://docs.expo.dev/versions/v57.0.0/sdk/netinfo/
8. **verified** (netinfo master README (12.x)) — NetInfo computes `isInternetReachable` by issuing HTTP requests to a configurable `reachabilityUrl` defaulting to `https://clients3.google.com/generate_204` (204-status test, 5s short / 60s long retry intervals, 15s per-request timeout) — a recurring third-party call to Google from a Dutch healthcare app.  
   Source: https://raw.githubusercontent.com/react-native-netinfo/react-native-netinfo/master/README.md
9. **verified** (netinfo CHANGELOG @ master) — NetInfo 12.0.0 (Feb 2026) switched to `NEHotspotNetwork` and raised the floor to iOS 14+, with New Architecture support for Android and iOS added in 11.5.0 (Jan 2026).  
   Source: https://raw.githubusercontent.com/react-native-netinfo/react-native-netinfo/master/CHANGELOG.md
10. **verified** (react-native 0.86.3 / abort-controller 3.0.0) — React Native 0.86.3 installs global `AbortController` and `AbortSignal` via `polyfillGlobal(...)` in `Libraries/Core/setUpXHR.js`, sourced from the `abort-controller` npm package version 3.0.0.  
   Source: node_modules/react-native/Libraries/Core/setUpXHR.js and node_modules/abort-controller/package.json
11. **verified** (abort-controller 3.0.0 / react-native 0.86.3) — That polyfill has no `AbortSignal.timeout()` (the string "timeout" appears zero times in abort-controller 3.0.0's dist bundle) and `polyfillGlobal` replaces any engine-provided `AbortSignal` unconditionally, so request timeouts must be written as `setTimeout(() => controller.abort(), ms)` plus `clearTimeout` in a `finally`.  
   Source: grep over node_modules/abort-controller/dist/abort-controller.js and react-native/Libraries/Utilities/PolyfillFunctions.js
12. **verified** (whatwg-fetch 3.6.20 bundled in react-native 0.86.3) — `fetch` in RN 0.86.3 is whatwg-fetch 3.6.20, which does honour `signal` — it registers an `abort` listener that calls `xhr.abort()` and rejects with `new DOMException('Aborted', 'AbortError')`, so a timeout is distinguishable from a genuine network failure by `err.name === 'AbortError'`.  
   Source: node_modules/whatwg-fetch/dist/fetch.umd.js (lines 536, 626-632, 579) and Libraries/Network/fetch.js
13. **verified** (React Native 0.86 docs) — The React Native 0.86 networking doc covers fetch and XMLHttpRequest but says nothing about timeouts, cancellation, AbortController or retries — there is no built-in timeout to configure.  
   Source: https://reactnative.dev/docs/0.86/network
14. **verified** (Android Studio emulator console docs, read 2026-09-11) — Android emulator offline/slow is officially scriptable: `adb emu gsm data off` (or `gsm data off` over `telnet localhost 5554` + `auth <token from ~/.emulator_console_auth_token>`), restored with `gsm data home`, plus `network delay gprs` and `network speed edge` for slow-connection testing.  
   Source: https://developer.android.com/studio/run/emulator-console
15. **verified** (Apple archive networking guide, read 2026-09-11) — For iOS, Apple documents Network Link Conditioner as the tool that "can simulate various network conditions, including reduced bandwidth, high latency, DNS delays, packet loss, and so on" (shipped in Additional Tools for Xcode), and advises retrying transient failures while gradually increasing the wait between repeated failed attempts.  
   Source: https://developer.apple.com/library/archive/documentation/NetworkingInternetWeb/Conceptual/NetworkingOverview/WhyNetworkingIsHard/WhyNetworkingIsHard.html

### Open uncertainties

- `adb shell svc wifi disable` / `svc data disable` appeared only in community write-ups in my search results, not in any developer.android.com page I opened — prefer the officially documented `adb emu gsm data off` and treat `svc` as unverified.
- I could not open a doc confirming that Network Link Conditioner's macOS System Settings pane shapes the iOS Simulator's traffic (the Apple page I read describes the tool without saying whether it runs on the Mac or on a device); the widely-used practice of throttling the host to affect the Simulator is unverified here.
- I could not verify that `xcrun simctl` has no network on/off subcommand — `xcrun simctl` is not available on this machine ("unable to find utility simctl"), so the claim that there is no simctl network toggle is unverified.
- expo-network's CHANGELOG on `main` already shows a 58.0.0 entry dated 2026-09-10 while npm `latest` is 57.0.2; I did not confirm which version `npx expo install` resolves to under an SDK 57 project, though the SDK-aligned 57.0.x is expected.
- Expo's docs assert NetInfo is "Included in Expo Go" for SDK 57, but I did not verify against an actual Expo Go 57 binary that its native module is present; if that claim is stale, NetInfo becomes a dev-build cost and expo-network is the only safe choice.
- I did not benchmark how quickly `useNetworkState()` reports a transition on either platform, so the banner's perceived latency on airplane-mode toggles is unmeasured.
- Whether the Hermes engine in RN 0.86.3 natively ships `AbortSignal.timeout` is moot but untested — `polyfillGlobal` overwrites the global either way, which is what I verified.

### Adversarial verification

**Verdict:** partly-wrong

Claims the verifier refuted or corrected:

- **Claim:** Fact 11: "`polyfillGlobal` replaces any engine-provided `AbortSignal` unconditionally."  
  **Correction:** Refuted by the exact file the digest cites. `polyfillGlobal` delegates to `polyfillObjectProperty`, which reads the existing descriptor and bails out when one exists and is non-configurable: `if (descriptor && !configurable) { console.error('Failed to set polyfill. ' + name + ' is not configurable.'); return; }`. It also installs a lazy getter via `defineLazyObjectProperty` rather than assigning the value directly, and in `__DEV__` it preserves the original as `originalAbortSignal`. So the replacement is conditional and reversible, not unconditional. The practical conclusion the digest draws from it is still correct — `AbortSignal.timeout` is unavailable (the string "timeout" occurs 0 times in abort-controller 3.0.0's dist bundle, confirmed by grep), so `setTimeout(() => controller.abort(), ms)` plus `clearTimeout` in a `finally` remains the right pattern — but the stated mechanism is wrong and the digest's final uncertainty ("polyfillGlobal overwrites the global either way, which is what I verified") asserts something the source contradicts.  
  Source: node_modules/react-native/Libraries/Utilities/PolyfillFunctions.js (react-native 0.86.3) and node_modules/abort-controller/dist/abort-controller.js (3.0.0)
- **Claim:** Fact 14: Apple "documents Network Link Conditioner as the tool that 'can simulate various network conditions...' (shipped in Additional Tools for Xcode)", marked verified=true.  
  **Correction:** Partly unsupported by the cited page. The quoted sentence is real and reads verbatim "Xcode provides a tool called Network Link Conditioner that can simulate various network conditions, including reduced bandwidth, high latency, DNS delays, packet loss, and so on." But the page never says "Additional Tools for Xcode", and never states whether the tool runs on the Mac or on a device — that parenthetical is recalled, not read, and should be verified=false. The retry advice is supported and is in fact stronger than the digest's paraphrase: "Avoid retrying too quickly even when the network reachability APIs tell your application that the network has changed. When connections fail repeatedly, you should gradually increase the amount of time you wait between attempts until you reach a reasonably long retry interval (15 minutes, for example)." Note the tension with the recommendation's "at most two attempts with ~1s then ~3s backoff" — defensible for a foreground user-initiated send, but it is not what the cited page advises, so do not cite Apple as backing those numbers. The digest's own uncertainty list already flags that host-side Network Link Conditioner shaping of Simulator traffic is unverified; that flag is correct and the fact entry should not have been marked verified.  
  Source: https://developer.apple.com/library/archive/documentation/NetworkingInternetWeb/Conceptual/NetworkingOverview/WhyNetworkingIsHard/WhyNetworkingIsHard.html (read 2026-09-11)
- **Claim:** Fact 3: expo-network "exports `useNetworkState()` ... `isAirplaneModeEnabledAsync()` and `getIpAddressAsync()`", listed flat with no platform qualifiers.  
  **Correction:** Incomplete rather than false, and the omission matters for an iOS-primary project. The SDK 57 page badges `isAirplaneModeEnabledAsync(): Promise<boolean>` as **Android only** — it is not available on iOS, so it cannot be used as an iOS fallback for the missing internet probe described in Fact 4. The rest of Fact 3 is confirmed verbatim: `useNetworkState(): NetworkState` is documented as "This method initiates a listener for network state changes and cleans up before unmounting", `addNetworkStateListener(listener: (event: NetworkState) => void): EventSubscription` returns a subscription with a remove function, `getIpAddressAsync(): Promise<string>` returns `0.0.0.0` on failure, and NetworkState is `{ isConnected?, isInternetReachable?, type? }`.  
  Source: https://docs.expo.dev/versions/v57.0.0/sdk/network/ (Expo SDK 57, v57.0.0)

Expo Go compatibility of every package named:

| Package | In Expo Go | Config plugin | Dev build needed | Note |
|---|---|---|---|---|
| expo-network | yes | no | no | SDK 57 API page badges read verbatim "Android, iOS, tvOS, Web, Included in Expo Go"; install is `npx expo install expo-network`, no config plugin, and the page states "The permissions ACCESS_NETWORK_STATE and ACCESS_WIFI_STATE are added automatically." Cross-checked against expo/expo@sdk-57 packages/expo/bundledNativeModules.json, which pins "expo-network": "~57.0.2" (identical to the copy inside the project's own expo 57.0.22). Runs from a clean clone under `npx expo start` at zero build cost. |
| @react-native-community/netinfo | yes | no | no | The SDK 57 page exists and its badges also read verbatim "Android, iOS, tvOS, Web, Included in Expo Go"; bundledNativeModules.json on sdk-57 pins the exact version "12.0.1". npm latest is 12.0.1 with peerDependencies {react: "*", react-native: ">=0.59"}. IMPORTANT CAVEAT the digest omits: the Expo page says reading the Wi-Fi `ssid`/`bssid` properties does require extra setup (location permission plus iOS entitlements in app.json) and a rebuild via `eas build` or `expo run:ios` — so the base module is Expo Go-safe, but the SSID subset is a dev-build cost. Nothing in the digest's recommendation touches SSID. |
| react-native | yes | no | no | The Expo Go client runtime itself; project has 0.86.3 installed (node_modules/react-native/package.json). Everything the digest relies on from it — the `AbortController`/`AbortSignal` polyfill in Libraries/Core/setUpXHR.js and whatwg-fetch 3.6.20 behind Libraries/Network/fetch.js — is pure JS already present in the client, so no plugin and no dev build. |
| expo | yes | no | no | Installed at 57.0.22; it is the module that ships bundledNativeModules.json used by `npx expo install` to resolve SDK-aligned versions. Resolves the digest's open uncertainty about the 58.0.0 entry on expo-network's `main` CHANGELOG (dated 2026-09-10): under this SDK 57 project `npx expo install expo-network` resolves the pinned `~57.0.2`, not 58.x. |
| expo-image-picker | yes | no | no | Not named by this digest, but implied by the case study's photo step. Present in expo/expo@sdk-57 bundledNativeModules.json at "~57.0.17", so it is Expo Go-resolvable; a config plugin is optional (it exists only to customise the iOS permission strings) rather than required. I did not open its SDK 57 API page this run, so treat the Expo Go badge as inferred from the bundled list only. |
| expo-camera | yes | no | no | Not named by this digest, but implied by the case study's camera step. Present in expo/expo@sdk-57 bundledNativeModules.json at "~57.0.5". Same caveat: I did not open its SDK 57 API page this run, so the Expo Go badge is inferred from the bundled list only, and its config plugin (permission strings) is optional rather than required. |


## Topic: template-audit

*Expo SDK 57 'default' template (create-expo-app 4.0.0): what it ships, what to keep, and what to strip for a small production-quality e-consult form app*

### Recommendation

KEEP (hard requirements — removing any breaks the app): expo, expo-router, expo-constants, expo-linking, react-native-safe-area-context, react-native-screens, react, react-native, typescript, @types/react. The first four after expo-router are non-optional peers of expo-router 57.0.21 (verified in its package.json), so a "cleanup" PR that drops expo-constants or expo-linking because "nothing imports them" will break the router.

KEEP (cheap and earns its place here): expo-splash-screen (the app.json plugin entry references it — remove the package only if you also remove the plugin entry), expo-status-bar, expo-system-ui (sets the root/Android window background, which matters for the dark-mode + large-text audience).

DROP for a plain form app: @expo/ui, expo-glass-effect, expo-symbols (all three are transitive deps of expo-router anyway, so removing them from package.json is free and cannot break resolution), expo-web-browser, expo-device, expo-font (no custom fonts — and for elderly Dutch GP patients you want the system font so iOS Dynamic Type / Android font scaling works out of the box), react-native-web + react-dom (react-dom is an optional peer; you are iOS-simulator-first), and react-native-gesture-handler. Drop react-native-reanimated AND react-native-worklets together or not at all (worklets is a non-optional peer of reanimated 4.5.1) — drop both once you delete the template's AnimatedSplashOverlay, unless you want reanimated for the sending-state transitions, in which case keep the pair.

expo-image is a judgement call: it is not a peer of anything, but your flow shows a captured photo preview and expo-image gives you caching, contentFit and a recyclingKey that plain <Image> does not. Keep it if the preview is more than a thumbnail; otherwise RN's Image is one less dep.

ADD: expo-image-picker — it is "Included in Expo Go" on SDK 57 (verified on the v57.0.0 SDK page) and covers both camera and library, so the whole flow still satisfies the "clean clone + npx expo start" constraint with zero custom dev build. Configure its photosPermission/cameraPermission strings in Dutch via the config plugin in app.json; those strings are used when you later make a build, and writing them now costs nothing.

ROOT _LAYOUT: keep the template's pattern but import ThemeProvider/DefaultTheme/DarkTheme from 'expo-router', not '@react-navigation/native' — expo-router 57 vendors React Navigation and there is no top-level @react-navigation in node_modules. Do not add @react-navigation/native to package.json; that is the single most likely wrong move when porting SDK 53-era snippets. Delete the SplashScreen.preventAutoHideAsync() + AnimatedSplashOverlay dance unless you have real async bootstrap work; with no fonts to load it only adds a frame of blank screen.

EXPERIMENTS: keep typedRoutes — with a recipient id and a question flow you get compile-time checking of every Href, and the cost is only that route types live in git-ignored expo-env.d.ts/.expo/types. Because of that, add a `typecheck` script that runs `npx expo customize tsconfig.json` (or a dev-server start) before `tsc --noEmit`, or a reviewer doing a clean clone + typecheck will see spurious Href errors. Remember typed routes reject relative paths and require object-form Hrefs for dynamic segments. Keep reactCompiler too — it is on by default in the template, Babel is auto-wired on SDK 54+, and it only touches your app code — but know it is still labelled experimental with a @beta Babel plugin, so if you hit a memoization-shaped bug in the sending state machine, flipping it to false is a legitimate one-line bisect step. Say that in the README rather than silently disabling it.

TEMPLATE FILES: CLAUDE.md is just `@AGENTS.md` and AGENTS.md just says "read https://docs.expo.dev/versions/v57.0.0/ before writing any code". Replace their contents with real project rules (the offline/sending state machine, the two-call submit order, accessibility invariants) rather than deleting them — a reviewer of a 2026 take-home reads those files. Keep .claude/settings.json enabling expo@claude-plugins-official; it costs nothing at runtime and pulls in Expo's own skills plus the Expo MCP server for docs, though note the MCP server needs a `/mcp` auth step so it is not usable by a reviewer who just clones.

AFTER reset-project: the script only rewrites src/ and deletes scripts/ — it leaves every dependency, both experiments flags, ~23 demo PNGs (react-logo@2x/@3x, expo-badge, tabIcons/, tutorial-web.png, logo-glow.png), the stale README (which still mentions the nonexistent app-example dir and a reset-project script whose file is now gone), and a dangling `"reset-project"` entry in package.json. Prune all of that by hand: delete the unused assets, remove the dead script entry, and rewrite README.md into real run instructions. Keep assets/images/icon.png, splash-icon.png, favicon.png and the three android-icon-* files, which app.json actually references.

**Packages involved:** expo, expo-router, expo-constants, expo-linking, expo-status-bar, expo-splash-screen, expo-system-ui, expo-image-picker, expo-image, react, react-native, react-native-safe-area-context, react-native-screens, react-native-reanimated, react-native-worklets, typescript, @types/react, @expo/ui, expo-glass-effect, expo-symbols, expo-web-browser, expo-device, expo-font, react-native-web, react-dom, react-native-gesture-handler, @expo/metro-runtime, @expo/log-box, @react-native-masked-view/masked-view

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (create-expo-app 4.0.0 / SDK 57) — The template as generated ships 21 runtime deps — @expo/ui ~57.0.18, expo ~57.0.22, expo-constants, expo-device, expo-font, expo-glass-effect, expo-image, expo-linking, expo-router ~57.0.21, expo-splash-screen, expo-status-bar, expo-symbols, expo-system-ui, expo-web-browser, react 19.2.3, react-dom 19.2.3, react-native 0.86.3, react-native-gesture-handler ~2.32.0, react-native-reanimated 4.5.1, react-native-safe-area-context ~5.7.0, react-native-screens ~4.26.0, react-native-web ~0.21.0, react-native-worklets 0.10.1 — plus scripts start/reset-project/android/ios/web/lint.  
   Source: the main checkout — git show 7bb190dcece8f7478883f82d98c544e0ff2aa8df:package.json (commit message: 'Generated by create-expo-app 4.0.0')
2. **verified** (SDK 57 template) — The template root layout imports ThemeProvider, DarkTheme and DefaultTheme from 'expo-router' — NOT from '@react-navigation/native' — calls SplashScreen.preventAutoHideAsync() at module scope, renders <AnimatedSplashOverlay/> + <AppTabs/>, and loads NO fonts (there is no useFonts call anywhere in the template).  
   Source: git show 7bb190d:src/app/_layout.tsx in the local repo
3. **verified** (expo-router 57.0.21) — expo-router 57.0.21 vendors React Navigation and re-exports it (build/exports.d.ts: `export { ThemeProvider } from './react-navigation/core/theming/ThemeProvider'`), and node_modules has no top-level @react-navigation package — so you must not add @react-navigation/native yourself.  
   Source: node_modules/expo-router/build/exports.d.ts lines 23-24; ls node_modules/@react-navigation
4. **verified** (expo-router 57.0.21) — expo-router 57.0.21 lists @expo/ui, expo-glass-effect, expo-symbols, react-native-screens, @react-native-masked-view/masked-view and @expo/metro-runtime as its own `dependencies`, so deleting @expo/ui, expo-glass-effect and expo-symbols from package.json is safe — npm still installs them transitively.  
   Source: node_modules/expo-router/package.json `dependencies` field
5. **verified** (expo-router 57.0.21) — expo-router 57.0.21's NON-optional peerDependencies are expo, expo-constants ^57.0.18, expo-linking ^57.0.10, react, react-native, react-native-safe-area-context >=5.4.0, react-native-screens ^4.26.0, @expo/log-box ^57.0.4 and @expo/metro-runtime ^57.0.15 — removing expo-constants, expo-linking, safe-area-context or screens breaks the app.  
   Source: node_modules/expo-router/package.json peerDependencies + peerDependenciesMeta
6. **verified** (expo-router 57.0.21) — expo-router 57.0.21 marks react-native-gesture-handler, react-native-reanimated, react-native-web, react-dom, @testing-library/react-native and react-server-dom-webpack as OPTIONAL peers (peerDependenciesMeta.optional=true), so all six can be removed from a native-only form app without breaking routing.  
   Source: node_modules/expo-router/package.json peerDependenciesMeta
7. **verified** (react-native-reanimated 4.5.1) — react-native-reanimated 4.5.1 declares react-native-worklets 0.10.x as a non-optional peer (and react-native '0.83 - 0.86'), so the two must be kept or removed together — you cannot drop worklets while keeping reanimated.  
   Source: node_modules/react-native-reanimated/package.json peerDependencies
8. **verified** (react-native-screens 4.26.2) — react-native-screens 4.26.2 peers on only react and react-native, so it carries no hidden requirement of its own — it is required because expo-router peers on it, not because of expo-image/reanimated.  
   Source: node_modules/react-native-screens/package.json peerDependencies
9. **verified** (SDK 57 template) — Grepping the template's src/ at the generated commit shows expo-font, expo-constants, expo-system-ui, expo-status-bar, expo-linking, @expo/ui, expo-glass-effect and react-native-gesture-handler are imported by ZERO template files; expo-device is used only in src/app/index.tsx, expo-symbols only in collapsible.tsx/explore.tsx, expo-web-browser only in external-link.tsx, and expo-image + reanimated + worklets only in the animated splash and explore screens.  
   Source: git grep -l over 7bb190d -- src in the local repo
10. **verified** (SDK 57 template) — tsconfig.json extends expo/tsconfig.base with strict:true and two path aliases — `@/*` -> ./src/* and `@/assets/*` -> ./assets/* — and its `include` lists `.expo/types/**/*.ts` and `expo-env.d.ts`, both of which are git-ignored by the template .gitignore.  
   Source: tsconfig.json and .gitignore
11. **verified** (Expo Router / SDK 57 docs) — experiments.typedRoutes generates expo-env.d.ts (root) and .expo/types only when the dev server runs, requires `npx expo customize tsconfig.json` on CI, does not support relative paths (absolute Hrefs only), and needs object-form Href for dynamic routes — so on a clean clone `tsc --noEmit` before `npx expo start` has no route types.  
   Source: https://docs.expo.dev/router/reference/typed-routes/
12. **verified** (SDK 57 (v57.0.0 docs)) — experiments.reactCompiler is documented only as 'Experimentally enable React Compiler'; Babel is auto-configured on SDK 54+, the underlying babel-plugin-react-compiler is still referenced as @beta, it runs on app code only (no node_modules, client bundles only), and class components are not optimized.  
   Source: https://docs.expo.dev/versions/v57.0.0/config/app/ and https://docs.expo.dev/guides/react-compiler/
13. **verified** (SDK 57 template) — CLAUDE.md is a single line `@AGENTS.md`, and AGENTS.md is two lines: a '# Expo HAS CHANGED' heading and 'Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.' — nothing project-specific; .claude/settings.json contains only {"enabledPlugins":{"expo@claude-plugins-official":true}}.  
   Source: CLAUDE.md, AGENTS.md, .claude/settings.json
14. **verified** (SDK 57 template) — scripts/reset-project.js prompts 'move existing files to /example instead of deleting them? (Y/n)' (default Y), moves or deletes BOTH src/ and scripts/, then writes a bare src/app/index.tsx and a src/app/_layout.tsx containing only `<Stack />` — it never touches package.json, app.json, assets/, README.md, AGENTS.md, CLAUDE.md or .claude/, so all 21 deps (including @expo/ui, expo-symbols, expo-web-browser), both experiments flags, the splash plugin, ~23 demo PNGs, and a now-broken `"reset-project": "node ./scripts/reset-project.js"` script entry all remain; the README's claim that it creates 'app-example' is stale (it is /example, already git-ignored).  
   Source: git show 7bb190d:scripts/reset-project.js, README.md, .gitignore, and the post-reset working tree

### Open uncertainties

- The working copy at the main checkout had already been reset and pruned, and its HEAD advanced from 7bb190d to f39fb587 between two of my git calls (another agent or the parent workflow committed mid-run). All template facts above are pinned to the immutable SHA 7bb190dcece8f7478883f82d98c544e0ff2aa8df whose commit message is 'Generated by create-expo-app 4.0.0', so they reflect the genuine generated output, but I read that output from this local clone rather than from the expo/expo GitHub tree.
- The expo/expo main branch templates directory is already on SDK 58 (expo-template-default 58.0.0, RN 0.87.1, gesture-handler ~3.2.1, reanimated 4.6.0), so I could NOT diff the local copy against an upstream SDK 57 tag; I did not locate the sdk-57 branch/tag of templates/expo-template-default this run.
- I did not open a dedicated docs page confirming that ThemeProvider moved from @react-navigation/native to expo-router as an SDK 57 *change* — I verified only the end state (expo-router's own exports.d.ts re-exports it, the template imports it from expo-router, and no top-level @react-navigation exists). The claim that this is new in 57 is inference, not a read source.
- Expo's Claude plugin details (skills list, Expo MCP server, `claude plugin install expo@claude-plugins-official`) come from https://docs.expo.dev/agents/claude/ and a web search summary; I did not open the anthropics/claude-plugins-official marketplace.json to confirm the exact plugin manifest, nor did I verify which skills ship in the SDK 57-era version.
- I confirmed expo-image-picker is 'Included in Expo Go' on the v57.0.0 SDK page, but I did NOT verify that the full template as generated (reanimated 4.5.1 + worklets 0.10.1 on the New Architecture) runs unmodified in Expo Go SDK 57, nor did I check expo-camera's Expo Go status — if you want in-app camera rather than the system picker, that needs separate verification.
- Whether removing react-native-web and react-dom breaks `expo lint` or `npx expo start --web` is untested; they are optional peers of expo-router, but the template's app.json still declares a `web` block with output:'static' and a favicon, so the web script would need removing too.
- The reactCompiler risk assessment is qualitative. The v57.0.0 app config page gives only the one-line 'Experimentally enable React Compiler' description with no documented caveats, and the react-compiler guide lists no known-bad patterns beyond class components — I found no SDK 57-specific bug list, so 'flip it off to bisect' is my engineering advice, not a documented remedy.

### Adversarial verification

**Verdict:** partly-wrong

Claims the verifier refuted or corrected:

- **Claim:** Fact 1: 'The template as generated ships 21 runtime deps' followed by an enumeration.  
  **Correction:** It ships 23 runtime dependencies (plus 2 devDependencies). The enumerated list in the fact itself contains 23 names, so the number contradicts its own evidence; the error is repeated in fact 14 and twice in the recommendation ('all 21 deps'). The 23 names and all version ranges are otherwise exactly right.  
  Source: git show 7bb190d:package.json | Object.keys(deps).length => 23, devDeps => 2, at the main checkout
- **Claim:** Fact 5: 'removing expo-constants, expo-linking, safe-area-context or screens breaks the app.'  
  **Correction:** True for the first three, but wrong for react-native-screens: it is simultaneously listed in expo-router 57.0.21's own `dependencies` ('react-native-screens': '^4.26.0'), so npm still installs and autolinks it if you delete it from package.json — exactly the reasoning fact 4 uses to call @expo/ui/expo-glass-effect/expo-symbols safe to remove. The digest applies the transitive-dependency argument inconsistently.  
  Source: node_modules/expo-router/package.json (v57.0.21) — react-native-screens appears in both `dependencies` and `peerDependencies`
- **Claim:** Fact 9: 'expo-symbols only in collapsible.tsx/explore.tsx ... expo-image + reanimated + worklets only in the animated splash and explore screens.'  
  **Correction:** Three import-location errors: expo-symbols is also imported by src/components/app-tabs.web.tsx (3 files, not 2); expo-image is also imported by src/components/web-badge.tsx (4 files); react-native-reanimated is imported by animated-icon.tsx, animated-icon.web.tsx and ui/collapsible.tsx but NOT by explore.tsx. The 'imported by ZERO template files' half of the claim (expo-font, expo-constants, expo-system-ui, expo-status-bar, expo-linking, @expo/ui, expo-glass-effect, react-native-gesture-handler) is correct, and react-native-screens is also imported zero times.  
  Source: git grep -l "from '<module>" 7bb190d -- src, run per module in the local repo
- **Claim:** Fact 12: 'the underlying babel-plugin-react-compiler is still referenced as @beta.'  
  **Correction:** Not supported by the cited page for SDK 57. The React Compiler guide states 'Babel is automatically configured in Expo SDK 54 and later'; the `babel-plugin-react-compiler@beta` install instruction appears only in the SDK 53-and-earlier section, so it does not describe SDK 57. The rest of the fact (app-code only / no node_modules, client bundles only, class components not optimized, 'Experimentally enable React Compiler' as the whole app-config description) is verbatim correct.  
  Source: https://docs.expo.dev/guides/react-compiler/ and https://docs.expo.dev/versions/v57.0.0/config/app/
- **Claim:** Recommendation: 'Keep assets/images/icon.png, splash-icon.png, favicon.png and the three android-icon-* files, which app.json actually references.'  
  **Correction:** Incomplete in a way that breaks the build if followed literally: app.json also has `ios.icon: './assets/expo.icon'`, so the entire assets/expo.icon/ directory (icon.json, Assets/grid.png, 'Assets/expo-symbol 2.svg') must be kept too. Deleting everything not on the digest's keep-list removes the iOS icon source.  
  Source: app.json (expo.ios.icon) + git ls-tree -r 7bb190d -- assets
- **Claim:** Fact 14 / recommendation: '~23 demo PNGs (react-logo@2x/@3x, expo-badge, tabIcons/, tutorial-web.png, logo-glow.png)' are left behind.  
  **Correction:** Overcounted. assets/ holds 23 files total, of which 17 are PNGs; only 14 are unused demo images (expo-badge, expo-badge-white, expo-logo, logo-glow, react-logo, react-logo@2x, react-logo@3x, 6 tabIcons, tutorial-web). The other files are referenced by app.json. The substantive point — that reset-project deletes none of them — is correct.  
  Source: git ls-tree -r --name-only 7bb190d -- assets, and `find assets -type f` on the post-reset working tree
- **Claim:** Fact 11: typed routes 'generates expo-env.d.ts (root) and .expo/types only when the dev server runs'.  
  **Correction:** Substantively right but two gaps against the cited page: (a) the page describes the feature as still in beta ('While the feature is in beta, you can enable it by setting experiments.typedRoutes to true'), which the digest never mentions while recommending you keep it; (b) the page documents only the git-ignored expo-env.d.ts and the tsconfig modification — it does not mention .expo/types (that part is inferable from the template tsconfig's include array, not from the doc). The `npx expo customize tsconfig.json` escape hatch, no-relative-paths, and object-form Href for dynamic routes are all verbatim correct.  
  Source: https://docs.expo.dev/router/reference/typed-routes/ and tsconfig.json
- **Claim:** Fact 3: cites 'build/exports.d.ts lines 23-24' for the ThemeProvider re-export.  
  **Correction:** Cosmetic line-number slip: DarkTheme is line 22, DefaultTheme line 23, ThemeProvider line 24. The claim's substance is fully confirmed — expo-router 57.0.21 re-exports all three from vendored './react-navigation/...' paths and there is no @react-navigation directory anywhere under node_modules (checked top-level and nested to depth 4), so the 'do not add @react-navigation/native' advice stands.  
  Source: grep -n on node_modules/expo-router/build/exports.d.ts; ls + find for @react-navigation under node_modules
- **Claim:** Digest-wide framing: recommends expo-splash-screen partly on its visual value for the target audience.  
  **Correction:** Not a factual error but an omitted constraint the digest should have surfaced: the SDK 57 splash-screen page warns that 'From SDK 52... Expo Go and development builds cannot fully replicate the splash screen experience' and 'Expo Go will show your app icon instead of the splash screen.' Since the whole project is pinned to 'clean clone + npx expo start', any splash tuning is invisible to the reviewer.  
  Source: https://docs.expo.dev/versions/v57.0.0/sdk/splash-screen/
- **Claim:** Uncertainty 5: 'I did not check expo-camera's Expo Go status — if you want in-app camera rather than the system picker, that needs separate verification.'  
  **Correction:** Now verified and resolvable: expo-camera is 'Included in Expo Go' on SDK 57, so it needs no dev build — but its platform line is 'Android (device only), iOS (device only)', meaning it will not function on the iOS simulator that this project targets first. expo-image-picker remains the correct choice, for a different reason than the digest gave.  
  Source: https://docs.expo.dev/versions/v57.0.0/sdk/camera/ and https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/
- **Claim:** Task-level premise (inherited by the digest): packages/expo/bundledNativeModules.json is 'the list of third-party libraries bundled in Expo Go'.  
  **Correction:** It is a version-alignment map for `npx expo install`, not an Expo Go inclusion list: it contains dev-only entries (eslint-config-expo, jest-expo), things that by definition are not in Expo Go (expo-dev-client), and libraries that require a dev build (@stripe/stripe-react-native) — while omitting @expo/log-box, which is native and does ship inside Expo Go via expo-router. Per-package 'Included in Expo Go' badges on the v57.0.0 SDK pages are the authoritative source, which is what I used above.  
  Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo/bundledNativeModules.json compared against node_modules/@expo/log-box/package.json (57.0.4, has ios/ + android/ + podspec)

Expo Go compatibility of every package named:

| Package | In Expo Go | Config plugin | Dev build needed | Note |
|---|---|---|---|---|
| expo | yes | no | no | Baseline runtime; nothing to decide here. |
| expo-router | yes | yes | no | Docs: "If you are using the default template... expo-router's config plugin is already configured in your app config." Local app.json does list "expo-router" in plugins. |
| expo-constants | yes | no | no | Non-optional peer of expo-router 57.0.21 (node_modules/expo-router/package.json) — keep it. |
| expo-linking | yes | no | no | Non-optional peer of expo-router 57.0.21 — keep it even though no template file imports it. |
| expo-status-bar | yes | no | no | Cheap; no plugin entry needed. |
| expo-splash-screen | yes | yes | no | Docs: config plugin is 'the recommended method'; app.json already has the entry. Caveat the digest misses: 'Expo Go will show your app icon instead of the splash screen' since SDK 52, so your reviewer will never see the configured splash in Expo Go. |
| expo-system-ui | yes | no | no | Its v57.0.0 page is the one SDK page here that does NOT carry the 'Included in Expo Go' badge; I treat it as in Expo Go because the SDK 57 pin list includes it and the default template (which must run in Expo Go) depends on it. Plugin is optional (CNG only). |
| expo-image-picker | yes | yes | no | Digest's ADD recommendation is confirmed. Plugin props photosPermission/cameraPermission/microphonePermission; docs also warn it adds RECORD_AUDIO on Android unless microphonePermission:false — set that, since an e-consult photo flow has no audio use. |
| expo-camera | yes | yes | no | Resolves the digest's open uncertainty: in-app camera does NOT need a dev build. But it is 'device only' on both platforms, so it will not work in the iOS simulator — for a simulator-first demo, expo-image-picker's system camera/library UI is the safer pick. |
| expo-image | yes | no | no | Has an optional 'Configuration in app config' plugin for build-time settings; not required for a photo preview. |
| react | yes | no | no | Pure JS. |
| react-native | yes | no | no | Matches the template's 0.86.3. |
| react-native-safe-area-context | yes | no | no | Non-optional peer of expo-router (>= 5.4.0); also the only third-party lib actually imported by the template screens. |
| react-native-screens | yes | no | no | Installed 4.26.2; peers only react + react-native. It is both a dependency AND a non-optional peer of expo-router, so it resolves transitively either way. |
| react-native-reanimated | yes | no | no | Docs: 'No additional configuration is required. Reanimated Babel plugin is automatically configured in babel-preset-expo'. Optional peer of expo-router, so droppable. |
| react-native-worklets | yes | no | no | No /versions/v57.0.0/sdk/worklets/ docs page exists (404), so Expo Go status is inferred from the SDK 57 pin list plus reanimated 4.5.1's non-optional peer on worklets 0.10.x. Keep/drop as a pair with reanimated. |
| typescript | yes | no | no | Dev-only, pure JS. |
| @types/react | yes | no | no | Dev-only. |
| @expo/ui | yes | no | no | Safe to delete from package.json: it is a direct dependency of expo-router 57.0.21 (^57.0.18), so it still resolves. Note it is NOT listed for Web. |
| expo-glass-effect | yes | no | no | iOS/tvOS only. Also a direct dependency of expo-router, so removing it from package.json is free. |
| expo-symbols | yes | no | no | Direct dependency of expo-router, so removing it from package.json is free. |
| expo-web-browser | yes | no | no | Optional CNG plugin only. Used solely by the template's external-link.tsx; safe to drop. |
| expo-device | yes | no | no | No config plugin. Used only by the template's src/app/index.tsx; safe to drop. |
| expo-font | yes | no | no | Has an optional plugin 'recommended for Android and iOS' to embed fonts at build time — not needed if you drop it. No template file imports expo-font and there is no useFonts call anywhere at 7bb190d. |
| react-native-web | yes | no | no | Pure JS, web-only; optional peer of expo-router. If you drop it, also drop the "web" script and the app.json "web" block (output:'static' + favicon). |
| react-dom | yes | no | no | Pure JS; explicitly optional peer of expo-router. |
| react-native-gesture-handler | yes | no | no | Optional peer of expo-router; zero template imports. Safe to drop — but note expo-router's own dependency react-native-drawer-layout can pull gesture-handler in if you ever add a Drawer. |
| @expo/metro-runtime | yes | no | no | Pure JS. Both a dependency AND a non-optional peer of expo-router, so never list it yourself. |
| @expo/log-box | yes | no | no | Native, yet NOT present in bundledNativeModules.json sdk-57 — one concrete reason that file is not a reliable Expo Go inclusion list. It ships inside Expo Go by virtue of expo-router being bundled. |
| @react-native-masked-view/masked-view | yes | no | no | Transitive dependency of expo-router; never add it to package.json. |


## Topic: keyboard-safe-area

*Keyboard avoidance and safe areas for the e-consult flow in Expo SDK 57 / RN 0.86 (Expo Go only)*

### Recommendation

Ship the core-RN layout as the default and treat keyboard-controller as an optional upgrade.

Baseline (zero new dependencies, guaranteed to run from `npx expo start` in Expo Go):
1. Leave `app.json` alone — do NOT add `softwareKeyboardLayoutMode`. The default `"resize"` makes the Android window shrink for the keyboard, which is exactly what you want with a single ScrollView and no bottom tab bar; `"pan"` would break scrolling to the button.
2. One ScrollView per step, and put the primary action ("Volgende" / "Versturen") as the LAST CHILD INSIDE the scroll content, not in a pinned footer. This is the single change that removes the whole class of KeyboardAvoidingView bugs: RN's KAV measures its own onLayout frame and applies paddingBottom/height to itself, so a pinned footer + ScrollView + expo-router header needs a hand-tuned `keyboardVerticalOffset` and still lags on Android (Android listens to keyboardDidShow, not WillShow). A button inside the content also survives 200%+ Dynamic Type, which a fixed-height footer does not — relevant for elderly GP patients.
3. ScrollView props: `keyboardShouldPersistTaps="handled"` (otherwise the first tap on the button only dismisses the keyboard — a real two-tap bug for elderly users), `automaticallyAdjustKeyboardInsets` (iOS-only, default false; handles the keyboard inset on iOS with no KAV at all), `keyboardDismissMode={Platform.OS === 'ios' ? 'interactive' : 'on-drag'}`, and `contentContainerStyle={{ flexGrow: 1, padding: 16, paddingBottom: 24 + insets.bottom }}`.
4. Safe areas: expo-router already mounts SafeAreaProvider, and the Stack header already consumes the top inset — do not add a `SafeAreaView` with `top` under a header or you pay the inset twice. Use `useSafeAreaInsets()` and apply only `bottom` (plus `left`/`right`) as padding on the scroll content, since Android edge-to-edge is always on from SDK 54+ and content draws under the system bars. Do not mix SafeAreaView and useSafeAreaInsets on the same screen (documented flicker).
5. Only if the design demands a truly pinned footer: wrap the FOOTER (not the ScrollView) in `<KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : undefined} keyboardVerticalOffset={headerHeight}>`. Passing `undefined` on Android is Expo's own recommendation and avoids double-counting `adjustResize`.

Optional upgrade, still Expo Go-safe: `npx expo install react-native-keyboard-controller` (pins 1.21.9 for SDK 57), wrap the root `_layout.tsx` in `<KeyboardProvider>`, then use `KeyboardAwareScrollView` for the question/message steps and `KeyboardStickyView` for a footer button that tracks the keyboard frame-by-frame. Expo's SDK 57 reference page marks this library `inExpoGo: true`, so it needs no development build — but Expo's own keyboard-handling guide still carries a stale "not included in Expo Go" line, so verify on a real Expo Go SDK 57 client before you depend on it in a submission that must run from a clean clone, and call the discrepancy out in the README. Note it also requires reanimated (already present at 4.5.1).

For the sending/offline/error states: keep the same ScrollView shell and swap only the content, so the keyboard/inset behaviour is defined in one layout component (e.g. `FormScreenScaffold`) rather than repeated per screen.

**Packages involved:** expo, expo-router, react-native, react-native-safe-area-context, react-native-keyboard-controller, react-native-reanimated, react-native-screens

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (React Native 0.87 docs page) — KeyboardAvoidingView takes behavior 'height' | 'position' | 'padding', keyboardVerticalOffset (default 0), enabled (default true) and contentContainerStyle (only used with behavior='position'), and the docs state verbatim: "On both iOS and Android, setting behavior is recommended."  
   Source: reactnative.dev/docs/keyboardavoidingview (page served as RN 0.87 current docs; same API as 0.86)
2. **verified** (react-native 0.86.3) — In the RN 0.86.3 source shipped in this project, KeyboardAvoidingView subscribes to keyboardWillShow/keyboardWillHide on iOS but to keyboardDidShow/keyboardDidHide on Android (lines 198-213), so Android adjusts only after the keyboard has finished appearing rather than in sync with it.  
   Source: node_modules/react-native/Libraries/Components/Keyboard/KeyboardAvoidingView.js
3. **verified** (react-native 0.86.3) — The same source computes the shift from the view's own measured onLayout frame — `Math.max(frame.y + frame.height - keyboardY, 0)` where `keyboardY = keyboardFrame.screenY - keyboardVerticalOffset` — and applies it as `paddingBottom` (behavior='padding') or `height: this._initialFrameHeight - bottomHeight` (behavior='height'), which is why a KeyboardAvoidingView that is not full-screen, sits under an expo-router Stack header, or wraps only part of the screen shifts by the wrong amount unless keyboardVerticalOffset compensates for the header.  
   Source: node_modules/react-native/Libraries/Components/Keyboard/KeyboardAvoidingView.js lines 98-110, 236-279
4. **verified** (React Native 0.86 docs) — ScrollView's `automaticallyAdjustKeyboardInsets` is iOS-only, defaults to `false`, and "Controls whether the ScrollView should automatically adjust its contentInset and scrollViewInsets when the Keyboard changes its size" — i.e. it fixes scrollable content on iOS but does nothing for a button rendered outside/below the ScrollView and nothing at all on Android.  
   Source: reactnative.dev/docs/0.86/scrollview
5. **verified** (React Native 0.86 docs) — `keyboardShouldPersistTaps` defaults to `'never'` (a tap outside the focused input dismisses the keyboard and the child does NOT receive the tap), while `'handled'` keeps the keyboard up when a child handles the tap — so without `keyboardShouldPersistTaps="handled"` the patient must tap the next/send button twice.  
   Source: reactnative.dev/docs/0.86/scrollview
6. **verified** (React Native 0.86 docs) — `keyboardDismissMode` defaults to `'none'`; `'interactive'` exists only on iOS (on Android it behaves like `'none'`) and `'on-drag'` works on both, which is the accessible "scroll to dismiss" behaviour for elderly users.  
   Source: reactnative.dev/docs/0.86/scrollview
7. **verified** (Expo SDK 57 docs branch) — Expo's own SDK 57 reference page for react-native-keyboard-controller declares `platforms: ['android', 'ios', 'expo-go']` and `inExpoGo: true` in its front matter, i.e. the library is bundled in Expo Go for SDK 57 and needs no development build.  
   Source: raw.githubusercontent.com/expo/expo/sdk-57/docs/pages/versions/unversioned/sdk/keyboard-controller.mdx and docs.expo.dev/versions/v57.0.0/sdk/keyboard-controller/
8. **verified** (expo 57.0.22 / react-native-keyboard-controller 1.22.5 on npm (peerDeps: react-native-reanimated >=3.0.0)) — Expo pins the Expo Go-compatible version: this project's expo 57.0.22 lists `"react-native-keyboard-controller": "1.21.9"` in bundledNativeModules.json, so it must be added with `npx expo install` (npm's latest is 1.22.5, whose native code would not match the Expo Go binary).  
   Source: node_modules/expo/bundledNativeModules.json plus registry.npmjs.org/react-native-keyboard-controller/latest
9. **verified** (Expo SDK 57 docs branch / keyboard-controller blog (1.20.0 era)) — Expo's own keyboard-handling guide on the same sdk-57 branch still contains the contradictory line "The Keyboard Controller library is not included in Expo Go. See Create a development build for more information.", which appears to be stale — the library's author blog states "In Expo SDK 54, react-native-keyboard-controller lib was added to the Expo Go" and reactnative.directory reports `"expoGo": true` for it.  
   Source: raw.githubusercontent.com/expo/expo/sdk-57/docs/pages/guides/keyboard-handling.mdx; kirillzyusko.github.io/react-native-keyboard-controller/blog/expo-snacks; reactnative.directory/api/libraries?search=keyboard-controller
10. **verified** (Expo SDK 57 docs) — Expo's keyboard guide recommends KeyboardAvoidingView only for basic cases ("padding" on iOS, `undefined` on Android), points to react-native-keyboard-controller "For more complex keyboard interactions, such as larger scrollable entry forms with several text input fields", requires `KeyboardProvider` at the app root, and recommends `"softwareKeyboardLayoutMode": "pan"` only for apps with a Bottom Tab navigator.  
   Source: docs.expo.dev/guides/keyboard-handling/
11. **verified** (Expo SDK 57 app config reference) — `android.softwareKeyboardLayoutMode` accepts `"resize"` (default) or `"pan"` and "maps to the android:windowSoftInputMode property"; leaving the default `resize` means the Android window already shrinks for the keyboard, which is why stacking a padding-based KeyboardAvoidingView on top of it double-counts the keyboard height.  
   Source: docs.expo.dev/versions/v57.0.0/config/app/
12. **verified** (react-native-keyboard-controller 1.22.0 docs) — KeyboardStickyView (docs for keyboard-controller 1.22.0) "just moves the content along with keyboard and not resizing the inner view", with only `enabled` and `offset: { closed, opened }` props — the intended primitive for a pinned bottom action button.  
   Source: kirillzyusko.github.io/react-native-keyboard-controller/docs/api/components/keyboard-sticky-view
13. **verified** (react-native-safe-area-context 5.7.0 (installed)) — react-native-safe-area-context 5.7.0's SafeAreaView is "a regular View component with the safe area insets applied as padding or margin", and its `edges` prop accepts either an array of top/right/bottom/left or an object whose per-edge mode is `'additive'` (default, finalPadding = safeArea + padding), `'maximum'`, or `'off'`; `useSafeAreaInsets` returns `{top, right, bottom, left}` from the nearest provider and is "not updated synchronously".  
   Source: node_modules/react-native-safe-area-context/package.json + appandflow.github.io/react-native-safe-area-context/api/safe-area-view and /api/use-safe-area-insets
14. **verified** (React Navigation 7.x / Expo SDK 57 docs) — React Navigation 7 (the engine under expo-router's Stack) "automatically applies proper insets to the built-in UI elements of the navigators, such as headers, tab bars, and drawers", recommends `useSafeAreaInsets` over SafeAreaView, warns that "Using both SafeAreaView component and useSafeAreaInsets hook together can cause flickering", and says "Don't wrap your whole app in SafeAreaView" — so with a Stack header visible you only need the bottom (and horizontal, in landscape) inset yourself; Expo's safe-areas guide notes SafeAreaProvider must be added manually only "if you're not using Expo Router".  
   Source: reactnavigation.org/docs/handling-safe-area/ and docs.expo.dev/develop/user-interface/safe-areas/
15. **verified** (Expo SDK 54 changelog (carries forward to SDK 57)) — Android edge-to-edge is effectively permanent from SDK 54 onward: "With Expo SDK 54 and React Native 0.81 now targeting Android 16, edge-to-edge will be enabled in all Android apps, and cannot be disabled", the `edgeToEdgeEnabled` app.json property has no effect on Android 16, and Expo now toggles it via React Native's `enableEdgeToEdge` Gradle property instead of react-native-edge-to-edge — so content draws under the status and navigation bars and every screen must apply insets itself.  
   Source: expo.dev/changelog/sdk-54

### Open uncertainties

- Expo's SDK 57 docs contradict themselves on Expo Go support for react-native-keyboard-controller: the SDK reference page front matter says inExpoGo: true (and reactnative.directory + the library author's blog agree it landed in Expo Go in SDK 54), while docs/pages/guides/keyboard-handling.mdx on the same sdk-57 branch still says it is not included — I could not run Expo Go to settle it, so the recommendation keeps the core-RN path as the default.
- I did not verify by execution that the Expo Go SDK 57 binary contains keyboard-controller 1.21.9 native code; the 1.21.9 pin comes from expo 57.0.22's bundledNativeModules.json, which governs `npx expo install` resolution and is only strong evidence of Expo Go inclusion, not proof.
- Whether Android's `windowSoftInputMode=adjustResize` (Expo's default `"resize"`) still resizes the window reliably under always-on edge-to-edge on Android 15/16 is not stated in any doc I opened — adjustResize was deprecated at API 30 in favour of WindowInsetsAnimation, so the resize-based baseline should be smoke-tested on an Android 16 emulator.
- No doc I opened specifies how `automaticallyAdjustKeyboardInsets` interacts with `contentInsetAdjustmentBehavior` or with a button rendered outside the ScrollView on iOS; my recommendation sidesteps this by keeping the button inside the scroll content.
- react-native-keyboard-controller's npm peerDependencies list only `react-native-reanimated >=3.0.0`; I did not find an explicit statement that 1.21.9 is validated against reanimated 4.5.1 / react-native-worklets 0.10.1 beyond Expo's own version pin.
- The RN KeyboardAvoidingView 'jump' and double-padding failure modes on Android are inferred from reading the 0.86.3 implementation (keyboardDidShow listeners, LayoutAnimation, self-measured frame), not from an official statement of the limitation — no Expo or RN doc I opened lists them explicitly.
- I did not open any expo-router-specific doc on header height; if you need `keyboardVerticalOffset` for a pinned footer, the header height must come from `useHeaderHeight()` (React Navigation) — its availability through expo-router's Stack was not verified this run.

### Adversarial verification

**Verdict:** partly-wrong

Claims the verifier refuted or corrected:

- **Claim:** Recommendation step 5: "wrap the FOOTER in <KeyboardAvoidingView ... keyboardVerticalOffset={headerHeight}>", with uncertainty #7 saying the header height "must come from useHeaderHeight() (React Navigation)".  
  **Correction:** This does not work in this project as written. There is no @react-navigation/* package installed at all (ls node_modules/@react-navigation -> not present, and expo-router 57.0.21's dependencies list contains no @react-navigation entry). expo-router 57 VENDORS React Navigation under expo-router/build/react-navigation/, where useHeaderHeight is exported from build/react-navigation/elements/index.js, but it is not re-exported from expo-router's public entry (no useHeaderHeight in build/index.d.ts or build/exports.d.ts). So you would have to add a new dependency or import a deep internal build path — a real cost the digest does not flag. Prefer the digest's own step 2 (button inside the scroll content) which needs no header height.  
  Source: node_modules/expo-router/package.json, .../build/index.d.ts, .../build/exports.d.ts, .../build/react-navigation/elements/index.js (expo-router 57.0.21, installed)
- **Claim:** Fact 13 / recommendation step 4 cite docs.expo.dev/develop/user-interface/safe-areas/ for "SafeAreaProvider must be added manually only if you're not using Expo Router" / "expo-router already mounts SafeAreaProvider".  
  **Correction:** The conclusion is true but the cited source does not support it. The live Expo safe-areas page only says "You can skip installing react-native-safe-area-context if you have created a project using the default template. This library is installed as peer dependency for Expo Router library." — that is about INSTALLING the package, not about a provider being mounted. The real evidence is in the installed source: expo-router/build/ExpoRoot.js line 79 renders react_native_safe_area_context_1.SafeAreaProvider, and build/react-navigation/native-stack/views/NativeStackView.js line 53 wraps in SafeAreaProviderCompat. Cite the source, not the doc.  
  Source: docs.expo.dev/develop/user-interface/safe-areas/ (opened) vs node_modules/expo-router/build/ExpoRoot.js and .../build/react-navigation/native-stack/views/NativeStackView.js
- **Claim:** Fact 3: KeyboardAvoidingView "computes the shift ... `Math.max(frame.y + frame.height - keyboardY, 0)`".  
  **Correction:** Incomplete: that is only the default/'padding'/'position' branch. For behavior='height' the installed 0.86.3 source uses `Math.max(this.state.bottom + frame.y + frame.height - keyboardY, 0)` — it feeds the previous bottom back in, which is exactly why behavior='height' is the most jump-prone option. The rest of the fact (iOS keyboardWillShow/Hide vs Android keyboardDidShow/Hide at lines 198-213; paddingBottom for 'padding'; height: this._initialFrameHeight - bottomHeight for 'height') checks out verbatim against the file.  
  Source: node_modules/react-native/Libraries/Components/Keyboard/KeyboardAvoidingView.js (react-native 0.86.3, installed)
- **Claim:** Fact 11 second half: "leaving the default resize means the Android window already shrinks for the keyboard, which is why stacking a padding-based KeyboardAvoidingView on top of it double-counts the keyboard height."  
  **Correction:** Only the first half is in the source. docs.expo.dev/versions/v57.0.0/config/app/ says verbatim and no more: "Determines how the software keyboard will impact the layout of your application. This maps to the android:windowSoftInputMode property. Defaults to resize. Valid values: resize, pan." The double-counting consequence is the digest's own inference; I also opened expo.dev/blog/edge-to-edge-display-now-streamlined-for-android and it contains NO statement about adjustResize/windowSoftInputMode behaviour under edge-to-edge. Treat the digest's own uncertainty #3 as unresolved and smoke-test on an Android 16 emulator.  
  Source: docs.expo.dev/versions/v57.0.0/config/app/ and expo.dev/blog/edge-to-edge-display-now-streamlined-for-android (both opened 2026-09-11)
- **Claim:** Fact 14 cites expo.dev/changelog/sdk-54 for three things: edge-to-edge cannot be disabled, "the edgeToEdgeEnabled app.json property has no effect on Android 16", and "Expo now toggles it via React Native's enableEdgeToEdge Gradle property instead of react-native-edge-to-edge".  
  **Correction:** Only part of this is on the page I opened. The changelog does carry verbatim: "With Expo SDK 54 and React Native 0.81 now targeting Android 16, edge-to-edge will be enabled in all Android apps, and cannot be disabled", and it does say react-native-edge-to-edge is "no longer a dependency of the expo package". I could NOT extract the edgeToEdgeEnabled-has-no-effect sentence or the enableEdgeToEdge Gradle-property sentence from the page itself (only from a search-result summary), and docs.expo.dev/versions/v57.0.0/config/app/ no longer documents edgeToEdgeEnabled at all. The practical conclusion (content draws under system bars; apply insets yourself) stands; the two sub-claims are unverified as quoted.  
  Source: expo.dev/changelog/sdk-54 and docs.expo.dev/versions/v57.0.0/config/app/ (both opened)
- **Claim:** Fact 1 cites "reactnative.dev/docs/keyboardavoidingview (page served as RN 0.87 current docs; same API as 0.86)" and assumes the 0.86 API matches.  
  **Correction:** Not wrong, but the digest hedged unnecessarily and the version cited is off by one. I opened reactnative.dev/docs/0.86/keyboardavoidingview directly: it documents behavior enum('height','position','padding') with no default, contentContainerStyle (View Style, used when behavior is 'position'), enabled (boolean, default true), keyboardVerticalOffset (number, default 0), and states verbatim "Android and iOS both interact with this prop differently. On both iOS and Android, setting behavior is recommended." Cite the 0.86 page. Note this contradicts Expo's own guide, whose code sample passes `undefined` on Android — flag that tension in the README rather than presenting both as agreeing.  
  Source: reactnative.dev/docs/0.86/keyboardavoidingview and raw.githubusercontent.com/expo/expo/sdk-57/docs/pages/guides/keyboard-handling.mdx (both opened)

Expo Go compatibility of every package named:

| Package | In Expo Go | Config plugin | Dev build needed | Note |
|---|---|---|---|---|
| expo (~57.0.22) | yes | no | no | The Expo Go client for SDK 57 IS the expo runtime; bundledNativeModules.json is the version map npx expo install resolves against. Config plugins are irrelevant in Expo Go (no prebuild runs). |
| expo-router (~57.0.21) | yes | no | no | Ships an app.plugin.js (node_modules/expo-router/app.plugin.js) but the SDK 57 install doc does not ask you to add it to app.json, and plugins have no effect in Expo Go. Installed at 57.0.21. |
| react-native (0.86.3) | yes | no | no | KeyboardAvoidingView, ScrollView (automaticallyAdjustKeyboardInsets, keyboardShouldPersistTaps, keyboardDismissMode) and Keyboard are all core RN — the zero-risk baseline for this case study. |
| react-native-safe-area-context (~5.7.0, installed 5.7.0) | yes | no | no | Already in package.json at ~5.7.0. Verified in installed source that expo-router/build/ExpoRoot.js renders <SafeAreaProvider> itself, and its vendored NativeStackView wraps in SafeAreaProviderCompat — so you do NOT add a provider. |
| react-native-screens (~4.26.0, installed 4.26.2) | yes | no | no | Also a direct dependency of expo-router 57.0.21 (^4.26.0). Nothing to configure. |
| react-native-reanimated (4.5.1) | yes | no | no | Same page: "Reanimated Babel plugin is automatically configured in babel-preset-expo" — no manual babel.config.js work. Install command is `npx expo install react-native-reanimated react-native-worklets`. |
| react-native-worklets (0.10.1) | yes | no | no | Required peer of Reanimated 4; already in package.json at 0.10.1. No Expo SDK reference page of its own, so Expo Go status is inferred from the bundled-modules pin + the reanimated page's install command. |
| react-native-gesture-handler (~2.32.0) | yes | no | no | Not named in the digest's libraries list but present in the template and pulled in by navigation; listed for completeness. |
| react-native-keyboard-controller (1.21.9 for SDK 57) | yes | no | no | CONFLICT, real and still live today: docs.expo.dev/guides/keyboard-handling/ (and the same file on the sdk-57 branch) still says "The Keyboard Controller library is not included in Expo Go. See Create a development build for more information." Three independent sources outweigh that one stale prose line, so inExpoGo=true — but smoke-test on a real Expo Go SDK 57 client before you depend on it in a clean-clone submission. No app.plugin.js: unpkg listing for 1.21.9 shows only android/, common/, ios/, jest/, lib/, src/, LICENSE, README.md, package.json, the podspec and react-native.config.js. Install with `npx expo install` (npm latest is 1.22.5, which would not match the Expo Go binary). Peer deps on npm: react *, react-native *, react-native-reanimated >=3.0.0. |


## Topic: accessibility

*Accessibility APIs in React Native 0.86.3 / Expo SDK 57 for a VoiceOver- and TalkBack-usable e-consult form flow (labels, error announcement, focus management, touch targets, font scaling, testing)*

### Recommendation

NOT VERIFIED THIS RUN (mark down accordingly): VoiceOver does not run on the iOS Simulator — you must use Xcode's Accessibility Inspector against the Simulator, and test real VoiceOver speech on a physical device. Apple's doc pages (developer.apple.com HIG/documentation) are JS-rendered and returned title-only through WebFetch on every attempt; the claim came from developer.apple.com search snippets of Apple's archived "Testing Accessibility of iOS Apps" technote, not a page body I opened. Treat as strong-but-unconfirmed and verify before putting it in the write-up.

CONCRETE PLAN FOR THE E-CONSULT FORM

1. Error → input association. There is no aria-describedby/aria-errormessage in RN 0.86.3 (fact 1), and aria-live is Android-only (fact 2). So do all three layers:
   (a) Put the error text INTO the input's own accessible name: `accessibilityLabel={hasError ? \`${label}. Fout: ${errorText}\` : label}` — this is the only mechanism that works identically on VoiceOver and TalkBack.
   (b) Render the visible error node with `accessibilityLiveRegion="polite"` (Android picks it up automatically; no-op on iOS).
   (c) On iOS, call `AccessibilityInfo.announceForAccessibility(errorText)` when the error first appears. Gate it behind `await AccessibilityInfo.isScreenReaderEnabled()` and debounce — announcements fire on every render otherwise.
   Do NOT rely on accessibilityLabelledBy/nativeID: it is Android-only (fact 5).

2. Focus management between steps (recipient → questions → message → photo → sending → confirmation). On validation failure, move screen-reader focus to the first invalid field using the non-deprecated call: `AccessibilityInfo.sendAccessibilityEvent(inputRef.current, 'focus')` — pass the ref directly, no findNodeHandle (fact 4). For a TextInput you usually want both: `inputRef.current?.focus()` to raise the keyboard AND sendAccessibilityEvent to move the a11y cursor; they are independent. On route change under expo-router, announce the new step ("Stap 2 van 4: vragen") via announceForAccessibility after a short delay, since neither platform reliably announces RN screen transitions.

3. Grouping. Wrap each question (label + helper + control) in a `<View accessible accessibilityRole="radiogroup">` only when you want ONE swipe stop; note that `accessible` on a parent collapses all children into a single element and children become unreachable — for a text question you almost never want this, because the TextInput must stay individually focusable. Use grouping for the recipient cards (one stop per recipient: name + role + hint) and leave form fields ungrouped.

4. Choice questions. Use Pressable with `accessibilityRole="radio"` inside a parent `accessibilityRole="radiogroup"`, and `accessibilityState={{ checked: selected }}`. Pressable already gives you accessible=true and maps `disabled` into accessibilityState (fact 7), so do not duplicate it. Add `accessibilityHint` sparingly — on Android TalkBack hints cannot be turned off by the user, so a hint on every option becomes verbose noise for elderly users.

5. Touch targets + hitSlop. Target 48x48 as the single number (satisfies Android's 48dp and exceeds Apple's 44pt, fact 12). Prefer real minHeight/minWidth over hitSlop for the primary actions (Verstuur, Kies ontvanger, Verwijder foto) — hitSlop enlarges the press region but I could NOT confirm this run that it enlarges the VoiceOver/TalkBack activation frame, so a hitSlop-only target may be big for fingers and small for a screen reader. Use hitSlop only as a secondary cushion on small icon buttons like the photo "remove" X.

6. Font scaling for large text. Leave allowFontScaling at its default true (fact 9) — do not disable it anywhere; elderly Dutch patients are the whole point. Apply `maxFontSizeMultiplier` only on genuinely constrained chrome (a fixed-height header), around 1.5-2.0, and never on body copy, question text, or the message field. Read scale from `useWindowDimensions().fontScale`, NOT `PixelRatio.getFontScale()` — the latter returns the device pixel ratio (typically 3) when no font scale is set (fact 10), which will silently break any `if (fontScale > 1.5)` branch. Use fontScale to switch button rows from horizontal to vertical stacking at large scales rather than to shrink text.

7. State announcements for sending/offline. Give the send button `accessibilityState={{ busy: isSending, disabled: !canSend }}` and change its accessibilityLabel across the two calls ("Bezig met versturen", "Bericht verstuurd, bijlage wordt geüpload"). For the offline banner use accessibilityLiveRegion="polite" plus a one-shot announceForAccessibility on the offline→online transition. Give the confirmation screen's heading `accessibilityRole="header"` and focus it on mount.

8. Testing. iOS: Xcode → Open Developer Tool → Accessibility Inspector, target the Simulator, walk the element hierarchy and run the audit; then verify real speech order on a physical iPhone. Android: TalkBack on the emulator requires a Google Play system image (AOSP images generally lack Android Accessibility Suite) — enable via Settings → Accessibility → TalkBack per Android's testing guide, then check linear swipe order, that every element is reachable, and that alerts are read aloud. Budget for the fact that Accessibility Inspector does not speak, so announcement ordering and interruption bugs will only surface on a real device.

WHAT TO CALL OUT IN THE CASE STUDY: the entire accessibility layer is core react-native with zero native modules and zero config plugins (fact 13), so it costs nothing against the Expo Go / `npx expo start` constraint — unlike the camera/library picker, which is where your custom-dev-build risk actually sits.

**Packages involved:** react-native, react, expo, expo-router, react-native-safe-area-context, react-native-screens

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (react-native 0.86.3 (installed)) — RN 0.86.3 supports exactly these aria-* aliases — aria-label, aria-labelledby, aria-live, aria-modal, aria-hidden, aria-busy, aria-checked, aria-disabled, aria-expanded, aria-selected, aria-valuemax/min/now/text — and has NO aria-invalid, aria-errormessage, aria-describedby, or aria-required, so the web pattern of pointing an input at its error node via aria-describedby/aria-errormessage simply does not exist.  
   Source: node_modules/react-native/Libraries/Components/View/ViewAccessibility.js lines 221-423 (grep for 'aria-' across Libraries/ returned no invalid/errormessage/describedby/required hits)
2. **verified** (reactnative.dev docs for 0.86) — accessibilityLiveRegion and its alias aria-live are Android-only ('none'|'polite'|'assertive'), so a live-region-based error or status announcement gives you nothing on iOS/VoiceOver.  
   Source: https://reactnative.dev/docs/accessibility (accessibilityLiveRegion listed Android; aria-live listed Android)
3. **verified** (react-native 0.86.3 (installed)) — AccessibilityInfo.announceForAccessibility(string) works on both platforms, but announceForAccessibilityWithOptions' options are iOS-only — the source branches on Platform.OS === 'android' and drops both `queue` and `priority` ('low'|'default'|'high'), calling the plain announce instead.  
   Source: node_modules/react-native/Libraries/Components/AccessibilityInfo/AccessibilityInfo.js lines 474-508; https://reactnative.dev/docs/accessibilityinfo
4. **verified** (react-native 0.86.3 (installed)) — AccessibilityInfo.setAccessibilityFocus(reactTag) is marked @deprecated in 0.86.3 in favour of AccessibilityInfo.sendAccessibilityEvent(hostInstance, 'focus'), which takes a ref/host instance directly (no findNodeHandle needed) and silently no-ops for eventType 'click' on iOS.  
   Source: node_modules/react-native/Libraries/Components/AccessibilityInfo/AccessibilityInfo.js lines 443-467 ('@deprecated Use `sendAccessibilityEvent` with eventType `focus` instead.'); https://reactnative.dev/docs/accessibilityinfo
5. **verified** (react-native 0.86.3 (installed)) — accessibilityLabelledBy / aria-labelledby (point an input at a label's nativeID) is Android-only — it is registered only in BaseViewConfig.android.js, and TextInput.js reads it inside the `Platform.OS === 'android'` branch at line 729-730, so on iOS it is silently ignored.  
   Source: node_modules/react-native/Libraries/NativeComponent/BaseViewConfig.android.js:218, Libraries/Components/TextInput/TextInput.js:726-770, Libraries/Image/Image.android.js:267
6. **verified** (react-native 0.86.3 (installed)) — accessibilityRole accepts 'button', 'header', 'alert', 'checkbox', 'radio', 'radiogroup', 'adjustable', 'text', 'link', 'image', 'imagebutton', 'progressbar', 'switch', 'togglebutton', 'combobox' and ~20 more, and the type falls back to bare `string`, so a typo compiles clean and fails silently at runtime.  
   Source: node_modules/react-native/Libraries/Components/View/ViewAccessibility.js (export type AccessibilityRole, ending in `| string`)
7. **verified** (react-native 0.86.3 (installed)) — Pressable already sets `accessible: accessible !== false` (i.e. accessible defaults to true) and automatically folds its `disabled` prop into accessibilityState.disabled, so you only need to add accessibilityRole="button" plus a label — you do not need to hand-wire disabled state.  
   Source: node_modules/react-native/Libraries/Components/Pressable/Pressable.js lines 227-257
8. **verified** (reactnative.dev docs for 0.86) — hitSlop on Pressable "Sets additional distance outside of element in which a press can be detected" and accepts a number or Rect, and is separate from pressRetentionOffset (default {bottom:30,left:20,right:20,top:20}).  
   Source: https://reactnative.dev/docs/pressable
9. **verified** (react-native 0.86.3 (installed) + reactnative.dev 0.86 docs) — allowFontScaling defaults to true on both Text and TextInput, and maxFontSizeMultiplier defaults to null/undefined meaning 'inherit from parent or the global default (0)', where 0 means no cap and any value >= 1 sets the cap for that node.  
   Source: https://reactnative.dev/docs/text, https://reactnative.dev/docs/textinput, and node_modules/react-native/Libraries/Text/Text.d.ts:109-112,197-203
10. **verified** (react-native 0.86.3 (installed)) — PixelRatio.getFontScale() is implemented as `Dimensions.get('window').fontScale || PixelRatio.get()`, so when no font scale is set it returns the device PIXEL RATIO (e.g. 3) rather than 1 — useWindowDimensions() is the safer source because it exposes fontScale and re-renders when it changes.  
   Source: node_modules/react-native/Libraries/Utilities/PixelRatio.js:99-101 and Libraries/Utilities/useWindowDimensions.js:32
11. **verified** (react-native 0.86.3 + reactnative.dev 0.86 docs) — Platform-restricted APIs to branch on: iOS-only are accessibilityViewIsModal/aria-modal, accessibilityElementsHidden, accessibilityLanguage, onAccessibilityEscape, onMagicTap, onAccessibilityTap, and the 'magicTap'/'escape' accessibility actions; Android-only are importantForAccessibility (incl. 'no-hide-descendants'), accessibilityLiveRegion/aria-live, accessibilityLabelledBy/aria-labelledby, and the 'longpress'/'expand'/'collapse' actions.  
   Source: https://reactnative.dev/docs/accessibility (platform column) cross-checked against node_modules/react-native/Libraries/Components/View/ViewAccessibility.js
12. **verified** (reactnative.dev docs for 0.86) — AccessibilityInfo.addEventListener events also split by platform: 'screenReaderChanged' and 'reduceMotionChanged' fire on both, 'accessibilityServiceChanged' is Android-only, and 'announcementFinished', 'boldTextChanged', 'grayscaleChanged', 'invertColorsChanged', 'reduceTransparencyChanged' are iOS-only.  
   Source: https://reactnative.dev/docs/accessibilityinfo
13. **verified** (Apple UI Design Dos and Don'ts (2026), Android developer guide (2026), WCAG 2.2) — Touch-target minimums are confirmed from primary sources: Apple — "Create controls that measure at least 44 points x 44 points so they can be accurately tapped with a finger."; Android — "For touch interfaces, we recommend that each interactive UI element have a focusable area, or touch target size, of at least 48dpx48dp."; WCAG 2.2 SC 2.5.5 (AAA) — "The size of the target for pointer inputs is at least 44 by 44 CSS pixels".  
   Source: https://developer.apple.com/design/tips/ (Hit Targets), https://developer.android.com/guide/topics/ui/accessibility/apps, https://www.w3.org/WAI/WCAG22/Understanding/target-size-enhanced.html
14. **verified** (react-native 0.86.3, expo ~57.0.22 (installed)) — Every API above ships inside the react-native package itself (AccessibilityInfo, Pressable, PixelRatio, useWindowDimensions, findNodeHandle are all lazy getters on react-native/index.js) and requires no config plugin, so the whole accessibility layer is 100% Expo Go compatible with zero cost to the 'clean clone + npx expo start' requirement.  
   Source: node_modules/react-native/index.js lines 78, 190, 254, 309, 383; app.json (plugins: only expo-router and expo-splash-screen)

### Open uncertainties

- VoiceOver's unavailability on the iOS Simulator and the Accessibility Inspector workflow are NOT verified this run: every developer.apple.com documentation URL I fetched (HIG accessibility, HIG layout, /documentation/accessibility/accessibility-inspector, /documentation/accessibility/inspecting-the-accessibility-of-screens, and the archived TestingAccessibilityOfiOSApps technote) returned title-only JS shells. Only https://developer.apple.com/design/tips/ rendered, which is why the 44x44 pt quote is verified but the Simulator claim is not.
- Whether hitSlop enlarges the VoiceOver/TalkBack activation frame (not just the touch region) is unverified — the Pressable docs only describe press detection, and I did not open native iOS/Android source. This matters for the minimum-target strategy; prefer real minHeight/minWidth until confirmed.
- Whether nested <Text> children inherit allowFontScaling and maxFontSizeMultiplier from a parent Text is not stated on reactnative.dev/docs/text; the maxFontSizeMultiplier type comment says null/undefined means 'inherit from the parent node or the global default (0)', which implies inheritance but I did not confirm it in native code.
- BaseViewConfig.ios.js does register 'aria-labelledby': true and 'aria-live': true at lines 227-228, which contradicts the docs' Android-only labelling. The JS wiring in TextInput.js and Image.android.js is Android-gated, so I am confident these are effectively Android-only, but I did not open the iOS native implementation to prove the view-config entries are inert.
- Expo publishes no accessibility-specific page for SDK 57 — https://docs.expo.dev/debugging/tools/ and https://docs.expo.dev/workflow/ios-simulator/ mention neither VoiceOver, TalkBack, nor Accessibility Inspector, so there is no Expo-blessed a11y testing workflow to cite; all guidance here comes from RN/Apple/Android sources.
- Android's testing guide (https://developer.android.com/guide/topics/ui/accessibility/testing) documents how to enable TalkBack but says nothing about emulator support or which system images preinstall Android Accessibility Suite; the Google-Play-image requirement is recalled, not read, and should be confirmed against emulator docs before you rely on it.
- The installed react-native 0.86.3 package ships no CHANGELOG.md, so I could not diff 0.86 against 0.85 for accessibility-specific changes; the setAccessibilityFocus deprecation is confirmed present in 0.86.3 but I do not know which release introduced it.
- experimental_accessibilityOrder (an array of nativeIDs controlling assistive-tech focus order, documented as cross-platform) appears in the RN docs but did not match a grep of ViewAccessibility.js in the installed 0.86.3 tree, so I could not confirm it is actually available in this version — do not build the step-to-step focus order on it.
- expo-router / react-native-screens screen-transition announcement behaviour was not researched; the recommendation to manually announce each step is a defensive default, not a documented gap.

### Adversarial verification

**Verdict:** partly-wrong

Claims the verifier refuted or corrected:

- **Claim:** Uncertainty #8: "experimental_accessibilityOrder ... did not match a grep of ViewAccessibility.js in the installed 0.86.3 tree, so I could not confirm it is actually available in this version — do not build the step-to-step focus order on it."  
  **Correction:** WRONG, and the resulting advice is wrong. experimental_accessibilityOrder IS present in the installed react-native 0.86.3 — it just lives in ViewPropTypes.js, not ViewAccessibility.js. ViewPropTypes.js:510 declares `experimental_accessibilityOrder?: ?Array<string>` with the doc comment "Defines the order in which descendant elements receive accessibility focus. The elements in the array represent nativeID values for the respective descendant elements.", and it is registered on BOTH platforms: BaseViewConfig.ios.js:203 and BaseViewConfig.android.js:227. reactnative.dev/docs/accessibility carries no platform annotation on it, i.e. cross-platform. It is opt-out for ScrollView and TextInput (both Omit it from their prop types: ScrollView.js:685, TextInput.flow.js:1060). It is still experimental, so treat it as a nice-to-have rather than the backbone of focus order — but the digest's "could not confirm it exists" is a search error, not a real gap.  
  Source: node_modules/react-native/Libraries/Components/View/ViewPropTypes.js:510, .../NativeComponent/BaseViewConfig.ios.js:203, .../BaseViewConfig.android.js:227, .../ScrollView/ScrollView.js:685, .../TextInput/TextInput.flow.js:1060 (react-native 0.86.3 installed)
- **Claim:** Fact 5 (marked verified=true): accessibilityLabelledBy / aria-labelledby "is registered only in BaseViewConfig.android.js".  
  **Correction:** The 'only' is false. BaseViewConfig.ios.js registers BOTH 'aria-labelledby': true (line 227) and 'aria-live': true (line 228). The Android-gating lives one layer up, in the JS components: TextInput.js reads aria-labelledby at lines 729-730 inside the `} else if (Platform.OS === 'android') {` branch that begins at line 727 and forwards it as accessibilityLabelledBy to <AndroidTextInput> at line 770, with no iOS equivalent. So the practical conclusion (labelledby is Android-only, per reactnative.dev/docs/accessibility which annotates accessibilityLabelledBy and aria-labelledby as "Android") stands, but the cited evidence does not say what the digest says it says — and the digest's own uncertainties list already contradicts its verified=true fact, which should have forced a downgrade.  
  Source: node_modules/react-native/Libraries/NativeComponent/BaseViewConfig.ios.js:227-228; .../BaseViewConfig.android.js:218,236-237,249; .../Components/TextInput/TextInput.js:727,729-730,770; https://reactnative.dev/docs/accessibility
- **Claim:** Fact 2: "accessibilityLiveRegion and its alias aria-live are Android-only ('none'|'polite'|'assertive')".  
  **Correction:** The value set is conflated. The two props do not take the same values: aria-live is typed `?('polite' | 'assertive' | 'off')` (ViewAccessibility.js:249) while accessibilityLiveRegion is the one that takes 'none'. Pressable.js:247 does the translation explicitly: `ariaLive === 'off' ? 'none' : (ariaLive ?? props.accessibilityLiveRegion)`. Writing aria-live="none" is a type error and silently no-ops. The Android-only half of the claim is correct per reactnative.dev/docs/accessibility.  
  Source: node_modules/react-native/Libraries/Components/View/ViewAccessibility.js:249; .../Components/Pressable/Pressable.js:247; https://reactnative.dev/docs/accessibility
- **Claim:** Fact 11: Android-only accessibility actions are "'longpress'/'expand'/'collapse'".  
  **Correction:** Half-supported. reactnative.dev/docs/accessibility does list 'expand' and 'collapse' as Android-only actions, but they do not exist in the installed 0.86.3 type: AccessibilityActionName in ViewAccessibility.js enumerates only 'activate' | 'increment' | 'decrement' | 'longpress' (@platform android) | 'magicTap' (@platform ios) | 'escape' (@platform ios), and the TypeScript d.ts has the same six — 'expand'/'collapse' only type-check because ViewAccessibility.d.ts:104 widens the field to `AccessibilityActionName | string`. So they compile but are undocumented in the shipped types; do not assume they are wired through on this version. Everything else in fact 11 is CONFIRMED against reactnative.dev/docs/accessibility (Android: accessibilityLabelledBy, aria-labelledby, accessibilityLiveRegion, aria-live, importantForAccessibility; iOS: accessibilityLanguage, accessibilityViewIsModal, aria-modal, accessibilityElementsHidden, onAccessibilityEscape, onAccessibilityTap, onMagicTap).  
  Source: node_modules/react-native/Libraries/Components/View/ViewAccessibility.js (AccessibilityActionName) and ViewAccessibility.d.ts:104,108; https://reactnative.dev/docs/accessibility
- **Claim:** Recommendation closing line: "the entire accessibility layer ... costs nothing against the Expo Go / npx expo start constraint — unlike the camera/library picker, which is where your custom-dev-build risk actually sits."  
  **Correction:** There is NO custom-dev-build risk from the camera or the picker. docs.expo.dev/versions/v57.0.0/sdk/camera/ and .../imagepicker/ both carry "Included in Expo Go", and both are pinned in expo 57.0.22's bundledNativeModules.json (expo-camera ~57.0.5 line 34, expo-image-picker ~57.0.17 line 54). The real constraint is different and more damaging to the stated plan: expo-camera's availability line reads "Android (device only), iOS (device only)" — live camera capture does not work in the iOS Simulator or the Android emulator, which are the declared primary and secondary targets. Library picking via expo-image-picker has no such restriction. Call out simulator-unsupported camera capture, not a dev build.  
  Source: https://docs.expo.dev/versions/v57.0.0/sdk/camera/, https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/, node_modules/expo/bundledNativeModules.json lines 34 and 54
- **Claim:** Fact 12: touch-target minimums, citing only "WCAG 2.2 SC 2.5.5 (AAA) — 44 by 44 CSS pixels".  
  **Correction:** Not wrong, but it cites the aspirational bar and omits the one that actually governs AA conformance: WCAG 2.2 SC 2.5.8 Target Size (Minimum), Level AA — "The size of the target for pointer inputs is at least 24 by 24 CSS pixels, except when:" (five exceptions: spacing, equivalent, inline, user agent control, essential). For a Dutch-government-adjacent GP portal the AA bar is the compliance-relevant one; 2.5.5 AAA is the stretch goal. The Apple ("Create controls that measure at least 44 points x 44 points so they can be accurately tapped with a finger.") and Android ("For touch interfaces, we recommend that each interactive UI element have a focusable area, or touch target size, of at least 48dpx48dp. Larger is even better.") quotes are both CONFIRMED verbatim, so the practical 48x48 recommendation is sound.  
  Source: https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html, https://www.w3.org/WAI/WCAG22/Understanding/target-size-enhanced.html, https://developer.apple.com/design/tips/, https://developer.android.com/guide/topics/ui/accessibility/apps
- **Claim:** Fact 3 cites "https://reactnative.dev/docs/accessibilityinfo" alongside the source file for the `priority` ('low'|'default'|'high') option of announceForAccessibilityWithOptions.  
  **Correction:** Partly unsupported by that cited source. The docs page only documents the `queue` option ("By default announcements will interrupt any existing speech, but on iOS they can be queued behind existing speech by setting queue to true in the options object.") and does not mention `priority` at all. The `priority` option and its iOS-only handling ARE real and confirmed in the installed source, so the claim itself is right — but it rests on the JS file alone, and an undocumented option is a weaker thing to build an announcement strategy on. The Android-drops-options behaviour is CONFIRMED: `if (Platform.OS === 'android') { NativeAccessibilityInfoAndroid?.announceForAccessibility(announcement); }` discards the whole options object.  
  Source: node_modules/react-native/Libraries/Components/AccessibilityInfo/AccessibilityInfo.js (announceForAccessibilityWithOptions); https://reactnative.dev/docs/accessibilityinfo

Expo Go compatibility of every package named:

| Package | In Expo Go | Config plugin | Dev build needed | Note |
|---|---|---|---|---|
| react-native@0.86.3 | yes | no | no | React Native is the Expo Go runtime itself. Every API the digest recommends is a lazy getter on react-native/index.js: Pressable (l.78), AccessibilityInfo (l.190-191), findNodeHandle (l.254-255), PixelRatio (l.309), useWindowDimensions (l.383-384). Zero native modules, zero config plugins, zero dev-build cost. Digest fact 13 CONFIRMED. |
| react@19.2.3 / react-dom@19.2.3 | yes | no | no | Pure JS, no native code. |
| expo@57.0.22 | yes | no | no | The SDK package that Expo Go is built from; app.json already declares only expo-router and expo-splash-screen as plugins. |
| expo-router@57.0.21 | yes | yes | no | Config plugin entry is required and is already present in app.json. Docs: "If you are using the default template to create a new project, expo-router's config plugin is already configured in your app config." |
| react-native-safe-area-context@5.7.0 | yes | no | no | Bundled in Expo Go at exactly the installed version. Relevant to a11y because useSafeAreaInsets keeps 48dp targets off the home indicator at large font scales. |
| react-native-screens@4.26.2 | yes | no | no | Bundled. Note the digest never researched whether react-native-screens announces screen transitions to VoiceOver/TalkBack — that gap is real and its 'announce each step manually' advice remains a defensive default, not a documented fix. |
| react-native-gesture-handler@2.32.0 | yes | no | no | Bundled; pulled in transitively by expo-router. Not required by anything in the a11y recommendation. |
| react-native-reanimated@4.5.1 + react-native-worklets@0.10.1 | yes | no | no | Bundled at exact pinned versions. Needs the react-native-worklets Babel plugin, which babel-preset-expo wires up automatically — that is a Babel config, not an app.json config plugin, so it costs nothing against `npx expo start`. |
| react-native-web@0.21.0 | yes | no | no | Web-only JS shim; irrelevant to the iOS-simulator/Android-emulator targets. |
| expo-image-picker@57.0.17 (implied by the flow's 'photo from library') | yes | no | no | IN Expo Go, so no dev build. Config plugin is OPTIONAL: "You can configure expo-image-picker using its built-in config plugin if you use config plugins in your project." You only need the app.json entry for custom iOS permission strings in a real binary; Expo Go supplies its own. Library picking works in the iOS Simulator. |
| expo-camera@57.0.5 (implied by the flow's 'photo from camera') | yes | no | no | CRITICAL FOR THIS PROJECT: bundled in Expo Go (no dev build), but the docs mark it "device only" on both Android and iOS — it does NOT work in the iOS Simulator or the Android emulator. Since the stated primary target is the iOS simulator, plan a stub/mock camera path. Config plugin is optional and only sets permission strings for a standalone build. |
| @react-native-community/netinfo@12.0.1 (implied by the required 'offline' state) | yes | no | no | Bundled in Expo Go at an exact pin. This is the package the digest's offline-banner recommendation (accessibilityLiveRegion + one-shot announceForAccessibility) implicitly depends on; it is not currently in package.json. |


## Topic: fake-backend

*Faking three REST services in an Expo SDK 57 e-consult app (hand-rolled fake module vs msw/native vs local HTTP server), EXPO_PUBLIC_* env vars, and a __DEV__ settings panel*

### Recommendation

Use (a): a hand-rolled, typed in-app fake API module. It is the only option that survives the hard constraint "clean clone + npx expo start, Expo Go, no dev build" without qualification.

Shape it like a real client so the reviewer sees the seam, not a pile of setTimeouts. One transport shim, three services:
- src/services/http.ts — `async function request<T>(endpoint: Endpoint, produce: () => T, init?: {signal?: AbortSignal}): Promise<T>` that (1) awaits a jittered delay from config, (2) consults a fault map, (3) throws typed errors (`NetworkError | ServerError | TimeoutError | ValidationError`) so the UI can distinguish offline from 500 from 422.
- src/services/recipients.ts (`getRecipients()`), src/services/questions.ts (`getQuestions(practiceId)`), src/services/econsult.ts (`createEconsult(draft)` then `uploadAttachment(econsultId, asset)`).
- src/fixtures/*.json imported statically (Metro inlines JSON at bundle time, no I/O, works identically in Expo Go on both platforms).

Deterministic failure: make `uploadAttachment` the forced-failure endpoint. It is the second of the two sending calls, so failing it deterministically demonstrates the genuinely interesting state — e-consult created, attachment not uploaded — which is a partial success needing a retry-attachment path, not a blanket "sending failed". Drive it from a `faults: Record<Endpoint, 'none' | 'network' | 'server' | 'timeout'>` record.

Config layering: defaults from EXPO_PUBLIC_* read as literal dot-notation (`process.env.EXPO_PUBLIC_PRACTICE_ID`, `process.env.EXPO_PUBLIC_API_LATENCY_MS`, `process.env.EXPO_PUBLIC_FAIL_UPLOAD`), committed as a `.env` plus `.env.example` so a clean clone behaves the same for the reviewer. Because those values are inlined at bundle time and need a full app reload to change, they are seeds only — never the live toggle.

Runtime overrides live in a React context (`DevSettingsProvider` holding `{practiceId, latencyMs, faults, forceOffline}` in useState), and the fake API reads config through that context (or a `useSyncExternalStore`-backed store), not from module-level mutable globals — React Compiler is enabled in this project, so mutating module state during render is exactly the pattern it will punish. Gate the panel with `if (__DEV__)` at the render site and behind a hidden expo-router route (e.g. `app/(dev)/settings.tsx` plus a triple-tap on the header title, which keeps the production a11y tree clean for VoiceOver/TalkBack users). Give the panel large hit targets and plain Dutch labels — it doubles as your demo remote when walking a reviewer through loading/empty/error/sending/offline.

Reject (b) msw: two open, unanswered React Native issues (#2592 since Sep 2025, #2748 since May 2026), and the current 2.15.0 line has a live regression where mocked bodies come back empty in RN because the interceptor expects a `.body` ReadableStream that RN's whatwg-fetch Response does not have. On top of that, SDK 57 swaps the global fetch for the native-module-backed expo/fetch, so what msw would be patching is not the runtime the docs were written against, and its own polyfill advice (react-native-url-polyfill, fast-text-encoding) duplicates URL/TextDecoder that Expo already installs. msw's own page calls the integration "potentially incomplete". That is a coin-flip on the reviewer's first `npx expo start`, for zero visible benefit in a take-home.

Reject (c) local HTTP server: json-server has no stable v1 (only 1.0.0-beta.15), demands Node >= 22.12, and dropped `--delay`, so you would write a Node script anyway. Worse, it needs a second terminal and three different base URLs — `http://localhost:3000` for the iOS simulator, `http://10.0.2.2:3000` for the Android emulator (127.0.0.1 there is the emulated device itself), and the host LAN IP for a phone on Expo Go over LAN (or `adb reverse tcp:3000 tcp:3000` for a USB Android device). That directly contradicts "runs from a clean clone with npx expo start" and hands the reviewer a networking puzzle instead of your UI.

Call this out explicitly in the README (2-3 sentences: fake API chosen deliberately, real-client-shaped seam, how to flip failures) — showing you evaluated msw and a local server and rejected them for reviewer-reproducibility reasons is itself a point in your favour.

**Packages involved:** expo, react-native, expo-router, react, typescript

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (react-native 0.86.3 (installed in this project)) — React Native 0.86.3's global fetch/Headers/Request/Response come from the `whatwg-fetch` polyfill, and setUpXHR installs only XMLHttpRequest, FormData, fetch, Headers, Request, Response, WebSocket, Blob, File, FileReader, URL, URLSearchParams, AbortController and AbortSignal.  
   Source: node_modules/react-native/Libraries/Network/fetch.js and Libraries/Core/setUpXHR.js (opened this run)
2. **verified** (react-native 0.86.3 / expo 57.0.22) — Grepping the installed react-native 0.86.3 package finds no TextEncoder, structuredClone or ReadableStream implementation in Libraries/, while Expo's winter runtime installs TextDecoder, TextDecoderStream, TextEncoderStream, URL, URLSearchParams, DOMException and structuredClone (via @ungap/structured-clone) and notes "ReadableStream is injected by Metro as a global".  
   Source: grep over node_modules/react-native/Libraries + node_modules/expo/src/winter/runtime.native.ts lines 15-35
3. **verified** (expo 57.0.22 / docs v57.0.0) — In Expo SDK 57 the native runtime replaces the global `fetch` with the native-module-backed `expo/fetch` (ExpoFetchModule) unless `EXPO_PUBLIC_USE_RN_FETCH` is set to '1' or 'true', which means the fetch your app calls is not RN's XHR-backed whatwg-fetch.  
   Source: node_modules/expo/src/winter/runtime.native.ts:40-52 and src/winter/fetch/fetch.ts; corroborated by https://docs.expo.dev/versions/v57.0.0/sdk/expo/ ("On Android and iOS, expo/fetch is also installed as the global fetch")
4. **verified** (msw 2.x docs as of 2026-09-11) — msw's own React Native page tells you to import `setupServer` from 'msw/native', add `react-native-url-polyfill` and `fast-text-encoding` in an `msw.polyfills.js`, and warns "This integration is potentially incomplete", asking RN users to report discrepancies.  
   Source: https://mswjs.io/docs/integrations/react-native (opened this run)
5. **verified** (msw 2.15.0) — The latest published msw is 2.15.0, engines node>=18, with a `./native` export that has explicit `react-native` conditions and ~19 runtime dependencies including @mswjs/interceptors ^0.41.3, yargs, graphql, tough-cookie and @inquirer/confirm.  
   Source: https://registry.npmjs.org/msw/latest (opened this run)
6. **verified** (msw 2.14.0-2.15.0 vs 2.12.x) — msw issue #2748 (opened 22 May 2026, still open, no maintainer reply) reports that msw >= 2.14.0 returns null/empty response bodies in React Native because @mswjs/interceptors >= 0.41.7 assumes `rawResponse.body` is a ReadableStream while RN's whatwg-fetch Response only has `_bodyText`; the workaround is patching `Response.prototype.body` or staying on msw <= 2.12.x.  
   Source: https://github.com/mswjs/msw/issues/2748 (opened this run)
7. **verified** (msw 2.x, Expo (version unstated in issue)) — msw issue #2592 (opened 26 Sep 2025, still open, no maintainer response) reports that msw/native simply fails to intercept requests in an Expo React Native runtime, with the reporter asking whether msw/native is even meant to intercept fetch/axios there.  
   Source: https://github.com/mswjs/msw/issues/2592 (opened this run)
8. **verified** (Expo SDK 57 docs (v57.0.0)) — Metro in SDK 57 enables `package.json:exports` resolution by default and adds the `react-native` condition for native platforms, so msw's ./native subpath does resolve, but Expo documents `unstable_enablePackageExports` as the escape hatch when a package's exports map misbehaves.  
   Source: https://docs.expo.dev/versions/v57.0.0/config/metro/ (opened this run)
9. **verified** (json-server 1.0.0-beta.15 (stable fallback 0.17.4)) — The only current json-server release is 1.0.0-beta.15 (README banner: "Viewing beta v1 documentation – usable but expect breaking changes"), it requires Node >= 22.12.0, defaults to port 3000, and v1 removed the `--delay` option entirely, telling users to throttle in browser DevTools instead.  
   Source: https://registry.npmjs.org/json-server/latest and https://github.com/typicode/json-server (opened this run)
10. **verified** (Android Studio emulator docs, retrieved 2026-09-11) — Inside the Android emulator 10.0.2.2 is a "Special alias to your host loopback interface (127.0.0.1 on your development machine)" while 127.0.0.1 is "The emulated device loopback interface", and `adb reverse tcp:DEVICE_PORT tcp:HOST_PORT` is the documented way to make plain `localhost` on the device/emulator reach a host server.  
   Source: https://developer.android.com/studio/run/emulator-networking-address and https://developer.android.com/develop/ui/views/layout/webapps/access-local-server (opened this run)
11. **verified** (Expo CLI docs, retrieved 2026-09-11) — Expo CLI serves over LAN by default (device must share the Wi-Fi network), `--localhost` restricts the dev server to the host machine, and `--tunnel` proxies through ngrok and requires @expo/ngrok plus a public URL, and is slower.  
   Source: https://docs.expo.dev/more/expo-cli/ (opened this run)
12. **verified** (Expo docs, retrieved 2026-09-11) — EXPO_PUBLIC_* variables from .env are inlined into the bundle at build time — "Every environment variable must be statically referenced as a property of process.env using JavaScript's dot notation for it to be inlined" (so `process.env['EXPO_PUBLIC_X']` and destructuring do not work) — editing .env needs no Expo CLI restart or cache clear but does need a full app reload, `EXPO_NO_DOTENV=1` disables dotenv loading, and the docs warn these values are visible in plain text so they must never hold secrets.  
   Source: https://docs.expo.dev/guides/environment-variables/ (opened this run, two passes)
13. **verified** (Expo docs, retrieved 2026-09-11) — Expo documents Expo Go as "a limited playground" that only runs the native libraries already bundled into the Expo Go app, with a development build required to "install any native library and configure or apply changes to a native project".  
   Source: https://docs.expo.dev/workflow/overview/ (opened this run)
14. **verified** (React Native 0.86 docs) — `__DEV__` is listed on the React Native 0.86 JavaScript Environment page only as a "Specific" polyfill in the globals table — the page does not define its dev-vs-release semantics or promise dead-code elimination.  
   Source: https://reactnative.dev/docs/0.86/javascript-environment (opened this run)

### Open uncertainties

- Whether `TextEncoder` exists at runtime on Hermes in RN 0.86 / Expo 57: it is not in react-native's Libraries/ and Expo's winter runtime installs TextDecoder and TextEncoderStream but not TextEncoder — I did not execute anything to confirm, so msw's fast-text-encoding requirement may or may not still be needed.
- Whether msw's fetch interceptor can wrap Expo SDK 57's native-module-backed global `fetch` at all, and whether the #2748 `Response.prototype.body` patch helps in that configuration — no opened source tests this combination.
- iOS simulator reachability of the host's `localhost` (and whether App Transport Security in Expo Go permits plain http to localhost / a LAN IP): I did not open an Apple source this run, so treat that leg of the local-server option as unverified rather than assumed-working.
- Whether the Expo Go Android app ships `usesCleartextTraffic` permitting plain http:// calls to 10.0.2.2 or a LAN IP — not verified against any opened source.
- json-server 0.17.4 (the stable fallback) and whether its `--delay` flag still works as documented — I only opened the v1 beta README and the registry entry for 1.0.0-beta.15.
- Whether react-native-url-polyfill / fast-text-encoding conflict with Expo 57's own URL, URLSearchParams and TextDecoder installs (installGlobal backs up existing globals, but I did not verify the override behaviour).
- Exact dead-code-elimination behaviour of `__DEV__` in a production Expo export: the RN 0.86 docs page lists `__DEV__` but documents no stripping guarantee, so the claim that a __DEV__ panel is fully removed from release bundles is unverified here.

### Adversarial verification

**Verdict:** sound

Claims the verifier refuted or corrected:

- **Claim:** Fact 12: 'Expo documents Expo Go as "a limited playground" that only runs the native libraries already bundled into the Expo Go app, with a development build required to "install any native library..."' — cited to https://docs.expo.dev/workflow/overview/  
  **Correction:** The quoted phrases are real but the middle clause is not on that page. /workflow/overview/ says only: 'Expo Go is a limited playground and not useful for building production-grade projects' and, of development builds, that they let you 'install any native library and configure or apply changes to a native project'. It contains NO statement that Expo Go runs only its pre-bundled native libraries. That limitation is real but is evidenced by expo/expo's packages/expo/bundledNativeModules.json (the installed copy at node_modules/expo/bundledNativeModules.json, 120+ pinned entries), not by the cited page. Substantively true, wrongly attributed.  
  Source: https://docs.expo.dev/workflow/overview/ (re-fetched twice with targeted prompts this run) + node_modules/expo/bundledNativeModules.json
- **Claim:** Fact 5: msw 2.15.0 has '~19 runtime dependencies'  
  **Correction:** Off by one: the registry shows exactly 18 entries in `dependencies` for 2.15.0. Everything else in this fact is confirmed verbatim — dist-tags.latest=2.15.0 (published 2026-07-08), engines {node: '>=18'}, a './native' subpath with explicit react-native import/default conditions (and browser:null), and the named deps @mswjs/interceptors ^0.41.3, yargs ^17.7.2, graphql ^16.13.2, tough-cookie ^6.0.1, @inquirer/confirm ^6.0.11.  
  Source: https://registry.npmjs.org/msw (full packument parsed this run) + https://registry.npmjs.org/msw/latest
- **Claim:** Fact 8: 'Metro in SDK 57 enables package.json:exports resolution by default ... but Expo documents unstable_enablePackageExports as the escape hatch'  
  **Correction:** Conclusion is right, wording is stronger than the source. The SDK 57 metro page never says 'enabled by default' and never states unstable_enablePackageExports' default value; it says 'This section applies from SDK 53 on all platforms' and only describes 'disabling the unstable_enablePackageExports option' as a remedy. The 'react-native condition is added for native platforms' half IS stated verbatim, so msw's ./native subpath does resolve.  
  Source: https://docs.expo.dev/versions/v57.0.0/config/metro/ (fetched twice with targeted prompts this run)
- **Claim:** Fact 10: `adb reverse tcp:DEVICE_PORT tcp:HOST_PORT` is cited to https://developer.android.com/develop/ui/views/layout/webapps/access-local-server as 'the documented way to make plain localhost on the device/emulator reach a host server'  
  **Correction:** The command text is verbatim correct ('adb has a reverse command that can forward requests on a specific port on the device to a different port on the host machine'), but the page's actual title and scope is 'Access a local development server from WebView' — it is WebView-debugging documentation, not a general React Native / Expo networking doc. The 10.0.2.2 and 127.0.0.1 half of the fact is verbatim correct against the emulator-networking-address page ('Special alias to your host loopback interface (127.0.0.1 on your development machine)' and 'The emulated device loopback interface'). Use it, but do not present that URL as an RN-sanctioned instruction.  
  Source: https://developer.android.com/develop/ui/views/layout/webapps/access-local-server + https://developer.android.com/studio/run/emulator-networking-address (both opened this run)
- **Claim:** Recommendation-level assertions: 'Metro inlines JSON at bundle time, no I/O', 'React Compiler ... is exactly the pattern it will punish' (module-level mutable state), and 'a __DEV__ panel is fully removed from release bundles'  
  **Correction:** None of these carry a source in the digest and I opened none supporting them this run — verified=false. The digest is honest about the third (it appears in its own uncertainties, and I confirmed reactnative.dev/docs/0.86/javascript-environment lists __DEV__ only as a row under the Polyfills > 'Specific' table with no dev/release semantics and no stripping guarantee). The first two are stated as settled fact in the recommendation without that hedge; treat them as engineering judgement, not documented behaviour.  
  Source: https://reactnative.dev/docs/0.86/javascript-environment (opened this run); no source opened for the Metro-JSON-inlining or React-Compiler claims
- **Claim:** Everything else in the facts list survived adversarial checking — recorded here so the caller knows these were attacked and held, not merely skipped.  
  **Correction:** Confirmed verbatim this run: fact 1 (setUpXHR.js polyfills exactly XMLHttpRequest, FormData, fetch, Headers, Request, Response, WebSocket, Blob, File, FileReader, URL, URLSearchParams, AbortController, AbortSignal; fetch.js requires whatwg-fetch); fact 2 (zero TextEncoder/structuredClone/ReadableStream hits in RN Libraries/; winter runtime installs the listed globals plus the literal comment 'ReadableStream is injected by Metro as a global'); fact 3 (runtime.native.ts gates on EXPO_PUBLIC_USE_RN_FETCH === '1' || 'true' then install('fetch', ...), matching the SDK 57 docs sentence); fact 4 (msw docs quote 'This integration is potentially incomplete...' and name msw/native, react-native-url-polyfill, fast-text-encoding, msw.polyfills.js); facts 6 and 7 (issue #2748 'Interceptors returning null in React native caused by breaking change in msw 2.14.0', open, 22 May 2026, no maintainer reply, interceptors 0.41.7 root cause, Response.prototype.body/_bodyText workaround; issue #2592 'MSW fails to intercept requests in React Native runtime', open, 26 Sep 2025, no maintainer reply); fact 9 (beta banner, Node >=22.12.0, port 3000, migration note 'Use browser DevTools (Network tab > throttling) instead of the removed --delay CLI option'; the parenthetical 'stable fallback 0.17.4' also checks out — 0.17.4 is the last non-beta on npm, published 2023-09-29, engines node >=12); fact 11 (all four env-var quotes verbatim, including 'Variables can be updated as you edit your code without restarting the Expo CLI or clearing the cache' and the full-reload requirement); fact 13.  
  Source: node_modules/react-native/Libraries/**, node_modules/expo/src/winter/runtime.native.ts, https://docs.expo.dev/versions/v57.0.0/sdk/expo/, https://mswjs.io/docs/integrations/react-native, https://github.com/mswjs/msw/issues/2748, https://github.com/mswjs/msw/issues/2592, https://github.com/typicode/json-server, https://registry.npmjs.org/json-server, https://docs.expo.dev/guides/environment-variables/, https://docs.expo.dev/more/expo-cli/ (all opened this run)

Expo Go compatibility of every package named:

| Package | In Expo Go | Config plugin | Dev build needed | Note |
|---|---|---|---|---|
| expo (~57.0.22) | yes | no | no | docs.expo.dev/versions/v57.0.0/sdk/expo/ shows the 'Included in Expo Go' availability badge; node_modules/expo has no app.plugin.js. This page also confirms the digest's fetch claim verbatim: 'On Android and iOS, expo/fetch is also installed as the global fetch' and 'To keep React Native's built-in fetch as the global, set EXPO_PUBLIC_USE_RN_FETCH=1'. |
| react-native (0.86.3) | yes | no | no | It is the runtime the Expo Go client embeds, not an installable native module. Verified locally: Libraries/Network/fetch.js is a side-effect require of 'whatwg-fetch' and Libraries/Core/setUpXHR.js polyfills exactly the 14 globals the digest lists. |
| expo-router (~57.0.21) | yes | yes | no | Listed as 'expo-router': '~57.0.21' in bundledNativeModules.json and the SDK 57 router page shows 'Included in Expo Go'. It DOES need a plugins entry: the package ships app.plugin.js and the docs say the plugin 'is already configured in your app config' for template projects — confirmed, this project's app.json already lists "expo-router" in plugins. So no new work, but the digest's suggested app/(dev)/settings.tsx route rides on an already-plugin-configured router. |
| react (19.2.3) / react-dom (19.2.3) | yes | no | no | Pure JS, no native code, not in bundledNativeModules.json because it needs no native pairing. The digest's DevSettingsProvider / useSyncExternalStore recommendation is plain React and carries zero native cost. |
| typescript (~6.0.3) | yes | no | no | A devDependency compile-time tool; never shipped in the JS bundle and never loaded by the Expo Go client. Trivially Expo Go safe. |
| react-native-safe-area-context (~5.7.0) | yes | no | no | Present in bundledNativeModules.json at exactly 5.7.0; no app.plugin.js in the installed package. |
| react-native-screens (~4.26.0) | yes | no | no | Present in bundledNativeModules.json at 4.26.0; no app.plugin.js in the installed package. |
| react-native-reanimated (4.5.1) | yes | no | no | Present in bundledNativeModules.json at exactly 4.5.1; no app.plugin.js. Reanimated 4 needs a Babel plugin (react-native-worklets/plugin), which is a Babel concern handled by babel-preset-expo, not an app.json config plugin and not a dev build. |
| react-native-worklets (0.10.1) | yes | no | no | Pinned at 0.10.1 in bundledNativeModules.json, matching this project's package.json exactly; no app.plugin.js. |
| react-native-gesture-handler (~2.32.0) | yes | no | no | Present in bundledNativeModules.json at 2.32.0; no app.plugin.js in the installed package. |
| msw (2.15.0) — option (b), rejected by the digest | yes | no | no | IMPORTANT NUANCE: msw is pure JS (18 deps, all JS, engines node>=18) so it does NOT require a dev build and would install fine in Expo Go — the digest's rejection is correct but for functional reasons, not native-build reasons. Its './native' export does carry explicit react-native conditions and Metro adds the 'react-native' condition for native, so it resolves; it just may not work (issues #2592 and #2748, both open and unanswered, both confirmed this run). |
| json-server (1.0.0-beta.15) — option (c), rejected by the digest | no | no | no | Not an app dependency at all — it is a host-machine Node CLI (bin: lib/bin.js), so it is never loaded by Expo Go and cannot need a config plugin or dev build; inExpoGo=false records 'not applicable / never in the bundle'. Its real cost is exactly what the digest says: a second terminal, Node >=22.12.0, and per-platform base URLs, which breaks 'clean clone + npx expo start'. |
| react-native-url-polyfill (msw prerequisite, transitively rejected) | yes | no | no | Pure JS polyfill, so no native cost — but redundant here: Expo's winter runtime already installs URL and URLSearchParams via install('URL', ...) and install('URLSearchParams', ...), on top of RN's own Blob/URL polyfills. Adding it only to satisfy msw is duplicated work. |
| fast-text-encoding (msw prerequisite, transitively rejected) | yes | no | no | Pure JS, no native cost. Genuinely unresolved whether it is still needed: grepping react-native 0.86.3's Libraries/ finds zero TextEncoder hits, and Expo's winter runtime installs TextDecoder/TextDecoderStream/TextEncoderStream but never TextEncoder — yet Expo's own fetch code calls `new TextEncoder()` with the comment '@ts-ignore: TextEncoder is not available in all environments', so Hermes is presumed to provide it. I did not execute anything to confirm; treat as unverified. |


## Topic: expo-router-flow

*Linear multi-step e-consult flow with expo-router ~57.0 Stack: file layout, screen options, back-prevention on confirmation, draft state sharing, typed routes, unstable_settings, Dynamic Type in the native header, and SDK 57 template/entry resolution*

### Recommendation

Use a single nested group with one Stack, and do not nest a second navigator inside it.

Layout: `src/app/_layout.tsx` stays `<Stack />` (it already is — nothing to strip; the SDK 57 template's ThemeProvider/splash code is not present here). Add `src/app/econsult/_layout.tsx` exporting a `<DraftProvider>` around `<Stack>` with four `<Stack.Screen>` entries for `recipient`, `questions`, `message`, `sent`, plus `export const unstable_settings = { anchor: 'recipient' }` so a deep link or reload into `/econsult/message` still renders a back arrow. Prefer the group form `src/app/(econsult)/...` only if you want the segment hidden from the URL; for a case study the visible `/econsult/...` path reads better.

Screen options: set `screenOptions={{ headerBackButtonDisplayMode: 'minimal' }}` on the Stack so long Dutch back-titles degrade to a chevron instead of being truncated, and give each screen a short `title`. Leave `presentation` at its default `card` for the four steps — a `modal`/`formSheet` presentation fights the linear back-stack mental model and adds a second dismissal gesture that elderly users hit by accident.

Confirmation screen: `router.replace('/econsult/sent')` when the create + upload calls resolve, and on the `sent` screen set `options={{ headerBackVisible: false, gestureEnabled: false, headerLeft: () => null }}`. Note two verified gaps: `gestureEnabled` is iOS-only, and `headerBackVisible` is a no-op if `sent` ends up first in the stack. So `replace` alone is not enough on Android — either keep `predictiveBackGestureEnabled: false` (already in app.json) and add `usePreventRemove(true, () => {})` from `expo-router/react-navigation`, or, better, have the "Klaar" button call `router.dismissAll()` followed by `router.replace('/')` to unwind the whole flow. Reserve `dismissTo` for a "start over" action; its fallback behaviour (replace when the href isn't on the stack) makes it unreliable as a back-prevention mechanism.

Draft state: put it in React context in `src/app/econsult/_layout.tsx`, not in route params. The layout component is documented as the place for context providers, it survives the child route changes, and it keeps the photo URI, the answer map and the in-flight send status out of URLs — route params would have to serialise the answer map and would break typed `Href` checking. Keep `useLocalSearchParams` only for a `recipientId` if you want the recipient step to be linkable.

Typed routes are already enabled; run `npx expo start` once so `expo-env.d.ts` is generated before relying on `Href` in TypeScript, and use absolute hrefs only (relative `./` paths are unsupported).

Accessibility: the native header exposes no font-scaling knob, so do not set `headerTitleStyle.fontSize`. Keep the native title short and put the real step heading ("Stap 2 van 4 — Vragen") in the scrollable body as a normal `<Text>` that scales freely. If you need the header itself to wrap at accessibility text sizes, `headerShown: false` plus your own header component is the only route — and it also gives you a place to put a progress indicator and a correctly-labelled back button for VoiceOver/TalkBack.

Expo Go cost: zero for everything above. All of it lives in expo-router, which is JS-only on top of react-native-screens already bundled in Expo Go; no custom development build is needed for the routing layer. The dev-build question only arises later for the camera/photo-library step, which is outside this topic.

**Packages involved:** expo, expo-router, react, react-native, react-native-screens, react-native-safe-area-context, react-native-gesture-handler, expo-splash-screen, babel-preset-expo, typescript

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (expo 57.0.22 (bundled @expo/cli), expo-router 57.0.21) — `src/app` is resolved by the CLI's `getRouterDirectory(projectRoot)`, which returns `path.join('src','app')` if that directory exists and otherwise falls back to `'app'`, logging "Using src/app as the root directory for Expo Router." — no config needed, and `extra.router.root` in app.json can override it (erroring with INVALID_ROUTER_ROOT if outside the project).  
   Source: node_modules/expo/node_modules/@expo/cli/build/src/start/server/metro/router.js lines 107-133 (read this run) + https://docs.expo.dev/router/reference/src-directory/ ("The src/app directory takes higher precedence than the root app directory. Only the src/app directory will be used if you have both.")
2. **verified** (expo-router 57.0.21, babel-preset-expo (expo 57.0.22)) — `"main": "expo-router/entry"` resolves to entry.js -> `import 'expo-router/entry-classic'` -> `@expo/metro-runtime` + `renderRootComponent(App)`, and the route tree comes from `require.context(process.env.EXPO_ROUTER_APP_ROOT, true, ...)` in `_ctx.ios.js`, whose env var is inlined at build time by babel-preset-expo's `expo-router` plugin as a path relative to the file being transformed.  
   Source: node_modules/expo-router/entry.js, entry-classic.js, _ctx.ios.js and node_modules/babel-preset-expo/build/plugins/expo-router-plugin.js (getExpoRouterAppRoot / EXPO_ROUTER_ABS_APP_ROOT), all read this run
3. **verified** (expo-router 57.0.21) — expo-router 57 vendors React Navigation — there is no `@react-navigation/*` in node_modules; `Stack` is the native stack typed with `NativeStackNavigationOptions` (from expo-router/build/react-navigation/native-stack), and `ThemeProvider`/`DarkTheme`/`DefaultTheme`/`useTheme` are re-exported from `expo-router` itself, so you never install or import react-navigation packages directly.  
   Source: node_modules/expo-router/build/exports.d.ts, build/layouts/StackClient.d.ts, build/react-navigation/native-stack/ (ls showed no top-level @react-navigation dir)
4. **verified** (expo-router 57.0.21) — The `Stack` export is a component plus a namespace: `Stack.Screen` (with `Stack.Screen.Title` and `Stack.Screen.BackButton`), `Stack.Protected`, `Stack.Header`, `Stack.SearchBar`, `Stack.Title` and `Stack.Toolbar.*`; a pure-JS alternative ships as `expo-router/js-stack`.  
   Source: node_modules/expo-router/build/layouts/StackClient.d.ts (tail) and node_modules/expo-router/js-stack.d.ts
5. **verified** (Expo Router docs, SDK 57 current) — Every directory under src/app (including src/app itself) may declare a `_layout.tsx` that renders before the page and is "where you would put initialization code that may have previously gone inside an App.jsx file, such as loading fonts, interacting with the splash screen, or adding context providers" — so a `src/app/econsult/_layout.tsx` holding a draft-state React context is the documented pattern; the same page warns to "only [nest navigators] when it is truly needed".  
   Source: https://docs.expo.dev/router/basics/layout/ (fetched this run)
6. **verified** (expo-router 57.0.21 vendored native-stack) — `gestureEnabled` ("Whether you can use gestures to dismiss this screen. Defaults to `true`.") is annotated `@platform ios` / "Only supported on iOS", so setting `gestureEnabled: false` on the confirmation screen stops the iOS swipe-back but does nothing for the Android hardware/predictive back button.  
   Source: node_modules/expo-router/build/react-navigation/native-stack/types.d.ts line ~552-558 (read this run); same wording on https://reactnavigation.org/docs/native-stack-navigator/
7. **verified** (expo-router 57.0.21 vendored native-stack) — `headerBackTitle` is "Only supported on iOS and Web" (@platform ios, web) and defaults to the previous scene's title; `headerBackButtonDisplayMode: 'minimal'` always shows only the chevron, and the space-aware shortening is disabled once you set a custom font family or size via `headerBackTitleStyle` — relevant because a long Dutch title like "Vragen beantwoorden" will otherwise be truncated to "Back"/an arrow.  
   Source: node_modules/expo-router/build/react-navigation/native-stack/types.d.ts lines 145-175 and ~385-405 (read this run)
8. **verified** (expo-router 57.0.21 vendored native-stack) — `headerBackVisible?: boolean` exists and its doc string notes "This will have no effect on the first screen in the stack", so hiding the back chevron on a confirmation screen only works if that screen is not the stack's first route — which is why replacing the stack is the more reliable approach.  
   Source: node_modules/expo-router/build/react-navigation/native-stack/types.d.ts lines 138-144 (read this run)
9. **verified** (expo-router 57.0.21) — The imperative router types declare `replace(url, options)`, `dismiss(count?)`, `dismissTo(href, options?)` ("Dismisses screens until the provided href is reached. If the href is not found, it will instead replace the current screen with the provided href"), `dismissAll()` ("Returns to the first screen of the closest stack — equivalent to a stack popToTop action"), `canDismiss()` and `canGoBack()`.  
   Source: node_modules/expo-router/build/global-state/router.d.ts (read this run)
10. **verified** (expo-router 57.0.21) — `usePreventRemove(preventRemove: boolean, callback)` — "Hook to prevent screen from being removed. Can be used to prevent users from leaving the screen" — is available via `import { usePreventRemove } from 'expo-router/react-navigation'` (react-navigation/index -> ./native -> ../core), but is NOT re-exported from the root `expo-router` entry.  
   Source: node_modules/expo-router/build/react-navigation/core/usePreventRemove.d.ts, build/react-navigation/index.d.ts, build/react-navigation/native/index.d.ts line 61, build/exports.d.ts (grep found no match)
11. **verified** (expo-router 57.0.21) — `unstable_settings` is read as `unstable_settings.anchor ?? unstable_settings.initialRouteName ?? <first child of matching group>`, with per-group overrides looked up as `unstable_settings[groupName].anchor` — `anchor` is the current name and `initialRouteName` still works as a fallback; the docs note it "currently do[es] not work with async routes (development-only)" and exists mainly so deep links/reloads still show a back arrow.  
   Source: node_modules/expo-router/build/getRoutesCore.js lines 642-710 (read this run) + https://docs.expo.dev/router/advanced/router-settings/
12. **verified** (expo 57.0.22, expo-router 57.0.21) — Typed routes are already on in this project (`experiments.typedRoutes: true` in app.json); the CLI writes `expo-env.d.ts` containing only `/// <reference types="expo/types" />`, gitignores it, and keeps both `expo-env.d.ts` and `.expo/types/**/*.ts` in tsconfig `include` — the docs add that `Href` rejects unknown paths, requires the object form for dynamic segments (`{ pathname: '/user/[id]', params: { id: 1 } }`), and that relative hrefs like `./about` are unsupported.  
   Source: node_modules/expo/node_modules/@expo/cli/build/src/start/server/type-generation/expo-env.js and tsconfig.js (read this run) + https://docs.expo.dev/router/reference/typed-routes/; local app.json/tsconfig.json
13. **verified** (expo-router 57.0.21 vendored native-stack) — The native header offers no font-scaling control: `headerTitleStyle` accepts only `fontFamily | fontSize | fontWeight` plus `color`, and grepping the whole native-stack options type found no `allowFontScaling` or `maxFontSizeMultiplier` — so for guaranteed large-Dynamic-Type behaviour you must either leave the title unstyled (native nav bar defaults) or render your own header via the `header?: (props: NativeStackHeaderProps) => React.ReactNode` option / `headerShown: false`.  
   Source: node_modules/expo-router/build/react-navigation/native-stack/types.d.ts lines 137, 358-360 and grep for /FontScaling/ returning no hits (this run)
14. **verified** (expo/expo sdk-57 branch template; local project expo 57.0.22) — The SDK 57 default template's root layout is `ThemeProvider` (imported from `expo-router`, not react-navigation) wrapping `<AnimatedSplashOverlay />` + `<AppTabs />`, with a module-level `SplashScreen.preventAutoHideAsync()` and no `useFonts` call — whereas this repo's src/app/_layout.tsx is already reduced to `export default function RootLayout() { return <Stack />; }`, so there is nothing left to strip.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/templates/expo-template-default/src/app/_layout.tsx (fetched this run) + the local src/app/_layout.tsx

### Open uncertainties

- I did not run `npx expo start`, so I could not observe the generated `.expo/types` / `expo-env.d.ts` contents or confirm the exact shape of the typed `Href` union for a nested `econsult` group in this project — the tsconfig already includes both paths but neither file exists yet on disk.
- I did not find an authoritative source describing how the iOS UINavigationBar title behaves at accessibility Dynamic Type sizes under react-native-screens 4.26. My claim is a verified negative (no allowFontScaling/maxFontSizeMultiplier option exists in the native-stack option types) rather than a positive statement about rendering; test on the simulator with Larger Text at max before committing to the native header.
- Android back-button interception: `usePreventRemove` is present in the vendored react-navigation core and importable from `expo-router/react-navigation`, but I did not find Expo-authored SDK 57 documentation endorsing that import path, and I could not run it. Treat the import path as verified-by-types but the runtime behaviour with the native stack as untested.
- `app.json` sets `android.predictiveBackGestureEnabled: false`; I did not open a doc confirming how that interacts with `usePreventRemove` or with a `replace`d confirmation screen on Android 14+.
- I read the SDK 57 default template's root `_layout.tsx` from the `sdk-57` branch on GitHub, but did not open `components/animated-icon` or `app-tabs`, so I cannot describe exactly what `AnimatedSplashOverlay` does beyond that it pairs with the module-level `SplashScreen.preventAutoHideAsync()` call.
- The `unstable_settings` `anchor` key is confirmed in the compiled `getRoutesCore.js`, but the docs page I fetched still describes the feature primarily as `initialRouteName`; I did not find a changelog entry stating when `anchor` became the preferred name or whether `initialRouteName` is formally deprecated in 57.
- Whether `Stack.Protected` is a cleaner way to gate the `sent` screen (e.g. `guard={hasSubmitted}`) than `replace` — I confirmed the export exists in the type declarations but did not open its documentation or implementation.

### Adversarial verification

**Verdict:** partly-wrong

Claims the verifier refuted or corrected:

- **Claim:** Recommendation: "All of it lives in expo-router, which is JS-only on top of react-native-screens already bundled in Expo Go."  
  **Correction:** False as stated. expo-router 57.0.21 ships its own native code — expo-module.config.json declares Apple modules `ExpoHeadModule`, `LinkPreviewNativeModule`, `RouterToolbarModule` plus an `ExpoHeadAppDelegateSubscriber`, and Android module `expo.modules.router.ExpoRouterModule` with a local-maven-repo publication. The conclusion (no dev build needed) still holds, but only because expo-router is listed in sdk-57 bundledNativeModules.json as `~57.0.21`, not because it is JS-only.  
  Source: node_modules/expo-router/expo-module.config.json and node_modules/expo-router/{ios,android}/ (read this run) + https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo/bundledNativeModules.json
- **Claim:** Recommendation: "keep `predictiveBackGestureEnabled: false` (already in app.json) and add `usePreventRemove(...)`" as an Android back-prevention measure.  
  **Correction:** Misleading twice over: the SDK 57 app config reference gives `android.predictiveBackGestureEnabled` a **default of `false`**, so the app.json entry changes nothing; and it is a native-manifest setting applied when config plugins run during `npx expo prebuild` ("Config plugins allow you to modify these native projects … and applying them when you run `npx expo prebuild`"), so it has no effect on the prebuilt Expo Go client this project must run in. Back-prevention on Android must come entirely from JS (`usePreventRemove`) or from not leaving a back target.  
  Source: https://docs.expo.dev/versions/v57.0.0/config/app/ and https://docs.expo.dev/config-plugins/introduction/ (both fetched this run)
- **Claim:** Fact 11: "`anchor` is the current name and `initialRouteName` still works as a fallback", cited partly to https://docs.expo.dev/router/advanced/router-settings/.  
  **Correction:** The precedence order is correct in the compiled source (getRoutesCore.js lines 654, 666, 708, 720 read this run), but the cited docs page never mentions `anchor` at all — it documents only `initialRouteName`, so "anchor is the current name" is unsupported by its own source. The same page also adds a limitation the digest omits: `initialRouteName` "only applies during deep-linking scenarios" and you need `withAnchor` (`<Link href="/route" withAnchor />` / `router.push('/route', { withAnchor: true })`) for in-app navigation to place the anchor beneath the target — relevant if the flow is ever entered with `push` rather than a deep link.  
  Source: https://docs.expo.dev/router/advanced/router-settings/ (fetched this run) + node_modules/expo-router/build/getRoutesCore.js
- **Claim:** Fact 1: "`extra.router.root` in app.json can override it" (cited to the CLI source and the src-directory docs page).  
  **Correction:** The CLI code is exactly as described (`exp.extra?.router?.root`, INVALID_ROUTER_ROOT when outside the project), but the documented, user-facing override is the expo-router **config plugin** option — `["expo-router", { "root": "./src/routes" }]` — and the docs call custom roots "highly discouraged", warning that bug reports for projects with custom root directories will not be accepted. Presenting `extra.router.root` as the app.json knob points at an internal field rather than the supported API.  
  Source: https://docs.expo.dev/router/reference/src-directory/ (fetched this run) + node_modules/expo/node_modules/@expo/cli/build/src/start/server/metro/router.js
- **Claim:** Fact 12: the CLI "writes `expo-env.d.ts` containing only `/// <reference types=\"expo/types\" />`".  
  **Correction:** The template in expo-env.js is two parts, not one line: `/// <reference types="expo/types" />` followed by `// NOTE: This file should not be edited and should be in your git ignore`. Also, the CLI does not write the gitignore entry — it already ships in this repo's template .gitignore; the docs only say Expo CLI "will generate a git ignored expo-env.d.ts file".  
  Source: node_modules/expo/node_modules/@expo/cli/build/src/start/server/type-generation/expo-env.js (read this run) + https://docs.expo.dev/router/reference/typed-routes/ + local .gitignore
- **Claim:** Fact 12 / recommendation present typed routes as a settled, production-ready feature ("Typed routes are already on in this project").  
  **Correction:** Enabled it is, but the SDK 57 docs still frame it as unreleased: "While the feature is in beta, you can enable it by setting `experiments.typedRoutes` to `true` in your app.json." For a take-home whose grading includes a clean `npx expo start`, that beta status is worth stating rather than treating `Href` typing as a guarantee.  
  Source: https://docs.expo.dev/router/reference/typed-routes/ (fetched this run)

Expo Go compatibility of every package named:

| Package | In Expo Go | Config plugin | Dev build needed | Note |
|---|---|---|---|---|
| expo (~57.0.22) | yes | no | no | The `expo` package is the Expo Go runtime itself; its own `node_modules/expo/bundledNativeModules.json` (read this run, identical to the sdk-57 branch copy) is the canonical list of third-party native modules baked into the client. |
| expo-router (~57.0.21) | yes | yes | no | SDK 57 API page lists platforms as "Android, iOS, tvOS, Web, Included in Expo Go" and says "If you are using the default template to create a new project, expo-router's config plugin is already configured in your app config" — this repo's app.json already has `"plugins": ["expo-router"]`. |
| expo-router native modules (fact-check on "JS-only") | yes | yes | no | expo-router 57.0.21 is NOT pure JS: expo-module.config.json declares apple modules `ExpoHeadModule`, `LinkPreviewNativeModule`, `RouterToolbarModule` and android `expo.modules.router.ExpoRouterModule`, with ios/ and android/ source dirs present — it works in Expo Go only because it is bundled in the client, not because it has no native code. |
| react (19.2.3) | yes | no | no | Pure JS; version is pinned by SDK 57 and is not in bundledNativeModules.json because it has no native code. |
| react-native (0.86.3) | yes | no | no | The version the Expo Go SDK 57 client is built against; no plugin, no dev build for core RN APIs. |
| react-native-screens (~4.26.0) | yes | no | no | Listed as `"react-native-screens": "~4.26.0"` in bundledNativeModules.json for sdk-57, matching the version installed here, so the native stack works in Expo Go unchanged. |
| react-native-safe-area-context (~5.7.0) | yes | no | no | Listed as `"~5.7.0"` in sdk-57 bundledNativeModules.json, exactly the installed range. |
| react-native-gesture-handler (~2.32.0) | yes | no | no | Listed as `"~2.32.0"` in sdk-57 bundledNativeModules.json; nothing in the routing recommendation actually requires importing it directly, since the native stack's swipe-back is handled by react-native-screens. |
| expo-splash-screen (~57.0.9) | yes | yes | no | Listed as `"~57.0.9"` in bundledNativeModules.json so the JS API runs in Expo Go, but the SDK 57 page warns "From SDK 52 … Expo Go and development builds cannot fully replicate the splash screen experience" and shows the app icon instead — the config-plugin block already in app.json therefore cannot be visually verified from `npx expo start`. |
| react-native-reanimated (4.5.1) + react-native-worklets (0.10.1) | yes | no | no | Both are in sdk-57 bundledNativeModules.json (`"4.5.1"` / `"0.10.1"`); the required Babel transform is wired automatically by babel-preset-expo, which passes `reanimated` and `worklets` platform options through its own index.js — no manual babel.config.js plugin entry. |
| babel-preset-expo | yes | no | no | Build-time only, pure JS; its `plugins/expo-router-plugin.js` inlines EXPO_ROUTER_APP_ROOT via `path.relative(path.dirname(currentFile), appFolder)`, confirming the digest's entry-resolution fact. |
| typescript (~6.0.3) | yes | no | no | devDependency, compile-time only; the CLI's tsconfig type-generation adds `.expo/types/**/*.ts` and `expo-env.d.ts` to `include`, both of which are already present in this repo's tsconfig.json. |
| expo-image-picker (~57.0.17) / expo-camera (~57.0.5) — implied by the later photo step, not by this topic | yes | yes | no | Both appear in sdk-57 bundledNativeModules.json, so the photo step itself does not force a development build; each ships a config plugin for iOS usage-description strings, which matters for a native build but not for `npx expo start` in Expo Go. |


## Topic: data-fetching

*Data fetching + form state for a 4-step e-consult flow on Expo SDK 57 / React 19.2 with React Compiler enabled (TanStack Query v5 vs plain hooks, draft state across expo-router screens, zod validation)*

### Recommendation

Ship three dependencies: @tanstack/react-query@5.102.8, expo-network@~57.0.2, zod@4.6.2. All are pure JS or bundled into Expo Go, so `npx expo start` from a clean clone still works — no development build cost.

Why Query rather than plain hooks here: the brief demands loading, empty, error, sending and offline states on poor connections, and Query gives four of them as data rather than code — isPending / isError / isSuccess for the screen, fetchStatus === 'paused' for offline, and isPending on the mutation for "sending". Its defaults already match the audience: queries retry 3x with exponential backoff capped at 30s, and in the default networkMode 'online' a request made while offline is paused and continued on reconnect instead of failing. Hand-rolled useEffect fetching would need a per-screen state machine plus cancellation and dedupe, and would still leave you writing your own offline handling. For a take-home judged on code quality, the RN-specific wiring is the part that reads as competence, so do it explicitly in a `src/lib/queryClient.ts` + root `_layout.tsx`: the documented expo-network onlineManager listener (with the getNetworkStateAsync seed) and the AppState focusManager effect guarded by Platform.OS !== 'web'. Derive the offline banner from the same expo-network state via useNetworkState() so the UI and Query agree.

Step 2's two requests: one useQueries call for recipients + practice questions with `combine` to expose a single { isPending, isError, isEmpty, data } — it is exactly the documented use and avoids two ad-hoc spinners. Step 4's send: one useMutation whose mutationFn awaits createEConsult(...) then uploadAttachment(id, photo) — a single isPending drives the sending screen and a single onSuccess navigates to confirmation. Two things to handle deliberately and mention in the README: mutations do not retry by default, so set retry explicitly (e.g. 1–2 with backoff) only on the idempotent-safe parts, and treat "consult created but upload failed" as its own outcome ("message sent, photo could not be attached — retry upload") rather than blindly retrying the pair, since a retry of the whole mutationFn would create a second e-consult.

Form/draft state: plain useReducer + a Context provider mounted on the expo-router group layout for the flow (e.g. src/app/(econsult)/_layout.tsx), with a discriminated-union action type. Zero dependencies, the draft naturally lives and dies with the flow (no manual reset like a module-level zustand store would need), it is trivially testable, and a pure reducer is exactly what the React Compiler wants. Skip react-hook-form: its strengths (uncontrolled perf, big schemas) do not apply to three-to-five practice-configured fields, and it still has documented React Compiler friction — the compiler forbids reading or writing ref.current during render, which is where RHF's register/reset path breaks, and the recommended fixes are useWatch/useFormState/Controller or the explicitly-temporary 'use no memo' directive. Adding a library you then have to opt out of the compiler for is a worse story than 40 lines of reducer. Skip zustand too, but name it in the README as the upgrade path if the draft ever needs to outlive the flow (persistence/resume).

React Compiler rules to respect while writing this: no mutation of props/state/refs during render (lazy `if (ref.current === null)` init is the one allowed case), keep the reducer pure, no conditional hooks. Expo compiles app code only — not node_modules — so Query's internals are untouched; your components are the surface that must be rules-clean. Run `npx expo-doctor` / the react-hook lint rules as evidence in the README.

zod: validate the two GET responses inside the queryFn with safeParse and map a parse failure onto the same error UI (a malformed practice-question payload should not crash a 78-year-old's screen), and infer the TS types from the schemas so there is one source of truth. 2kb gzipped, zero dependencies, TS 5.5+ — your tsconfig already has strict: true. Keep zod at the network boundary only; do not reach for it as a form-validation engine here, the reducer can hold the two or three "required" checks.

**Packages involved:** @tanstack/react-query, expo-network, zod

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (@tanstack/react-query 5.102.8) — @tanstack/react-query's latest release is 5.102.8, its only runtime dependency is @tanstack/query-core@5.102.8 and its peer range is react ^18 || ^19, so it is pure JS with no native module and works in Expo Go.  
   Source: https://registry.npmjs.org/@tanstack/react-query/latest (fetched via curl)
2. **verified** (TanStack Query v5 docs, read 2026-09-11) — The official React Native page ships an expo-network wiring for onlineManager: onlineManager.setEventListener((setOnline) => { ... Network.addNetworkStateListener((state) => setOnline(!!state.isConnected)) ... Network.getNetworkStateAsync().then(...) ; return eventSubscription.remove }).  
   Source: https://tanstack.com/query/latest/docs/framework/react/react-native and https://raw.githubusercontent.com/TanStack/query/main/docs/framework/react/react-native.md
3. **verified** (TanStack Query v5 docs, read 2026-09-11) — The same page wires refetch-on-app-focus with AppState: an effect subscribing to AppState 'change' that calls focusManager.setFocused(status === 'active') guarded by Platform.OS !== 'web', and unsubscribes via subscription.remove().  
   Source: https://raw.githubusercontent.com/TanStack/query/main/docs/framework/react/react-native.md
4. **verified** (TanStack Query v5 docs, read 2026-09-11) — networkMode defaults to 'online', so while offline queries and mutations do not execute and report fetchStatus 'paused', and when the connection returns they 'continue' rather than refetch.  
   Source: https://tanstack.com/query/latest/docs/framework/react/guides/network-mode
5. **verified** (TanStack Query v5 docs, read 2026-09-11) — Queries retry 3 times by default with delay Math.min(1000 * 2 ** attemptIndex, 30000), while mutations are not retried at all by default ('TanStack Query will not retry a mutation on error, but it is possible with the retry option').  
   Source: https://tanstack.com/query/latest/docs/framework/react/guides/query-retries and .../guides/mutations
6. **verified** (TanStack Query v5 docs, read 2026-09-11) — useQueries 'can be used to fetch a variable number of queries' in one hook and its combine option merges the results into one structurally-shared value, which is the documented way to render a single loading/error state for two parallel requests.  
   Source: https://tanstack.com/query/latest/docs/framework/react/reference/useQueries
7. **verified** (TanStack Query v5 docs, read 2026-09-11) — For a sequential create-then-upload, the docs say to 'use mutateAsync instead of mutate to get a promise which will resolve on success or throw on an error', and warn that callbacks passed to mutate() fire only once and only if the component is still mounted (whereas useMutation-level handlers run per call).  
   Source: https://tanstack.com/query/latest/docs/framework/react/guides/mutations
8. **verified** (TanStack Query v5 docs, read 2026-09-11) — A query exposes isPending / isError / isSuccess plus a separate fetchStatus of 'fetching' | 'paused' | 'idle', which maps one-to-one onto the required loading, error, success and offline UI states without hand-rolling a state machine.  
   Source: https://tanstack.com/query/latest/docs/framework/react/guides/queries
9. **verified** (expo-network 57.0.2 (SDK 57 docs)) — expo-network 57.0.2 is 'Included in Expo Go', has zero runtime dependencies (peers expo and react only), and exposes addNetworkStateListener(listener) returning an EventSubscription plus a useNetworkState() hook.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/network/ and https://registry.npmjs.org/expo-network/latest
10. **verified** (react.dev React Compiler docs + Expo SDK 54+ guide, read 2026-09-11) — React Compiler 'is now stable', and Expo enables it through app config experiments.reactCompiler (already true in this project's app.json) but only for application code — 'no node modules' — and it is disabled during server rendering, so library internals are never compiled while your components that call them are.  
   Source: https://react.dev/learn/react-compiler/introduction, https://docs.expo.dev/guides/react-compiler/, app.json
11. **verified** (eslint-plugin-react-hooks lints docs, read 2026-09-11) — The compiler-aligned refs lint rule forbids both reading and writing ref.current during render (values 'can be stale or inconsistent'), allowing only the lazy-initialization pattern `if (ref.current === null) ref.current = ...`.  
   Source: https://react.dev/reference/eslint-plugin-react-hooks/lints/refs
12. **verified** (react.dev React Compiler directives, read 2026-09-11) — The only documented escape hatch for compiler-hostile code is the 'use no memo' directive, and the docs state opt-out directives 'should be temporary' (add with a TODO, track, fix, remove).  
   Source: https://react.dev/reference/react-compiler/directives
13. **verified** (react-hook-form 7.87.0, zustand 5.0.15; discussion last summarised May 2026) — react-hook-form is at 7.87.0 (peer react ^16.8 || ^17 || ^18 || ^19) but its React Compiler discussion still lists failing patterns — watch() via useFormContext, formState reads in child components, and register combined with reset — with useWatch/useFormState/Controller or 'use no memo' as the workarounds; zustand is at 5.0.15 (peer react >=18).  
   Source: https://registry.npmjs.org/react-hook-form/latest, https://github.com/orgs/react-hook-form/discussions/12524, https://registry.npmjs.org/zustand/latest
14. **verified** (zod 4.6.2) — zod's latest is 4.6.2 (published 2026-09-10), advertised as a '2kb core bundle (gzipped)' with 'zero external dependencies', tested against TypeScript v5.5+ and requiring tsconfig strict (this project already sets strict: true and TS ~6.0.3).  
   Source: https://zod.dev/, https://registry.npmjs.org/zod (dist-tags + time), tsconfig.json

### Open uncertainties

- zod.dev states 'Works in Node.js and all modern browsers' but I found no explicit React Native / Hermes support statement in the docs I opened — RN compatibility is widely relied on in practice but I did not verify it from an official source this run.
- The react-hook-form React Compiler status came from GitHub Discussion #12524 (community thread, latest comments summarised as May 2026), not official RHF docs; the exact set of still-broken patterns may have changed since.
- I did not verify TanStack Query v5's own behaviour under React Compiler; Expo only compiles app code (not node_modules), so library internals should be unaffected, but I read no compatibility statement from either project.
- I did not check whether the TanStack Query DevTools options listed for React Native (Rozenite, native macOS app, Flipper, Reactotron) work in Expo Go without a development build — assume they need one until checked.
- expo-network is not yet in this project's package.json; I confirmed 57.0.2 exists on npm and is listed as included in Expo Go, but did not install or run it.
- React 19.2's own form primitives (useActionState / useOptimistic) were not researched this run, so I cannot say whether they would cover the sending state more cheaply than useMutation on React Native.
- The Expo React Compiler guide I opened does not mention SDK 57 explicitly (it says Babel is auto-configured in SDK 54 and later); the project's app.json already sets experiments.reactCompiler: true, which I did confirm.

### Adversarial verification

**Verdict:** partly-wrong

Claims the verifier refuted or corrected:

- **Claim:** Fact 12: react-hook-form's React Compiler discussion 'still lists failing patterns — watch() via useFormContext, formState reads in child components, and register combined with reset — with useWatch/useFormState/Controller or "use no memo" as the workarounds', used to justify 'Skip react-hook-form'.  
  **Correction:** OUTDATED / MISCHARACTERISED. That failing-API table is the Feb 2026 comment; the discussion's latest substantive update (2026-05-06, on the GA stack React 19.2.5 + react-hook-form 7.75 + babel-plugin-react-compiler 1.0.0) says 'most of the historically broken APIs are now fixed... If you only stuck "use no memo" on useForm hosts to dodge watch, Controller, getValues, reset, useFieldArray, etc., you can probably drop it', reducing the remaining advice to style habits (prefer useWatch/useFormState over render-time watch()). Separately, RHF release v7.79.0 (2026-06-13) shipped 'fix #13505 issue: In v8 with React compiler, a change in formState errors does not cause child components to re-render (#13510)' — i.e. the exact 'formState reads in child components' pattern the digest lists as broken was fixed two months before the 7.87.0 the digest cites. The 'skip RHF' recommendation may still be right on scope grounds (3-5 fields), but the compiler-friction argument is no longer supported by the source.  
  Source: https://api.github.com/repos/react-hook-form/react-hook-form/discussions/12524/comments (comments of 2026-05-06 and 2026-05-14) + https://api.github.com/repos/react-hook-form/react-hook-form/releases (v7.79.0 body), opened 2026-09-11
- **Claim:** Fact 8: isPending/isError/isSuccess plus fetchStatus 'maps one-to-one onto the required loading, error, success and offline UI states without hand-rolling a state machine'.  
  **Correction:** UNSUPPORTED BY THE CITED PAGE — the docs say the opposite about the mapping being one-to-one. guides/queries.md: 'a query that mounts and has no data will usually be in pending status and fetching fetchStatus, but it could also be paused if there is no network connection', and guides/network-mode.md warns 'it might not be enough to check for pending state to show a loading spinner. Queries can be in state: "pending", but fetchStatus: "paused"'. status and fetchStatus are orthogonal (a cached success query can also be paused), so the flow still needs an explicit precedence rule — paused-before-pending — in the UI. The underlying API facts are correct; the 'one-to-one, no state machine' framing is the digest's inference, not the source's.  
  Source: https://raw.githubusercontent.com/TanStack/query/main/docs/framework/react/guides/queries.md + .../guides/network-mode.md, opened 2026-09-11
- **Claim:** Recommendation: expo-network is presented as the dependency to add for onlineManager wiring ('Ship three dependencies ... expo-network@~57.0.2').  
  **Correction:** INCOMPLETE — the TanStack React Native page lists @react-native-community/netinfo FIRST and expo-network only as the 'or' alternative, and NetInfo is equally free here: expo/expo sdk-57 bundledNativeModules.json pins '@react-native-community/netinfo': '12.0.1', so it is also bundled in Expo Go. expo-network is a defensible preference (first-party, useNetworkState hook), but the digest states it as if it were the only Expo-Go-compatible route; it is not a constraint.  
  Source: https://raw.githubusercontent.com/TanStack/query/main/docs/framework/react/react-native.md + https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo/bundledNativeModules.json, opened 2026-09-11
- **Claim:** Fact 6 cites https://tanstack.com/query/latest/docs/framework/react/reference/useQueries for the combine option.  
  **Correction:** CLAIM CORRECT, SOURCE CAVEAT ONLY — the live page does exist and says verbatim 'The useQueries hook can be used to fetch a variable number of queries' and 'The combine option can be used to combine the results of the queries into a single value. The result will be structurally shared to be as referentially stable as possible.' Note the markdown mirror path the digest used for its other facts (docs/framework/react/reference/useQueries.md on GitHub main) returns 404 — that reference directory now only contains classes/functions/interfaces/type-aliases — so only the website URL is checkable. No correction to the claim itself.  
  Source: https://tanstack.com/query/latest/docs/framework/react/reference/useQueries + https://api.github.com/repos/TanStack/query/contents/docs/framework/react/reference, opened 2026-09-11

Expo Go compatibility of every package named:

| Package | In Expo Go | Config plugin | Dev build needed | Note |
|---|---|---|---|---|
| @tanstack/react-query@5.102.8 | yes | no | no | npm registry shows latest 5.102.8 with a single runtime dep (@tanstack/query-core@5.102.8) and peer react ^18 \|\| ^19; the published tarball file listing (415 files) contains only build/*.js\|.cjs\|.d.ts — no /android, /ios, .podspec, expo-module.config.json or app.plugin.js, so it is pure JS and runs in Expo Go with no dev build. Digest fact 1 verified. |
| expo-network@57.0.2 | yes | no | no | Native module (ships /android/*.kt, /ios/*.swift, ExpoNetwork.podspec, expo-module.config.json) BUT it is bundled: expo/expo sdk-57 branch bundledNativeModules.json pins "expo-network": "~57.0.2" and the SDK 57 API page renders the 'Included in Expo Go' badge with 'Recommended version: ~57.0.2'. No app.plugin.js in the tarball and no 'Configuration in app config' section on the docs page — ACCESS_NETWORK_STATE/ACCESS_WIFI_STATE are 'added automatically' on Android. Install with npx expo install expo-network. |
| zod@4.6.2 | yes | no | no | npm dist-tags latest = 4.6.2 published 2026-09-10T21:44:31Z, no dependencies and no peerDependencies; tarball listing (840 files) has zero native/plugin markers. zod.dev states 'Zero external dependencies' and 'Tiny: 2kb core bundle (gzipped)'. Note: zod.dev still makes no React Native/Hermes support statement — the digest's own uncertainty on this is correct. |
| react-hook-form@7.87.0 (evaluated, rejected by digest) | yes | no | no | npm latest 7.87.0 (published 2026-08-30), no dependencies, peer react ^16.8 \|\| ^17 \|\| ^18 \|\| ^19; tarball has no native/plugin markers. Its Expo-Go cost is zero — the digest's reason to skip it is React Compiler friction, which is now largely stale (see refuted facts). |
| zustand@5.0.15 (evaluated, rejected by digest) | yes | no | no | npm latest 5.0.15, no runtime dependencies (peers react >=18, plus optional immer/use-sync-external-store/@types/react), tarball has no native/plugin markers — pure JS, Expo Go safe. |
| @rozenite/tanstack-query-plugin (devtools option named in the TanStack RN page) | no | no | yes | UNVERIFIED — defaulting to needsDevBuild=true as instructed. Rozenite's own quick start requires wrapping metro.config.js with withRozenite plus opening React Native DevTools; neither the getting-started page nor the TanStack Query plugin page mentions Expo or Expo Go. It is a Metro/devtools integration, not an Expo config plugin, and is a devDependency only — not needed for the recommended stack. |


## Topic: testing-lint

*Testing, linting and type-checking setup for the Expo SDK 57 / RN 0.86.3 / React 19.2.3 / TypeScript 6.0 e-consult app*

### Recommendation

Add exactly this devDependency block (all versions verified on npm / the Expo SDK 57 manifest this run):

  "devDependencies": {
    "@types/react": "~19.2.4",
    "typescript": "~6.0.3",
    "jest": "~29.7.0",
    "@types/jest": "29.5.14",
    "jest-expo": "~57.0.5",
    "@react-native/jest-preset": "^0.86.3",
    "@testing-library/react-native": "13.3.3",
    "eslint": "^9.0.0",
    "eslint-config-expo": "~57.0.2",
    "eslint-plugin-prettier": "^5.5.6",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6"
  }

The single most important decision: PIN @testing-library/react-native to 13.3.3, do not take 14.x. RNTL 14 made render/fireEvent/act async and swapped react-test-renderer for the new `test-renderer` package, but expo-router 57.0.21 still declares peer ">= 13.2.0", devDepends on ^13.3.0, and its compiled renderRouter calls render() and act() synchronously — under v14 renderRouter returns a Promise that its own typings model as a sync object, so every router test breaks. jest-expo 57.0.5 already brings react-test-renderer 19.2.3, which satisfies RNTL 13.3.3's peer, so nothing extra is needed. Likewise pin the Jest 29 line (jest-expo 57.0.5 depends on babel-jest/jest-environment-jsdom/@jest/globals ^29.2.1 — mixing in jest 30 gives you a v30 runner with v29 environment packages), and add @react-native/jest-preset explicitly since it is a required non-optional peer that throws a migration error when missing.

Jest config — keep it minimal in package.json; do NOT copy the transformIgnorePatterns snippet from the Expo unit-testing docs page, it is stale relative to what jest-preset.js actually sets (which already covers expo, @expo, @react-native, react-native-reanimated/plugin and @react-native/babel-preset):

  "jest": { "preset": "jest-expo" },
  "scripts": { "test": "jest", "test:watch": "jest --watchAll", "lint": "expo lint", "typecheck": "tsc" }

Testing approach for this flow. Write the recipient-picker, question, message and confirmation screens as expo-router tests using `import { renderRouter, screen, testRouter } from 'expo-router/testing-library'` with inline mock route objects and `{ initialUrl: '/econsult/new' }`, asserting navigation with `getPathname()`/`testRouter.push()`. Drive interactions with `const user = userEvent.setup()` and `await user.press(...)` / `await user.type(...)` rather than fireEvent — RNTL's docs explicitly recommend userEvent where supported because it emits the real host-element event sequence; enable Jest fake timers since `press` costs 130 ms and `longPress` 500 ms of simulated time each. Because the users are elderly VoiceOver/TalkBack patients, make the queries themselves the accessibility test: `screen.getByRole('button', { name: 'Versturen' })`, `getByRole('button', { name: 'Foto toevoegen', disabled: true })` while offline, and `getByLabelText(...)` for the photo preview and remove control. Remember a plain View only matches *ByRole if it carries `accessible={true}` — if a query fails, that is usually a genuine screen-reader bug, not a test bug. Use `getByRole('progressbar', { busy: true })` / `toHaveAccessibilityValue` for the sending state so the two-call create-then-upload sequence is announced.

Mocking expo-image-picker and expo-network: jest-expo already stubs the native `ExponentImagePicker` and `ExpoNetwork` modules, but the auto-mock returns `Promise.resolve(undefined)`, so `result.canceled` and `state.isConnected` blow up. Add a jest setup file (`setupFilesAfterEach`/`setupFiles` in your jest config) with explicit factories: `jest.mock('expo-image-picker', () => ({ launchCameraAsync: jest.fn(async () => ({ canceled: false, assets: [{ uri: 'file:///photo.jpg', width: 100, height: 100, mimeType: 'image/jpeg' }] })), launchImageLibraryAsync: jest.fn(async () => ({ canceled: true, assets: null })), requestCameraPermissionsAsync: jest.fn(async () => ({ granted: true, status: 'granted' })), requestMediaLibraryPermissionsAsync: jest.fn(async () => ({ granted: true, status: 'granted' })), MediaTypeOptions: { Images: 'Images' } }))` and `jest.mock('expo-network', () => ({ getNetworkStateAsync: jest.fn(async () => ({ isConnected: true, isInternetReachable: true, type: 'WIFI' })), useNetworkState: jest.fn(() => ({ isConnected: true, isInternetReachable: true })) }))`. Flip those return values per-test to cover the offline, permission-denied, cancelled-picker and upload-failed states. Note both expo-image-picker and expo-network still need `npx expo install expo-image-picker expo-network` — they are not in package.json today.

Linting: just run `npx expo lint` once and let it do the work — it installs eslint ^9.0.0 and eslint-config-expo, adds the lint script, and writes the flat config for you. Stay on ESLint 9; npm's eslint@latest is 10.10.0, which is outside the ^9.0.0 range the Expo CLI asks for. Do NOT install eslint-plugin-react-compiler: on SDK 55+ the React Compiler rules ship inside eslint-config-expo via eslint-plugin-react-hooks ^7 recommended, which matters here because the project has reactCompiler enabled. For Prettier, extend the generated file exactly as the Expo docs show:

  const { defineConfig } = require('eslint/config');
  const expoConfig = require('eslint-config-expo/flat');
  const eslintPluginPrettierRecommended = require('eslint-plugin-prettier/recommended');
  module.exports = defineConfig([expoConfig, eslintPluginPrettierRecommended, { ignores: ['dist/*'] }]);

`eslint-plugin-prettier/recommended` pulls in eslint-config-prettier for you, but list it as an explicit devDependency anyway since it is an optional peer.

Type-checking: the command is plain `tsc` — no `--noEmit` needed, because expo/tsconfig.base already sets `noEmit: true` (I read the installed copy). Your existing tsconfig.json already extends it correctly with strict + `@/*` paths and includes `.expo/types/**/*.ts`, which is what makes typed routes work; run `npx expo start` (or `expo customize tsconfig.json`) at least once before `tsc` in CI so `.expo/types` exists, otherwise typed-route hrefs will fail to resolve on a clean clone. TypeScript 6 caveat: stay on ~6.0.3 and do not bump to 7.x — @typescript-eslint/typescript-estree 8.70.0, which eslint-config-expo's `^8.59.0` range resolves to, declares peer `typescript: ">=4.8.4 <6.1.0"`, so TypeScript 7.0.2 would put lint into unsupported-version territory.

Expo Go cost callout: nothing in this setup requires a development build. jest-expo, RNTL, ESLint and tsc are all dev-time only, and expo-image-picker and expo-network are both standard Expo Go-included modules — so `npx expo start` from a clean clone still works.

**Packages involved:** jest-expo, jest, @types/jest, @react-native/jest-preset, babel-jest, jest-environment-jsdom, @testing-library/react-native, react-test-renderer, test-renderer, expo-router, expo-image-picker, expo-network, eslint, eslint-config-expo, eslint-plugin-react-hooks, eslint-plugin-react-compiler, eslint-plugin-prettier, eslint-config-prettier, prettier, typescript, @typescript-eslint/typescript-estree, @types/react, expo, @expo/cli

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (jest-expo 57.0.5) — jest-expo's SDK 57 line is 57.0.5 (npm `latest`), and its own package.json pins the Jest **29** ecosystem — `babel-jest ^29.2.1`, `@jest/globals ^29.2.1`, `jest-environment-jsdom ^29.2.1`, `jest-snapshot ^29.2.1` — so the project must run Jest 29, not Jest 30.  
   Source: https://registry.npmjs.org/jest-expo + https://unpkg.com/jest-expo@57.0.5/package.json (opened this run)
2. **verified** (SDK 57.0.0 manifest, read 2026-09-11) — The Expo versions API pins, for SDK 57, `jest: ~29.7.0`, `@types/jest: 29.5.14`, `typescript: ~6.0.3` and `@types/react: ~19.2.4` — these are exactly what `npx expo install --dev` will write, and npm's `jest@latest` (30.5.1) is NOT what you want.  
   Source: https://api.expo.dev/v2/versions/latest → data.sdkVersions["57.0.0"].relatedPackages (opened this run)
3. **verified** (jest-expo 57.0.5, @react-native/jest-preset 0.86.3) — jest-expo 57.0.5 declares a **required** (non-optional) peer dependency `@react-native/jest-preset: ^0.86.3`, and its jest-preset.js throws `'The React Native Jest preset that jest-expo relies on has moved to a separate package.'` if you forget it — 0.86.3 is published, so add it explicitly as a devDependency.  
   Source: https://unpkg.com/jest-expo@57.0.5/package.json + /jest-preset.js + https://registry.npmjs.org/@react-native/jest-preset (opened this run)
4. **verified** (jest-expo 57.0.5) — You only need `"jest": { "preset": "jest-expo" }` in package.json: jest-preset.js already sets `transformIgnorePatterns` to `['/node_modules/(?!(.pnpm|react-native|@react-native|@react-native-community|expo|@expo|@expo-google-fonts|react-navigation|@react-navigation|@sentry/react-native|native-base|standard-navigation))', '/node_modules/react-native-reanimated/plugin/', '/node_modules/@react-native/babel-preset/']`, which differs from (and supersedes) the older single-line pattern still printed on the Expo unit-testing docs page.  
   Source: https://unpkg.com/jest-expo@57.0.5/jest-preset.js vs https://docs.expo.dev/develop/unit-testing/ (both opened this run)
5. **verified** (@testing-library/react-native 14.0.1, released 2026-06-23) — @testing-library/react-native `latest` is 14.0.1, but v14.0.0 is a hard breaking release: `render()`/`renderHook()` return Promises, `fireEvent()` returns a Promise, `act()` must always be awaited, and the deprecated `react-test-renderer` is replaced by a new peer dependency `test-renderer: ^1.0.0`.  
   Source: https://api.github.com/repos/callstack/react-native-testing-library/releases (v14.0.0 body) + npm peerDependencies for 14.0.1 (opened this run)
6. **verified** (expo-router 57.0.21 vs RNTL 13.3.3 / 14.0.1) — expo-router 57.0.21 is NOT compatible with RNTL 14: it declares peer `@testing-library/react-native: ">= 13.2.0"`, devDepends on `^13.3.0`, and its shipped `build/testing-library/index.js` calls `rnTestingLibrary.render(...)` and `rnTestingLibrary.act(() => router.push(path))` synchronously without awaiting — so pin `@testing-library/react-native@13.3.3` (the last 13.x), which pairs with the `react-test-renderer@19.2.3` that jest-expo 57.0.5 already installs.  
   Source: https://unpkg.com/expo-router@57.0.21/package.json, /build/testing-library/index.js, /build/testing-library/index.d.ts + npm 13.3.3 peers (opened this run)
7. **verified** (RNTL 13.x docs) — RNTL's own docs say to prefer User Event over Fire Event where supported — "If User Event supports a given interaction, prefer it over the Fire Event counterpart, as it makes your tests more realistic and reliable" — because userEvent fires the full host-element event sequence with real event data, while fireEvent just calls the `onEventName` prop and traverses up to composite elements.  
   Source: https://raw.githubusercontent.com/callstack/react-native-testing-library/main/website/docs/13.x/docs/api/events/user-event.mdx (opened this run)
8. **verified** (RNTL 13.x docs) — Create the instance once with `const user = userEvent.setup()` (options `delay` and `advanceTimers`; the default setup handles both real and Jest fake timers), then `await user.press(el)` / `await user.type(el, text)` / `await user.longPress(el)` — note `press` takes a minimum of 130 ms of simulated time and `longPress` 500 ms, so use fake timers to keep the suite fast.  
   Source: https://raw.githubusercontent.com/callstack/react-native-testing-library/main/website/docs/13.x/docs/api/events/user-event.mdx (opened this run)
9. **verified** (RNTL 13.x docs) — For accessibility-first queries, `screen.getByRole('button', { name: 'Versturen', disabled: false })` supports options `name`, `disabled`, `selected`, `checked`, `busy`, `expanded` and `value` (reading `aria-*` or `accessibilityState`/`accessibilityValue`), but a `View` only matches if it has an explicit `accessible={true}`; `getByLabelText` matches `aria-label`/`accessibilityLabel` or the text of the view referenced by `aria-labelledby`/`accessibilityLabelledBy`.  
   Source: https://raw.githubusercontent.com/callstack/react-native-testing-library/main/website/docs/13.x/docs/api/queries.mdx (opened this run)
10. **verified** (expo-router 57.0.21) — expo-router screens are tested via `import { renderRouter, screen } from 'expo-router/testing-library'`, where the context can be an inline mock object (`{ index: MockComponent, '(group)/b': MockComponent }`, no leading `./` and no extension), a string array, or a fixture dir, with `{ initialUrl: '/directory/a' }`; the result adds `getPathname()`, `getPathnameWithParams()`, `getSegments()`, `getSearchParams()`, `getRouterState()`, and a `testRouter` helper exposes `navigate/push/replace/back/setParams/dismissAll` that assert the pathname for you.  
   Source: https://docs.expo.dev/router/reference/testing/ + https://unpkg.com/expo-router@57.0.21/build/testing-library/index.d.ts (opened this run)
11. **verified** (jest-expo 57.0.5) — jest-expo's setup already registers native-module stubs named `ExponentImagePicker` (`launchCameraAsync`, `launchImageLibraryAsync`, `getCameraPermissionsAsync`, `getMediaLibraryPermissionsAsync`, `requestPermissionsAsync`) and `ExpoNetwork` (`getNetworkStateAsync`, `getIpAddressAsync`), but the generic mock factory returns `jest.fn(() => Promise.resolve())` — i.e. `undefined` — so code reading `result.canceled` or `state.isConnected` will crash unless you add your own `jest.mock('expo-image-picker', ...)` / `jest.mock('expo-network', ...)` returning real-shaped payloads.  
   Source: https://unpkg.com/jest-expo@57.0.5/src/preset/setup.js + /src/preset/moduleMocks/expoModules.js (opened this run)
12. **verified** (@expo/cli 57.0.24, eslint-config-expo 57.0.2) — On first run `npx expo lint` installs exactly two devDependencies — `eslint` at range `^9.0.0` and `eslint-config-expo` (version resolved by expo install, currently 57.0.2) — writes a `lint` script, and copies a flat config template whose full contents are: `const { defineConfig } = require('eslint/config'); const expoConfig = require("eslint-config-expo/flat"); module.exports = defineConfig([expoConfig, { ignores: ["dist/*"] }]);`.  
   Source: https://unpkg.com/@expo/cli@57.0.24/build/src/lint/ESlintPrerequisite.js + /static/template/eslint.config.js (opened this run)
13. **verified** (eslint-config-expo 57.0.2, eslint-plugin-react-hooks 7.1.1) — React Compiler lint rules need no extra plugin on SDK 57 — the Expo docs state "React Compiler lint rules are included by default with eslint-config-expo in SDK 55 and later. If you previously installed eslint-plugin-react-compiler, you can uninstall it" — and eslint-config-expo 57.0.2 delivers this by depending on `eslint-plugin-react-hooks ^7.0.0` and spreading `pluginReactHooks.configs.recommended.rules` in flat/utils/react.js.  
   Source: https://docs.expo.dev/guides/react-compiler/ + https://unpkg.com/eslint-config-expo@57.0.2/flat/utils/react.js + npm eslint-config-expo deps (opened this run)
14. **verified** (expo 57.0.22, typescript-eslint 8.70.0, typescript 6.0.3 vs 7.0.2) — No `--noEmit` flag is needed: the local `node_modules/expo/tsconfig.base.json` already sets `"noEmit": true` (alongside `moduleResolution: "bundler"`, `customConditions: ["react-native"]`, `jsx: "react-jsx"`), so `tsc` alone type-checks — and you must stay on TypeScript ~6.0.3 because `@typescript-eslint/typescript-estree@8.70.0` (what eslint-config-expo's `^8.59.0` resolves to) declares peer `typescript: ">=4.8.4 <6.1.0"`, which excludes the current npm `typescript@latest` of 7.0.2.  
   Source: node_modules/expo/tsconfig.base.json + https://registry.npmjs.org/@typescript-eslint/typescript-estree + https://registry.npmjs.org/typescript (opened this run)

### Open uncertainties

- I did NOT find an official Expo statement blessing @testing-library/react-native 13.3.3 for SDK 57 — my pin is inferred from expo-router 57.0.21's peer range (">= 13.2.0"), its devDependency (^13.3.0) and its synchronous render()/act() calls, which I read directly. I did not actually run a renderRouter test against RNTL 14 to confirm the breakage empirically.
- The Expo unit-testing docs page says @testing-library/react-native "replaces the deprecated react-test-renderer due to React 19+ incompatibility", which reads as an endorsement of the RNTL 14 direction, yet jest-expo 57.0.5 still ships react-test-renderer 19.2.3 as a hard dependency. I could not resolve this apparent contradiction from the sources I opened; it may mean an expo-router release adding RNTL 14 support is imminent.
- The exact `eslint-config-expo` version string that `npx expo lint` writes into package.json is resolved at install time by Expo's version-resolution logic, not hardcoded in @expo/cli — I read the ESlintPrerequisite.js source and it specifies no version for that package. I used npm `latest` (57.0.2) as the value, which I verified is published, but did not confirm it is what the resolver picks.
- I verified eslint-config-expo 57.0.2 depends on eslint-plugin-react-hooks ^7.0.0 and spreads its `recommended` rules, and I read Expo's docs claim that React Compiler rules are included from SDK 55 on — but I did not open eslint-plugin-react-hooks v7's own config source to enumerate which specific react-compiler rules `recommended` turns on.
- I did not open any source confirming expo-image-picker and expo-network are included in the SDK 57 Expo Go client, nor their SDK 57 version numbers — neither appeared in the SDK 57 relatedPackages manifest I fetched. That part of my Expo Go cost callout is recalled, not verified; check https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/ before relying on it.
- I did not open documentation for the `setupFilesAfterEach` vs `setupFilesAfterEach`/`setupFiles` distinction under Jest 29 for the mock setup file, nor verify that jest-expo's preset leaves those keys free for you to extend. I only read that the preset pushes its own entry onto `setupFiles`.
- The claim that `.expo/types` must exist before `tsc` for typed routes to resolve on a clean clone is recalled, not verified — the Expo TypeScript docs page I fetched contained no mention of typed routes or `.expo/types` at all.
- I could not open the RNTL docs site directly (oss.callstack.com returned HTTP 403), so all RNTL API claims come from the 13.x markdown sources in the GitHub repo's main branch, which may drift slightly from the published 13.3.3 docs.

### Adversarial verification

**Verdict:** partly-wrong

Claims the verifier refuted or corrected:

- **Claim:** Fact 6: 'expo-router 57.0.21 is NOT compatible with RNTL 14: it declares peer @testing-library/react-native: ">= 13.2.0"'  
  **Correction:** The peer range `>= 13.2.0` ADMITS 14.0.1 — it does not exclude RNTL 14 at all, so it is evidence against the digest's own conclusion, not for it. It is also an OPTIONAL peer: expo-router 57.0.21's peerDependenciesMeta contains `"@testing-library/react-native": { "optional": true }`, so npm will not even warn. The only real evidence for incompatibility is the compiled code, which I did confirm: build/testing-library/index.js does `const result = rnTestingLibrary.render(<ExpoRoot .../>, options)` and then `Object.assign(result, { getPathname() {...} })`, and testRouter does `rnTestingLibrary.act(() => router.push(path)); expect(screen).toHavePathnameWithParams(path)` — all un-awaited. The pin advice survives; the stated justification does not.  
  Source: https://registry.npmjs.org/expo-router (57.0.21 peerDependencies + peerDependenciesMeta) and https://unpkg.com/expo-router@57.0.21/build/testing-library/index.js, both opened 2026-09-11
- **Claim:** Recommendation: 'under v14 renderRouter returns a Promise that its own typings model as a sync object'  
  **Correction:** False as written. The shipped index.d.ts defines `type Result = ReturnType<typeof rnTestingLibrary.render> & { getPathname(): string; ... }` — a structural reference to whatever RNTL version is installed. Under RNTL 14 that resolves to `Promise<RenderResult> & {...}`, i.e. the typings DO surface the Promise. So TypeScript would actually flag most misuse; the failure is a runtime/ordering one (render never flushed, act never awaited), not a silent typing lie.  
  Source: https://unpkg.com/expo-router@57.0.21/build/testing-library/index.d.ts + RNTL v14.0.0 release notes ('render() now returns Promise<RenderResult>'), both opened
- **Claim:** Fact 10: jest-expo 'registers native-module stubs named ExponentImagePicker (launchCameraAsync, launchImageLibraryAsync, getCameraPermissionsAsync, getMediaLibraryPermissionsAsync, requestPermissionsAsync)'  
  **Correction:** Two errors. (a) `requestPermissionsAsync` is not in the list; the six entries are getCameraPermissionsAsync, getMediaLibraryPermissionsAsync, launchCameraAsync, launchImageLibraryAsync, requestCameraPermissionsAsync, requestMediaLibraryPermissionsAsync — and the digest's own mock factory later uses the correct requestCameraPermissionsAsync/requestMediaLibraryPermissionsAsync names, so the fact and the code sample disagree. (b) They are not registered as a stub module: expoModules.js exports `{ NativeUnimoduleProxy: { callMethod: {type:'function',functionType:'promise'}, exportedMethods: { type:'object', mock: { ExponentImagePicker: [{name,argumentsCount,key},...], ExpoNetwork: [...] } } } }` — i.e. `{name, argumentsCount, key}` METADATA descriptors hung off NativeUnimoduleProxy.exportedMethods, passed through verbatim as a customMock. The `jest.fn(() => Promise.resolve())` factory applies to `callMethod` (functionType 'promise'), not to these entries. The practical conclusion — write your own jest.mock('expo-image-picker')/jest.mock('expo-network') returning real-shaped payloads — is still correct.  
  Source: https://unpkg.com/jest-expo@57.0.5/src/preset/moduleMocks/expoModules.js and /src/preset/setup.js, both opened
- **Claim:** Recommendation: the RNTL 13.3.3 pin can be reached through the documented Expo install flow  
  **Correction:** Unsupported and an active trap. @testing-library/react-native is absent from node_modules/expo/bundledNativeModules.json (grepped: no match), so `npx expo install @testing-library/react-native --dev` — the exact command https://docs.expo.dev/develop/unit-testing/ prints — resolves npm `latest` = 14.0.1 and silently gives you the broken combination. The 13.3.3 pin must be hand-written into package.json; the digest never says this.  
  Source: node_modules/expo/bundledNativeModules.json (expo 57.0.22) + https://docs.expo.dev/develop/unit-testing/ + https://registry.npmjs.org/@testing-library/react-native (dist-tags latest 14.0.1), all opened
- **Claim:** Recommendation devDependency block: '"@react-native/jest-preset": "^0.86.3"' presented as safe to just install  
  **Correction:** The range is right but the naive install is not: npm `latest` for @react-native/jest-preset is 0.87.1 (for RN 0.87), which does NOT satisfy jest-expo 57.0.5's `^0.86.3`. The 0.86 line lives under dist-tag `0.86-stable` = 0.86.3. `npm i -D @react-native/jest-preset` gives you 0.87.1 and a peer conflict — write the `^0.86.3` range literally, or install `@react-native/jest-preset@0.86-stable`.  
  Source: https://registry.npmjs.org/@react-native/jest-preset (dist-tags: latest 0.87.1, '0.86-stable' 0.86.3) + jest-expo 57.0.5 peerDependencies, opened
- **Claim:** Uncertainty 7: 'The claim that .expo/types must exist before tsc for typed routes to resolve on a clean clone is recalled, not verified'  
  **Correction:** It is verifiable and correct — the digest under-sold its own advice by fetching the wrong page. https://docs.expo.dev/router/reference/typed-routes/ states types 'are automatically generated when the development server starts', that tsconfig's include must keep expo-env.d.ts and the hidden .expo directory ('These entries are required and should not be removed'), and gives the CI escape hatch verbatim: 'If you find yourself in a situation where you need to generate these types without initiating the development server... run the command `npx expo customize tsconfig.json` on the CI.'  
  Source: https://docs.expo.dev/router/reference/typed-routes/ (opened); the project's tsconfig.json already includes '.expo/types/**/*.ts' and 'expo-env.d.ts'
- **Claim:** Uncertainty 5: 'I did not open any source confirming expo-image-picker and expo-network are included in the SDK 57 Expo Go client, nor their SDK 57 version numbers'  
  **Correction:** Now verified, and the digest's Expo Go cost callout holds. The SDK 57 reference pages state 'Included in Expo Go' for both; versions are expo-image-picker 57.0.17 and expo-network 57.0.2 (npm dist-tag sdk-57, matching bundledNativeModules.json ~57.0.17 / ~57.0.2). Neither requires a plugins entry for Expo Go — image-picker's plugin is CNG-only permission-string customisation, and network's Android permissions 'are added automatically'.  
  Source: https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/, https://docs.expo.dev/versions/v57.0.0/sdk/network/, npm dist-tags, node_modules/expo/bundledNativeModules.json — all opened
- **Claim:** Uncertainty 3: 'The exact eslint-config-expo version that npx expo lint writes is resolved at install time... not hardcoded'  
  **Correction:** It is effectively pinned: node_modules/expo/bundledNativeModules.json (expo 57.0.22) contains `"eslint-config-expo": "~57.0.2"`, which is the table Expo's install resolver consults for un-versioned packages. The digest's guess (57.0.2) was right, but for the wrong reason — the source is bundledNativeModules.json, not npm latest.  
  Source: node_modules/expo/bundledNativeModules.json + https://unpkg.com/@expo/cli@57.0.24/build/src/lint/ESlintPrerequisite.js, both opened
- **Claim:** Fact 4: the docs' transformIgnorePatterns is 'the older single-line pattern' that the preset 'supersedes'  
  **Correction:** Half right, and the 'do NOT copy it' advice is over-broad. The preset's three-element array is exactly as quoted (confirmed byte-for-byte), and jest-preset.js even carries the comment `// Also please keep \`unit-testing.mdx\` file up to date`, so the two are meant to agree. But the docs page is not a single stale line — it prints package-manager-specific variants for npm/Yarn, pnpm and Bun, presented as a fix for transform errors rather than as required boilerplate. For this project (npm, per package-lock.json) the digest's 'preset only' advice is correct; stated as a blanket rule it is not.  
  Source: https://unpkg.com/jest-expo@57.0.5/jest-preset.js + https://docs.expo.dev/develop/unit-testing/, both opened

Expo Go compatibility of every package named:

| Package | In Expo Go | Config plugin | Dev build needed | Note |
|---|---|---|---|---|
| expo-router | yes | yes | no | SDK 57 page states 'Android, iOS, tvOS, Web, Included in Expo Go'; bundledNativeModules pins ~57.0.21. Docs: 'If you are using the default template... expo-router's config plugin is already configured in your app config' — and the project's app.json already has "expo-router" in plugins, so nothing to add. |
| expo-image-picker | yes | no | no | Docs say verbatim 'Android, iOS, Web, Included in Expo Go'. SDK 57 version is 57.0.17 (npm dist-tag sdk-57 = 57.0.17; bundledNativeModules ~57.0.17). Config plugin is OPTIONAL — only needed under CNG/prebuild to customise photosPermission/cameraPermission strings; Expo Go uses its own Info.plist. Not currently in package.json: run `npx expo install expo-image-picker`. |
| expo-network | yes | no | no | Docs say 'Included in Expo Go' (Android, iOS, tvOS, Web) and explicitly that ACCESS_NETWORK_STATE / ACCESS_WIFI_STATE 'are added automatically' — no plugins entry. SDK 57 version 57.0.2. Not in package.json today. |
| expo | yes | no | no | Installed at ~57.0.22; it is the Expo Go runtime itself. |
| jest-expo | yes | no | no | Dev-time only, no native code. bundledNativeModules.json pins jest-expo ~57.0.5, so `npx expo install jest-expo --dev` resolves 57.0.5 (npm latest is also 57.0.5). |
| jest | yes | no | no | Pure JS. Expo SDK 57 manifest pins jest ~29.7.0; npm latest is 30.5.1 — take the Expo pin. |
| @types/jest | yes | no | no | Types only. Expo SDK 57 manifest pins the exact version 29.5.14; npm latest is 30.0.0. |
| @react-native/jest-preset | yes | no | no | Pure JS. Genuinely required: jest-expo 57.0.5 declares it a non-optional peer ^0.86.3, react-native 0.86.3 does NOT depend on it (checked deps and node_modules/@react-native/ — absent). CAUTION: npm `latest` is 0.87.1, which does NOT satisfy ^0.86.3; the 0.86 line is dist-tag `0.86-stable` = 0.86.3, so pin the range explicitly. |
| babel-jest | yes | no | no | Transitive dependency of jest-expo (^29.2.1); do not list it yourself. |
| jest-environment-jsdom | yes | no | no | Transitive dependency of jest-expo (^29.2.1). Only used by jest-expo's web project; irrelevant for the iOS/Android-first suite. |
| @testing-library/react-native | yes | no | no | Pure JS. It is NOT in bundledNativeModules.json, so `npx expo install @testing-library/react-native --dev` (the command the Expo unit-testing docs give) resolves npm latest = 14.0.1, not 13.x. If you want 13.3.3 you must write it into package.json by hand. |
| react-test-renderer | yes | no | no | Pure JS. jest-expo 57.0.5 hard-depends on exactly 19.2.3; that version is published and not marked deprecated on npm. It satisfies RNTL 13.3.3's peer `react-test-renderer >=18.2.0`, so no extra install is needed on the 13.x path. |
| test-renderer | yes | no | no | Pure JS, latest 1.2.0 (peer react ^19.0.0). Only needed on the RNTL 14 path; the v14 release notes' table says React 19.2 → test-renderer@1.2, so RNTL's declared peer `^1.0.0` is looser than what actually works. |
| eslint | yes | no | no | Pure JS. ESlintPrerequisite.js requiredPackages contains `{ version: '^9.0.0', pkg: 'eslint', dev: true }`. npm latest is 10.10.0 — outside that range; ESLint 9 maintenance tag is 9.39.5. |
| eslint-config-expo | yes | no | no | Pure JS. ESlintPrerequisite.js lists it with NO version, but bundledNativeModules.json contains `"eslint-config-expo": "~57.0.2"` — that is what the resolver picks (npm latest is also 57.0.2). This resolves the digest's third uncertainty. |
| eslint-plugin-react-hooks | yes | no | no | Transitive via eslint-config-expo (^7.0.0, latest 7.1.1). Do not add it directly. |
| eslint-plugin-react-compiler | yes | no | no | DO NOT INSTALL. Expo docs: 'React Compiler lint rules are included by default with eslint-config-expo in SDK 55 and later... If you previously installed eslint-plugin-react-compiler, you can uninstall it.' Verified concretely: react-hooks v7 `recommended` includes react-hooks/immutability, purity, refs, set-state-in-effect, set-state-in-render, static-components, preserve-manual-memoization, use-memo, gating, globals, config, error-boundaries, unsupported-syntax, incompatible-library. |
| eslint-plugin-prettier | yes | no | no | Pure JS, latest 5.5.6. Peers: eslint >=8.0.0, prettier >=3.0.0, and eslint-config-prettier '>= 7.0.0 <10.0.0 \|\| >=10.1.0' — 10.1.8 satisfies it. |
| eslint-config-prettier | yes | no | no | Pure JS, latest 10.1.8 (peer eslint >=7.0.0). |
| prettier | yes | no | no | Pure JS, latest 3.9.6 — the digest's ^3.9.6 is current. |
| typescript | yes | no | no | Pure JS. Expo SDK 57 manifest pins ~6.0.3 (already in package.json); npm latest is 7.0.2, which the typescript-eslint 8.x peer range excludes. |
| @typescript-eslint/typescript-estree | yes | no | no | Transitive (via @typescript-eslint/parser ^8.59.0 in eslint-config-expo 57.0.2). Latest 8.70.0 declares peer `typescript: '>=4.8.4 <6.1.0'` — confirms the 'stay on TS 6.0.x' advice. |
| @types/react | yes | no | no | Types only. SDK 57 manifest pins ~19.2.4; package.json currently has ~19.2.2, so `npx expo install --check` will want a bump. |
| @expo/cli | yes | no | no | Pure JS, transitive via `expo`. latest = sdk-57 = 57.0.24, matching the version the digest cites. |

## Completeness critic

### Recommendations that would break Expo Go or a clean clone

- Attachment upload as written breaks at runtime in Expo Go SDK 57 (not a dev-build issue, a crash): `new FormData(); body.append('photo', { uri, name, type }); fetch(url, { method: 'POST', body })` hits expo/fetch, which is the global fetch unless EXPO_PUBLIC_USE_RN_FETCH is set (node_modules/expo/src/winter/runtime.native.ts) — and node_modules/expo/src/winter/fetch/convertFormData.ts documents '`uri` is not supported for React Native's FormData' and throws 'Unsupported FormDataPart implementation', with a shipped test asserting exactly that. There is no .env in the repo today, so the clean clone takes the throwing path.
- react-native-keyboard-controller: Expo's own SDK 57 sources contradict each other (SDK reference page front matter inExpoGo: true and bundledNativeModules pins 1.21.9, while docs.expo.dev/guides/keyboard-handling/ still states 'The Keyboard Controller library is not included in Expo Go. See Create a development build'). If the guide is the accurate one, adopting it forces a development build and breaks the clean-clone constraint outright. Do not ship it un-smoke-tested.
- Installing @react-navigation/native (or any @react-navigation/* package) to obtain useHeaderHeight for the pinned-footer KeyboardAvoidingView: confirmed this run that node_modules has no top-level @react-navigation and expo-router 57.0.21 vendors its own copy under build/react-navigation/. A second, separately-instantiated React Navigation would not share expo-router's contexts, giving null/throwing hooks at runtime in Expo Go.
- msw: pure JS so it installs, but it patches a global fetch that SDK 57 has already replaced with the native-module-backed expo/fetch, on top of two open unanswered RN issues (#2592, #2748) and a live 2.14+ regression returning empty bodies in RN. A first-run failure on the reviewer's `npx expo start`.
- json-server or any local HTTP server: needs a second terminal, Node >= 22.12, and three different base URLs (localhost for the iOS Simulator, 10.0.2.2 for the Android emulator, LAN IP for a device). Directly violates 'runs from a clean clone with npx expo start'.
- The testing digest's hand-written devDependency block can fail `npm install` before `npx expo start` ever runs: npm latest for @react-native/jest-preset is 0.87.1 which does not satisfy jest-expo 57.0.5's ^0.86.3 (0.86 line is behind dist-tag 0.86-stable), @testing-library/react-native must be hand-pinned to 13.3.3 because `npx expo install --dev` resolves 14.0.1, and jest must stay on 29 against jest-expo's ^29.2.1 environment packages. An ERESOLVE here means the app never starts.
- Template pruning that removes non-optional expo-router peers: expo-constants and expo-linking are required peers of expo-router 57.0.21 — dropping them because 'nothing imports them' breaks routing at startup. Related but lower severity: dropping react-dom + react-native-web while package.json keeps the `web` script and app.json keeps the `web` block (output: 'static' + favicon, verified in the local app.json) breaks `npx expo start --web` and `npx expo install --check`.
- Camera capture cannot be demonstrated on the stated primary target: expo-camera's SDK 57 availability line is 'Android (device only), iOS (device only)', and launchCameraAsync has no camera to open in the iOS Simulator. Any recommendation whose demo depends on a real capture needs a device or the Android emulator's virtual-scene camera, which itself is unverified as a default AVD setting.
- Config-plugin entries added for expo-image-picker (Dutch cameraPermission/photosPermission) have zero effect under `npx expo start` in Expo Go — Expo's permissions guide states build-time permission configuration 'is not required when testing projects in the Expo Go app', so Expo Go shows its own prompts. Harmless to include, but a reviewer told 'the prompts are in Dutch' will see otherwise.
- Typed routes + `tsc` on a clean clone: .expo/types and expo-env.d.ts are generated only when the dev server runs, so a reviewer who clones and runs a typecheck script first gets spurious Href errors; the documented escape hatch is `npx expo customize tsconfig.json` in CI. Breaks the verification story, not the app.

### Contradictions between topics

- expo-network vs @react-native-community/netinfo: the network-offline digest says 'use expo-network and nothing else' and rejects NetInfo on GDPR grounds (its generate_204 probe), while the data-fetching verification found TanStack's own React Native page lists NetInfo FIRST with expo-network only as the alternative, and the accessibility digest's checks list NetInfo (bundled at 12.0.1) as the package the offline banner implicitly depends on. Unreconciled — and the rejected probe is precisely the capability that makes onlineManager trustworthy, since expo-network's isInternetReachable mirrors isConnected on iOS.
- Offline semantics: data-fetching relies on TanStack Query's default networkMode pausing requests when offline; network-offline insists the flag is advisory/fail-open and the request is authoritative with a 15s AbortController timeout. Under the documented 'online' mode ('Queries and Mutations will not fire unless you have network connection') the mutationFn never runs, so the timeout never arms and the sending state cannot resolve. One of the two designs has to be abandoned explicitly.
- Upload progress: the image-picker digest's verification recommends expo-file-system File.createUploadTask for byte-accurate progress, but that bypasses both the fetch-based fetchWithTimeout error taxonomy (AbortError / TypeError / non-2xx) from network-offline AND the single-mutationFn shape from data-fetching. No digest states which one wins, and the fake-backend digest's in-app fake exercises none of them.
- Reality of the transport layer: fake-backend chooses a hand-rolled in-app fake API with no HTTP at all, which silently invalidates the concrete mechanisms other digests spend their recommendations on — multipart FormData encoding, AbortController timeouts, Query's paused/networkMode behaviour, and upload progress. Nobody says which of these are actually demonstrated versus merely simulated, which is exactly what a reviewer will ask.
- Custom-dev-build framing: the image-picker digest says Expo Go compatibility 'is the single biggest reason to pick [expo-image-picker] over expo-camera', and the accessibility digest says 'the camera/library picker ... is where your custom-dev-build risk actually sits'. Both are wrong per the SDK 57 pages — expo-camera and expo-image-picker are each 'Included in Expo Go'. The real discriminator is that camera capture is device-only, which also undercuts the image-picker digest's plan to 'demo the real capture path' on the Android emulator's virtual scene.
- Dependency pruning vs later needs: template-audit recommends dropping react-native-reanimated + react-native-worklets and react-native-gesture-handler, while the keyboard digest's optional upgrade (react-native-keyboard-controller 1.21.9) declares a reanimated >=3 peer, and expo-router pulls gesture-handler back in for any Drawer. Dropping them is only safe if the keyboard-controller upgrade path is formally closed.
- Header-height dependency: the keyboard digest's pinned-footer fallback needs useHeaderHeight from React Navigation, but there is no @react-navigation package anywhere in node_modules (confirmed this run — expo-router 57.0.21 vendors it under build/react-navigation/{core,native,elements,...} and does not re-export useHeaderHeight). The recommendation therefore either adds an unvetted dependency or imports a deep internal build path, contradicting its own zero-new-dependency baseline.

### Gaps and the follow-up query for each

1. **Attachment upload transport: RN-style FormData {uri,name,type} is REJECTED by SDK 57's default global fetch** — The image-picker digest's core upload snippet (`body.append('photo', { uri, name, type } as any); await fetch(url, {method:'POST', body})`) throws at runtime on SDK 57 and no digest connected the dots, even though the fake-backend digest separately noted that SDK 57 swaps the global fetch. Verified in the installed source (expo 57.0.22, whose package.json `main` is `src/Expo.ts`, so this TS is what Metro bundles): node_modules/expo/src/winter/runtime.native.ts does `if (!useRnFetch) { install('fetch', () => require('./fetch').fetch) }`, gated only on EXPO_PUBLIC_USE_RN_FETCH — and there is no .env in the repo, so expo/fetch IS global fetch. node_modules/expo/src/winter/fetch/convertFormData.ts carries the docblock "`uri` is not supported for React Native's FormData" and its entry loop ends in `throw new Error('Unsupported FormDataPart implementation')` for a plain {uri,...} object; expo's own test asserts it: `it('should throw an error if the react-native FormData passing an uri', ...) expect(convertFormDataAsync(formData)).rejects.toThrow(/Unsupported FormDataPart implementation/)`. Secondary finding from the same read: RequestUtils.normalizeBodyInitAsync buffers the ENTIRE multipart body into one Uint8Array in JS memory before the native request starts, so a multi-MB photo is fully materialized and there is no streaming and no progress. Nobody researched which of the three viable paths (set EXPO_PUBLIC_USE_RN_FETCH=1; read the asset into a Blob/File and append that; or use expo-file-system's File.upload/createUploadTask) the project should take, and each one changes the error taxonomy the network-offline digest designed.  
   Follow-up query: In Expo SDK 57 (expo 57.0.22, RN 0.86.3), what is the supported way to POST a local image file picked with expo-image-picker as multipart/form-data when expo/fetch is the global fetch? Open node_modules/expo/src/winter/fetch/{convertFormData,RequestUtils,fetch}.ts, https://docs.expo.dev/versions/v57.0.0/sdk/expo/ (expo/fetch + EXPO_PUBLIC_USE_RN_FETCH), and https://docs.expo.dev/versions/v57.0.0/sdk/filesystem/ (File, File.blob()/bytes(), File.upload, createUploadTask, UploadTask progress events), and state for each of the three options: does it work in Expo Go, does it support AbortSignal, does it report upload progress, and does it buffer the whole file in JS memory?
2. **Offline flag vs TanStack Query networkMode: the two digests specify opposite send-path semantics** — data-fetching wires onlineManager to expo-network and keeps the default networkMode 'online'; network-offline says the flag must be advisory/fail-open and 'the request itself is the real check' with a 15s AbortController timeout. These cannot both hold: https://raw.githubusercontent.com/TanStack/query/main/docs/framework/react/guides/network-mode.md (opened) states that in online mode "Queries and Mutations will not fire unless you have network connection" and the work is "`paused` until you have connection again". So with onlineManager reporting offline, the send mutation never invokes mutationFn — no fetch, no AbortError, no TypeError, no timeout, and `isPending` is true forever while the user stares at a sending screen. Worse on iOS, where expo-network's isInternetReachable mirrors isConnected (per the network digest's own Fact 4), a captive portal reads as online and Query fires; a false-negative link state locks the send silently. Nobody specified the precedence rule (paused-before-pending), whether to set networkMode:'always' on the send mutation, or what the offline banner is allowed to do to the button.  
   Follow-up query: For TanStack Query v5.102.8 in React Native: read https://tanstack.com/query/latest/docs/framework/react/guides/network-mode, .../guides/mutations, .../reference/useMutation and .../react-native, and answer: with onlineManager wired to expo-network, what exact state does a paused useMutation expose (isPending? isPaused? status?), does onMutate/onError fire, does resuming on reconnect re-run mutationFn, and is networkMode:'always' the documented way to let a user-initiated submit fail fast with its own AbortController timeout instead of hanging paused?
3. **Photo payload size on poor connections — nobody researched downscaling, and expo-image-manipulator was never evaluated** — The only size control any digest proposes is `quality: 0.6` on the picker, which bounds JPEG compression but not pixel dimensions — a 12MP iPhone capture stays ~4000x3000. Combined with the finding above (expo/fetch materializes the whole multipart body as a Uint8Array in JS memory, node_modules/expo/src/winter/fetch/RequestUtils.ts), that is the single worst behaviour for the stated 'poor connections' user. expo-image-manipulator is free here and no digest mentions it: https://docs.expo.dev/versions/v57.0.0/sdk/imagemanipulator/ (opened) reads "Android, iOS, tvOS, Web, Included in Expo Go" for SDK 57 and exposes `ImageManipulator.manipulate(source)` / `useImageManipulator` with `.resize(size)`, `.renderAsync()` and `.saveAsync({ compress, format: SaveFormat.JPEG })`, and node_modules/expo/bundledNativeModules.json pins "expo-image-manipulator": "~57.0.17". Also unresearched: whether `fileSize`/`mimeType` are populated reliably enough to show the user a size, and what the practical byte budget for a GP-portal attachment is.  
   Follow-up query: Open https://docs.expo.dev/versions/v57.0.0/sdk/imagemanipulator/ and the sdk-57 source of expo-image-manipulator, and determine the Expo Go-compatible pipeline to take an expo-image-picker asset and produce a <=1MB JPEG bounded to ~1600px on the long edge: which API (manipulate/useImageManipulator vs deprecated manipulateAsync), what resize() accepts (width only? aspect preserved?), what renderAsync/saveAsync return (uri, width, height — is there a size in bytes?), whether the intermediate ImageRef must be released, and whether picker `quality` should then be left at 1.0 to avoid double compression.
4. **Clean-clone install integrity: no digest checked that the union of recommended dependencies actually installs with npm before `npx expo start` runs** — Every digest reasons about Expo Go compatibility per package, but the hard constraint is `git clone && npm install && npx expo start`, and npm 7+ fails the install outright on an unsatisfiable peer graph. The testing digest's hand-written block is the concrete hazard: its own verification found npm `latest` for @react-native/jest-preset is 0.87.1 which does NOT satisfy jest-expo 57.0.5's `^0.86.3` (the 0.86 line is behind dist-tag `0.86-stable`), that @testing-library/react-native is absent from node_modules/expo/bundledNativeModules.json so the documented `npx expo install @testing-library/react-native --dev` resolves 14.0.1 rather than the required 13.3.3, and that jest must be held at the 29 line. Nobody ran `npm install` on the combined set, nobody checked `npx expo install --check` / `npx expo-doctor` against it, and the project currently has only 2 devDependencies (@types/react, typescript) — verified in package.json.  
   Follow-up query: Take the exact combined dependency set proposed across the digests (expo 57.0.22 template deps + expo-image-picker, expo-network, expo-image-manipulator, @tanstack/react-query 5.102.8, zod 4.6.2, plus devDeps jest ~29.7.0, jest-expo ~57.0.5, @react-native/jest-preset ^0.86.3, @testing-library/react-native 13.3.3, react-test-renderer 19.2.3, eslint ^9, eslint-config-expo ~57.0.2, prettier ^3.9.6, typescript ~6.0.3) and resolve the full peer graph from the npm registry: does `npm install` succeed without --legacy-peer-deps, which packages must be pinned by exact version or dist-tag rather than installed via `npx expo install`, and what does `npx expo install --check` report for each?
5. **Draft survival and in-flight send across backgrounding / process death — explicitly designed away, never researched** — data-fetching puts the draft in a Context on the flow's group layout so it 'naturally lives and dies with the flow'. For an elderly user on a flaky connection who backgrounds the app to read an SMS code or whose phone reclaims memory mid-upload, that means a retyped message. The only backgrounding case any digest covers is expo-image-picker's Android `getPendingResultAsync()`. Nobody researched: what happens to an in-flight expo/fetch request when iOS suspends the JS thread; whether AppState 'background'/'inactive' should pause or abort the two-call send; whether a persisted draft is warranted; or whether the picked photo's cache URI (an app-cache path) is still readable after a relaunch. @react-native-async-storage/async-storage is free here — https://docs.expo.dev/versions/v57.0.0/sdk/async-storage/ (opened) reads "Android, iOS, macOS, tvOS, Web, Included in Expo Go" and node_modules/expo/bundledNativeModules.json pins 2.2.0 — so the cost side of the decision is zero and only the design side is missing.  
   Follow-up query: For Expo SDK 57 / RN 0.86.3: what happens to an in-flight fetch (expo/fetch) and to JS timers when the app goes to background on iOS and on Android — read https://reactnative.dev/docs/0.86/appstate, https://docs.expo.dev/versions/v57.0.0/sdk/async-storage/ and https://docs.expo.dev/versions/v57.0.0/sdk/filesystem/ and answer: is a pending request cancelled or merely stalled, is an expo-image-picker asset URI in the cache directory guaranteed to survive an app relaunch (and should it be copied into Paths.document), and what is the minimal AsyncStorage-based draft-persistence pattern that restores recipient + answers + message + photo URI after process death?
6. **App language and screen-reader pronunciation for a Dutch UI — no digest covers locale at all** — Every digest writes Dutch strings but nothing configures language. Verified in app.json: there is no `locales` key, no `ios.infoPlist`, no expo-localization, and `expo-localization` is not in package.json (though https://docs.expo.dev/versions/v57.0.0/sdk/localization/ (opened) reads "Android, iOS, tvOS, Web, Included in Expo Go" for SDK 57, so it is free). Consequences nobody stated: on a default English iOS Simulator, VoiceOver will read Dutch labels with an English voice unless `accessibilityLanguage` is set — and the accessibility digest's own verification established that prop is iOS-only, with no TalkBack equivalent; and the image-picker digest's Dutch permission strings never appear in Expo Go at all (the permissions guide says build-time permission configuration 'is not required when testing projects in the Expo Go app'), so the reviewer sees Expo Go's own English/system prompts. That is a demo-visible accessibility defect for the exact audience the case study is about.  
   Follow-up query: For Expo SDK 57 + RN 0.86.3, how do you make a Dutch-only app read correctly to VoiceOver and TalkBack? Open https://reactnative.dev/docs/accessibility (accessibilityLanguage, its platform annotation and whether it inherits to children), https://docs.expo.dev/versions/v57.0.0/sdk/localization/, and https://docs.expo.dev/versions/v57.0.0/config/app/ for the `locales` / CFBundleDevelopmentRegion keys — and state which of these have any effect inside Expo Go versus only in a prebuild/EAS binary, plus whether Android/TalkBack has any per-view language mechanism at all.

## followup:1

*Uploading an expo-image-picker photo as multipart/form-data in Expo SDK 57, where expo/fetch is the global fetch (three transport options compared)*

### Recommendation

Do not ship the RN-style `formData.append('photo', {uri, name, type})` snippet — on this repo it throws `Unsupported FormDataPart implementation` before a byte leaves the device, because there is no `.env` in the repo root and expo/fetch is therefore the global fetch.

Take Option C for the attachment call: `new File(asset.uri).createUploadTask(url, { uploadType: UploadType.MULTIPART, fieldName: 'photo', headers: { Authorization: ... }, parameters: { econsultId }, onProgress, signal })` then `await task.uploadAsync()`. It is the only one of the three that gives all four properties this brief needs at once — Expo Go compatible, AbortSignal-cancellable, real `{bytesSent, totalBytes}` progress for the "sending" state on a poor connection, and native streaming from disk instead of a multi-MB `Uint8Array` in Hermes on an elderly patient's old iPhone. Keep the first call (create e-consult, JSON body) on plain global fetch; only the attachment call uses UploadTask.

Two taxonomy edits the network/offline layer must absorb: `uploadAsync()` resolves on 4xx/5xx instead of rejecting, so the retry/error mapping has to branch on `result.status` explicitly rather than reusing the `res.ok` check from the JSON call; and cancellation surfaces as `error.name === 'AbortError'` on a plain Error, not the `FetchError` expo/fetch throws — a single `isAbort(e)` helper checking `e?.name === 'AbortError'` covers both transports. Also note `sessionType` defaults to `'background'` on iOS, so the native transfer may outlive app suspension while the JS task does not survive a relaunch; if the "sending" screen must be resumable, pass `sessionType: 'foreground'` and treat a cold start as failed-and-retryable.

Option B (`formData.append('photo', new File(asset.uri))` + expo/fetch) is the officially documented one-line fix and is a legitimate fallback if you want one transport for both calls — but it still buffers the entire photo into JS memory via `await entry.bytes()` and gives you no progress, so it only makes sense if you first downscale the image (e.g. `ImagePicker`'s `quality`/`allowsEditing`, or expo-image-manipulator) to a few hundred KB. If you take it, note the multipart `filename=` comes from `File.name`, i.e. the cache-dir basename, not `asset.fileName`, so pass the original name as a separate text field if the backend cares.

Reject Option A (`EXPO_PUBLIC_USE_RN_FETCH=1`): it needs a committed `.env` to survive a clean clone, it silently downgrades *every* request in the app back to RN's XHR fetch (losing expo/fetch's streaming responses), it still gives no upload progress, and for a case study it reads as working around the platform rather than using it. Mention it in the README as the known escape hatch, don't build on it.

**Packages involved:** expo, expo-file-system, expo-image-picker

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (expo 57.0.22) — In the installed expo 57.0.22, node_modules/expo/src/winter/runtime.native.ts ends with `const useRnFetch = process.env.EXPO_PUBLIC_USE_RN_FETCH === '1' || ... === 'true'; if (!useRnFetch) { install('fetch', () => require('./fetch').fetch) }`, so expo/fetch is the global fetch by default.  
   Source: node_modules/expo/src/winter/runtime.native.ts (expo package.json main = src/Expo.ts, so Metro bundles this TS)
2. **verified** (SDK 57 docs (v57.0.0)) — The SDK 57 docs confirm the swap and the only opt-out: "On Android and iOS, `expo/fetch` is also installed as the global `fetch`" and to keep React Native's fetch you "set `EXPO_PUBLIC_USE_RN_FETCH=1` in your environment variables."  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/expo/
3. **verified** (expo 57.0.22) — convertFormData.ts accepts exactly three entry kinds — a string, an `instanceof Blob`, or an object where `'bytes' in entry` (it then calls `await entry.bytes()`) — and every other value, including a plain `{uri, name, type}`, hits `throw new Error('Unsupported FormDataPart implementation')`, under the docblock "`uri` is not supported for React Native's FormData."  
   Source: node_modules/expo/src/winter/fetch/convertFormData.ts lines 33-79
4. **verified** (expo 57.0.22) — Expo's own test pins this behaviour: `it('should throw an error if the react-native FormData passing an uri', ...)` appends `{uri:'file:/path/to/test.jpg', type:'image/jpeg', name:'test.jpg'}` and asserts `rejects.toThrow(/Unsupported FormDataPart implementation/)`.  
   Source: node_modules/expo/src/winter/fetch/__tests__/convertFormData-test.native.ts lines 79-89
5. **verified** (expo 57.0.22) — The `getParts()` escape hatch in convertFormDataAsync is dead code at runtime, because runtime.native.ts calls `installFormDataPatch(FormData)` which overwrites `FormData.prototype.append` (pushing the raw value into `_parts`) and installs an `entries()` generator that yields `[name, value]` verbatim — so the `{uri}` object reaches the unsupported-part throw.  
   Source: node_modules/expo/src/winter/FormData.ts (installFormDataPatch, normalizeArgs, entries) + runtime.native.ts
6. **verified** (expo 57.0.22) — expo/fetch buffers every body: `normalizeBodyInitAsync` turns a FormData into one `Uint8Array` via `convertFormDataAsync` and sets `Content-Type: multipart/form-data; boundary=…` before `request.start(url, init, requestBody)` is ever called, so a multi-MB photo is fully materialized in JS memory and there is no streaming.  
   Source: node_modules/expo/src/winter/fetch/RequestUtils.ts + fetch.ts
7. **verified** (expo 57.0.22) — expo/fetch does support AbortSignal (it throws `FetchError('The operation was aborted.')` if `signal.aborted` up front, and on 'abort' calls `response.abort(signal.reason)` then `request.cancel()`), but fetch.ts exposes no upload-progress hook of any kind.  
   Source: node_modules/expo/src/winter/fetch/fetch.ts
8. **verified** (expo-file-system SDK 57 docs) — Option B is the documented fix: the SDK 57 FileSystem docs show `const formData = new FormData(); formData.append('data', file); await fetch(url, {method:'POST', body: formData})` with `file` an expo-file-system `File` and `fetch` from `expo/fetch`.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/filesystem/
9. **verified** (expo-file-system 57.0.7) — Option B works because `File extends ExpoFileSystem.FileSystemFile implements Blob` and exposes `bytes(): Promise<Uint8Array>` plus `name` and `type` getters, which convertFormData uses for the part body, the `filename=` in content-disposition, and the part's content-type — but `await entry.bytes()` still loads the whole photo into JS memory.  
   Source: node_modules/expo-file-system/src/File.ts + convertFormData.ts getFormDataPartHeaders
10. **verified** (expo-file-system 57.0.7) — Option C is `file.upload(url, options)` / `file.createUploadTask(url, options).uploadAsync()`, whose `UploadOptions` are `{httpMethod:'POST'|'PUT'|'PATCH', uploadType: UploadType.MULTIPART(1)|BINARY_CONTENT(0), headers, fieldName (default 'file'), mimeType, parameters: Record<string,string>, onProgress({bytesSent,totalBytes}), signal: AbortSignal, sessionType}`, and the file is handed to native (`this._nativeTask.start(url, this._file, nativeOpts)`) rather than serialized in JS.  
   Source: node_modules/expo-file-system/src/File.ts lines 220-249 and src/NetworkTasks.ts + src/NetworkTasks.types.ts
11. **verified** (expo-file-system 57.0.7 / expo 57.0.22) — Option C changes the error taxonomy twice over: `uploadAsync()` "resolves with response metadata and body for completed HTTP responses, including non-2xx status codes" (so you must branch on `result.status` yourself), and an aborted upload rejects with a plain `Error` whose `name` is set to `'AbortError'`, not expo/fetch's `FetchError`.  
   Source: node_modules/expo-file-system/src/NetworkTasks.ts (uploadAsync docblock, createAbortError) vs expo/src/winter/fetch/FetchErrors
12. **verified** (expo-file-system 57.0.7) — Option C's progress has two documented caveats: "For multipart uploads, the reported bytes may include multipart framing overhead (boundary strings, headers, form parameters) in addition to the file content", and the JS layer emits a synthetic final 100% event because "Native progress events may not fire for small files".  
   Source: node_modules/expo-file-system/src/NetworkTasks.types.ts (onProgress) + NetworkTasks.ts (uploadAsync)
13. **verified** (SDK 57 docs) — Both required modules are Expo Go compatible — the SDK 57 FileSystem page is marked "Included in Expo Go" and the ImagePicker page header reads "Android, iOS, Web, Included in Expo Go" — so no development build is needed for any of the three options.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/filesystem/ and https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/
14. **verified** (whatwg-fetch 3.6.20 / react-native 0.86.3) — Option A (EXPO_PUBLIC_USE_RN_FETCH=1) restores whatwg-fetch 3.6.20 over RN's XHR — which does honour `signal` (rejecting with `DOMException('Aborted','AbortError')`) and does not buffer the file in JS, but exposes no upload progress even though RN's XMLHttpRequest underneath has `upload` and `__didUploadProgress` — and the flag is a Metro build-time inline from a **.env** file ("The Expo CLI will automatically load environment variables with an `EXPO_PUBLIC_` prefix from **.env** files"), so it must be committed for a clean clone and it downgrades fetch app-wide.  
   Source: node_modules/whatwg-fetch/dist/fetch.umd.js lines 536-632, react-native/Libraries/Network/{fetch.js,XMLHttpRequest.js}, https://docs.expo.dev/guides/environment-variables/

### Open uncertainties

- expo-image-picker is NOT currently installed in the repo (package.json lists neither it nor expo-file-system; expo-file-system 57.0.7 is present only as a transitive dep of expo), so both need `npx expo install` — and I did not verify this run whether an undeclared transitive expo-file-system import triggers an Expo CLI/Metro warning.
- I did not verify that expo-image-picker's `asset.uri` is always a `file://` path on Android; if it can be a `content://` URI, `new File(asset.uri)` may fail and both Option B and Option C would need a copy-to-cache step first — the v57 docs only say "URI to the local image or video file".
- `File.type` is a native getter I could not read from JS source, so whether the multipart content-type comes from the file extension or from content sniffing is unconfirmed; pass `mimeType: asset.mimeType` explicitly in UploadOptions to be safe.
- The exact wire format the native MULTIPART uploader produces (field ordering, where `parameters` are placed relative to the file part, whether a filename is sent) is not inspectable from the TypeScript layer — it must be checked against the fake backend once wired.
- No documented upper bound was found for the single `Uint8Array` expo/fetch builds in Option B, so the photo size at which Hermes actually fails is unknown and untested here.


## followup:2

*TanStack Query v5 paused-mutation semantics vs. an advisory offline flag for the e-consult send path (React Native / Expo SDK 57)*

### Recommendation

Resolve it in favour of the network-offline digest: the send mutation must set `networkMode: 'always'`, and the offline flag stays advisory. Concretely: (1) Keep `onlineManager` wired to expo-network exactly as the TanStack RN guide shows (it is the documented wiring, works in Expo Go, and gives queries the free pause/continue-on-reconnect for the recipients and questions fetches, which is genuinely what you want for loads). (2) On the send mutation only, set `networkMode: 'always'`. Verified mechanism: `canStart()` calls `canFetch(networkMode)` which short-circuits to `true` for anything other than 'online', so mutationFn runs immediately, your own AbortController fires at 15s, and `retry: 0` sends it straight to `status: 'error'` with a retry button you control. This is the only way to satisfy 'the request itself is the real check' — with the default 'online' mode plus a false-negative link state (or a captive portal on iOS, where isInternetReachable mirrors isConnected), the mutation sits at `status: 'pending'`, `isPaused: true` forever, mutationFn is never called, no onError fires, and the user stares at the sending screen with no exit. (3) Because you are using 'always', there is no paused state on the send path, so no precedence rule is needed there — but if any mutation ever keeps 'online', render `isPaused` BEFORE `isPending` ("waiting for connection", with a Cancel affordance), since `isPending` is true in both cases (`isPending: state.status === 'pending'`). (4) Do NOT rely on pause/resume as your offline queue for this flow: resuming calls `Mutation.continue()` and re-invokes mutationFn WITHOUT re-running onMutate, and resume is triggered by focusManager (AppState 'active') as well as reconnect — meaning a backgrounded, "paused" e-consult could silently send minutes later when the patient reopens the app, which is unacceptable for a GP message with an attachment. (5) The offline banner must be advisory only: announce it via `accessibilityLiveRegion`/AccessibilityInfo and never `disabled` the Send button — disabling it is the same false-negative trap in a different layer, and with expo-network's iOS semantics you cannot distinguish "no internet" from "captive portal" anyway. Keep two clearly separated error copies after the real attempt: timeout/AbortError → "Versturen duurde te lang — probeer opnieuw", TypeError/network failure → "Geen verbinding — je bericht is bewaard". (6) Note the two-call shape: create e-consult then upload attachment inside one mutationFn means a partial success (consult created, upload failed) must be surfaced as its own state, not as a generic send failure, because retrying the whole mutation would create a duplicate consult — carry the created consult id in state and retry only the upload.

**Packages involved:** @tanstack/react-query, expo-network

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (TanStack Query docs, main branch read 2026-09-11 (npm @tanstack/react-query latest = 5.102.8, published 2026-08-27)) — In the default 'online' network mode the docs state "Queries and Mutations will not fire unless you have network connection" and the work is "`paused` until you have connection again", so with onlineManager offline the send mutationFn is never invoked.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/docs/framework/react/guides/network-mode.md
2. **verified** (query-core main branch, read 2026-09-11) — A mutation that cannot start dispatches `{type:'pending', variables, isPaused}` whose reducer sets `status: 'pending'`, `isPaused: true`, `submittedAt: Date.now()`, `error: null` — i.e. the paused mutation is indistinguishable from a real in-flight one by status alone.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/packages/query-core/src/mutation.ts (execute(), #dispatch reducer)
3. **verified** (query-core main branch, read 2026-09-11) — MutationObserver derives the result flags as `isPending: state.status === 'pending'`, so a paused send reports isPending === true AND isPaused === true simultaneously; only `isPaused` distinguishes 'queued, nothing sent' from 'request in flight'.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/packages/query-core/src/mutationObserver.ts (line ~231)
4. **verified** (query-core main branch, read 2026-09-11) — `onMutate` DOES run while offline: execute() dispatches the pending action, awaits the cache's onMutate and then `this.options.onMutate?.(...)`, and only afterwards calls `await retryer.start()` — so optimistic/local side effects fire even though no request goes out.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/packages/query-core/src/mutation.ts (execute())
5. **verified** (query-core main branch, read 2026-09-11) — `onSuccess`/`onError`/`onSettled` sit after `await retryer.start()` in execute(), so while paused none of them fire and the `mutateAsync` promise never settles — no AbortError, no TypeError, no timeout, no error UI.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/packages/query-core/src/mutation.ts (execute() try/catch)
6. **verified** (query-core main branch, read 2026-09-11) — The gate is `canStart = () => canFetch(config.networkMode) && config.canRun()` with `canFetch = (networkMode ?? 'online') === 'online' ? onlineManager.isOnline() : true`, so `networkMode: 'always'` makes canStart ignore onlineManager entirely and run mutationFn immediately.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/packages/query-core/src/retryer.ts (lines 53-57, 133)
7. **verified** (docs main branch, read 2026-09-11) — The docs describe 'always' as: TanStack Query "will always fetch and ignore the online / offline state", "Queries will never be `paused` because you have no network connection" and "Retries will also not pause" — the documented escape hatch for work that must fail fast on its own.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/docs/framework/react/guides/network-mode.md
8. **verified** (query-core main branch, read 2026-09-11) — Resuming re-runs the request but NOT onMutate: QueryClient.mount subscribes to both onlineManager and focusManager and calls `resumePausedMutations()` on online/focus, which calls `Mutation.continue()` → the existing retryer resumes and invokes mutationFn, while execute()'s `restored`/continue path skips the onMutate step.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/packages/query-core/src/queryClient.ts (mount, resumePausedMutations) + mutation.ts (continue/execute)
9. **verified** (query-core main branch, read 2026-09-11) — `MutationFunctionContext` is `{ client, meta, mutationKey? }` — there is no AbortSignal passed to mutationFn (unlike queryFn), so any 15s send timeout must be your own AbortController created inside mutationFn; TanStack will never cancel the in-flight send for you.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/packages/query-core/src/types.ts (MutationFunctionContext, MutationFunction)
10. **verified** (query-core main branch, read 2026-09-11) — Mutation retry defaults to 0 (`retry: this.options.retry ?? 0` when the retryer is created), so with networkMode 'always' a failed send goes straight to `status: 'error'` with no hidden retry storm and no duplicate e-consults.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/packages/query-core/src/mutation.ts (createRetryer call in execute())
11. **verified** (query-core / docs main branch, read 2026-09-11) — networkMode is a per-mutation option forwarded to the retryer (`networkMode: this.options.networkMode`), so the send mutation can opt into 'always' while queries keep the global 'online' default and its pause/resume-on-reconnect behaviour.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/packages/query-core/src/mutation.ts + docs signature `networkMode: 'online' | 'always' | 'offlineFirst'` defaulting to 'online'
12. **verified** (docs main branch, read 2026-09-11) — The official React Native guide now ships an expo-network onlineManager snippet (Network.addNetworkStateListener + a getNetworkStateAsync fallback, with a `.catch()` because "getNetworkStateAsync can reject on some platforms/SDK versions") and it calls `setOnline(!!state.isConnected)` — link state, not internet reachability.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/docs/framework/react/react-native.md
13. **verified** (query-core main branch, read 2026-09-11) — OnlineManager initialises `#online = true` and the class doc says it "assumes an active network connection" by default, so between app start and the first expo-network callback Query is fail-open and a send fires normally.  
   Source: https://raw.githubusercontent.com/TanStack/query/main/packages/query-core/src/onlineManager.ts (lines 6-16)
14. **verified** (Expo SDK 57 (v57.0.0 docs), read 2026-09-11) — expo-network for SDK 57 documents that "On iOS, this value [isInternetReachable] will always be the same as isConnected", and the module is included in Expo Go for Android/iOS/tvOS/Web — so on iOS a captive portal reads as online and neither flag can be trusted as a real reachability check.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/network/

### Open uncertainties

- I read the query-core/docs sources from the GitHub main branch on 2026-09-11, not from the exact v5.102.8 tag; npm confirms 5.102.8 is `latest` (published 2026-08-27) but a behavioural drift between the tag and main, while unlikely, was not verified.
- I did not find `isPaused` documented in the public useMutation reference page (docs/framework/react/reference/functions/usemutation.md returned no matches for isPaused/isPending/networkMode); the state shape is grounded in query-core source and the network-mode guide instead.
- Whether the API you are calling returns a JSON body on a 4xx/5xx, and therefore whether a fetch-based mutationFn should reject on !response.ok, is unverified — nothing in the case study specifies it.
- The exact wall-clock behaviour of AbortController + fetch on React Native 0.86 (Hermes/whatwg-fetch polyfill) was not tested this run; the 15s timeout claim rests on standard AbortController semantics, not on an RN 0.86 source I opened.
- Whether `focusManager` is actually wired to AppState in this project changes fact 8's practical impact: if you never call focusManager.setFocused, only reconnect resumes paused mutations.


## followup:3

*Expo Go-compatible photo downscaling for the e-consult attachment: expo-image-manipulator (SDK 57) pipeline to produce a ≤1 MB, ~1600px JPEG from an expo-image-picker asset*

### Recommendation

Add `expo-image-manipulator` (`npx expo install expo-image-manipulator` → ~57.0.17) — it is Included in Expo Go on SDK 57, so it costs nothing against the "clean clone + npx expo start" constraint, and it is the only way to bound *pixel dimensions*. Picker `quality` alone leaves a 4000x3000 capture at 4000x3000.

Pipeline (imperative, because this runs once on confirm rather than on render):

1. Pick with `quality: 1` (or omit and accept the default) and `exif: false`. Rationale: the manipulator re-encodes anyway, so picker compression is a wasted lossy generation, and the SDK 57 docs explicitly warn the picker's quality pass "may be bigger than the size of the original image" for already-compressed input and is ignored on iOS for .png/.bmp from the library. Do NOT stack `quality: 0.6` with `compress: 0.7` — that is double compression for no size benefit.
2. Compute the resize argument from the asset's own `width`/`height` so you bound the *long* edge, not always the width: if `width >= height` pass `{ width: Math.min(1600, asset.width) }`, else `{ height: Math.min(1600, asset.height) }`. Passing one dimension preserves aspect ratio (documented). Guard on `asset.width === 0` (documented as possible) by falling back to `{ width: 1600 }`. Skip the manipulation entirely when the image is already under both the pixel and byte budget.
3. `const ctx = ImageManipulator.manipulate(asset.uri); ctx.resize(size); const img = await ctx.renderAsync(); const out = await img.saveAsync({ compress: 0.7, format: SaveFormat.JPEG });` — then `img.release(); ctx.release();` in a `finally`. Releasing is not strictly required (GC handles it) but SharedObject's own JSDoc names "image bitmap" as the case where manual release is warranted, and the library's deprecated `manipulateAsync` does exactly this pair of release calls. Never set `base64: true` — that would materialize the image as a JS string on top of the Uint8Array the upload already builds.
4. Measure the result with `new File(out.uri).size` from expo-file-system (already installed, Expo Go-compatible; ImageResult has no size field). Use this to (a) show the patient "Foto: 0,4 MB" before sending, and (b) optionally re-save once at `compress: 0.5` if the first pass exceeds the budget. One retry, not a loop — a bisect costs multiple full decodes.
5. Upload `out.uri` (a cache-directory file). Because expo/fetch normalizes any FormData body into a single Uint8Array in JS memory, the ~300–600 KB this pipeline produces is the difference between a survivable and a hostile upload on a poor connection; it also cuts retry cost, which matters more than first-attempt latency for the offline/flaky states in the brief.

Use `useImageManipulator` only if you want a live preview transform tied to a source in render — it auto-releases via `useReleasingSharedObject`. For a one-shot on-submit resize, the imperative `manipulate()` + explicit release is the cleaner fit.

For the case study writeup, the honest framing is: this is one extra Expo Go-compatible dependency that turns an unbounded multi-megabyte upload into a predictable sub-megabyte one, and it is the single highest-leverage change for the "poor connections" user.

**Packages involved:** expo-image-manipulator, expo-image-picker, expo-file-system, expo

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (Expo SDK 57 (v57.0.0 docs)) — expo-image-manipulator's SDK 57 docs page lists platform support as "Android, iOS, tvOS, Web, Included in Expo Go", so adding it costs no custom development build and `npx expo start` from a clean clone still works.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/imagemanipulator/
2. **verified** (expo 57.0.22) — The project's own expo copy pins the compatible version at "expo-image-manipulator": "~57.0.17" and the package is not currently installed, so it must be added with `npx expo install expo-image-manipulator`.  
   Source: node_modules/expo/bundledNativeModules.json (lines 53-54) and `ls node_modules/expo-image-manipulator` returning NOT INSTALLED
3. **verified** (expo/expo branch sdk-57) — `manipulateAsync(uri, actions, saveOptions)` carries an explicit deprecation notice — "It has been replaced by the new, contextual and object-oriented API. Use `ImageManipulator.manipulate` or `useImageManipulator` instead." — so new code should use `manipulate()`/`useImageManipulator()`.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-manipulator/src/ImageManipulator.ts
4. **verified** (expo/expo branch sdk-57) — `resize()` is typed `resize(size: { width?: number | null; height?: number | null }): ImageManipulatorContext` and the docs state "If you specify only one value, the other will be calculated automatically to preserve image ratio", so passing width alone is enough to bound the long edge of a landscape photo.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-manipulator/src/ImageManipulatorContext.ts and https://docs.expo.dev/versions/v57.0.0/sdk/imagemanipulator/
5. **verified** (expo/expo branch sdk-57) — `renderAsync(): Promise<ImageRef>` is documented as "Awaits for all manipulation tasks to finish and resolves with a reference to the resulted native image", and `ImageRef extends SharedRef<'image'>` exposing only `width` and `height` (plus inherited `nativeRefType`) — no bytes.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-manipulator/src/ImageRef.ts
6. **verified** (expo/expo branch sdk-57) — `saveAsync(options?: SaveOptions): Promise<ImageResult>` takes `{ compress?: 0.0–1.0, format?: SaveFormat, base64?: boolean }` and returns `{ uri, width, height, base64? }` — ImageResult has NO byte-size field, so the app cannot learn the output size from the manipulator itself.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-manipulator/src/ImageManipulator.types.ts and https://docs.expo.dev/versions/v57.0.0/sdk/imagemanipulator/
7. **verified** (expo/expo branch sdk-57) — `ImageRef.saveAsync` is documented as "Saves the image to the file system in the cache directory", so the downscaled copy lands in cache and is subject to OS eviction — upload it in the same session rather than persisting the URI across restarts.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-manipulator/src/ImageRef.ts
8. **verified** (Expo SDK 57) — `SaveFormat` is an enum of JPEG/PNG/WEBP with `format` defaulting to JPEG, and `SaveFormat.JPEG = "jpeg"`; JPEG is the right choice for a photo attachment since `compress` only applies meaningfully to lossy formats.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/imagemanipulator/ and .../ImageManipulator.types.ts (sdk-57)
9. **verified** (expo/expo branch sdk-57) — `ImageManipulatorContext extends SharedObject` and `ImageRef extends SharedRef`, and the library's own deprecated `manipulateAsync` implementation calls `context.release()` and `image.release()` after saving — confirming both handles are explicitly releasable.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-manipulator/src/ImageManipulator.ts (body of manipulateAsync) and ImageManipulatorContext.ts
10. **verified** (expo/expo branch sdk-57) — `SharedObject.release()` JSDoc says "In most cases, you should never need to use this function, except some specific performance-critical cases when manual memory management makes sense and the native object is known to exclusively retain some native memory (such as binary data or image bitmap)" — a 12MP decoded bitmap is exactly that case, so releasing is advisable but not mandatory (GC handles it otherwise).  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-modules-core/src/ts-declarations/SharedObject.ts
11. **verified** (expo/expo branch sdk-57) — `useImageManipulator(source)` is implemented as `useReleasingSharedObject(() => ExpoImageManipulator.manipulate(source), [source])`, so the hook auto-releases the context when the source changes or the component unmounts — the imperative `ImageManipulator.manipulate()` path does not, and must release manually.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-manipulator/src/ImageManipulator.ts
12. **verified** (expo-file-system 57.0.7 (installed)) — There is no byte size in ImageResult, but `expo-file-system`'s File class exposes `size: number` — "A size of the file in bytes. 0 if the file does not exist, or it cannot be read." — and expo-file-system 57.0.7 is already installed transitively and is "Included in Expo Go", so `new File(result.uri).size` is a zero-cost way to verify the ≤1 MB budget and show the user a size.  
   Source: node_modules/expo-file-system/src/internal/NativeFileSystem.types.ts line 296-298 and https://docs.expo.dev/versions/v57.0.0/sdk/filesystem/
13. **verified** (Expo SDK 57) — The picker's `quality` doc carries the warning "If the selected image has been compressed before, the size of the output file may be bigger than the size of the original image", and on iOS the option is ignored for .bmp or .png files selected from the library — so picker `quality` is an unreliable size control and can even inflate the file.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/
14. **verified** (expo/expo branch sdk-57) — `ImagePickerAsset.fileSize` is documented as "File size of the picked image or video, in bytes" with no `@platform` tag (unlike `exif`, which is tagged android/ios), while `fileName` and `mimeType` are documented as possibly `null` "when the name is unavailable or user gave limited permission" and `width`/`height` "can be `0` if the system did not provide" them — so treat fileSize/mimeType as best-effort and fall back to File.size and a hardcoded image/jpeg.  
   Source: https://raw.githubusercontent.com/expo/expo/sdk-57/packages/expo-image-picker/src/ImagePicker.types.ts and https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/

### Open uncertainties

- I could not find the default value of expo-image-picker's `quality` in the SDK 57 JS source — ImagePicker.ts has no DEFAULT_OPTIONS and only validates options, so the default lives in native code. My advice to pass `quality: 1` explicitly is therefore a deliberate way to avoid depending on an unverified default, not a claim that the default is lossy.
- I did not verify empirically that 1600px long edge + compress 0.7 lands under 1 MB; that is an estimate from JPEG behaviour, not something I read in a source this run. This is exactly why step 4 measures File.size rather than assuming.
- The 1 MB / 1600px budget itself is not grounded in any source I opened — there is no Dutch GP-portal or NEN/MedMij attachment-size spec I verified. Treat it as a defensible product choice to state explicitly in the writeup, not a requirement.
- Whether `fileSize` is actually populated in practice on Android (especially for content:// URIs picked from the file system, where the docs note assetId is unavailable) is untested — the type carries no @platform tag, but absence of a tag is not proof of runtime reliability. The File.size fallback makes this moot.
- I did not verify peak native memory during the decode step: manipulating a 12MP source still decodes the full bitmap natively before resizing, so a low-end Android device could still be pressured at that moment even though the *output* is small. Whether release() measurably helps here is unverified.
- Unverified whether resize() upscales when the requested dimension exceeds the source; the guard `Math.min(1600, asset.width)` in my recommendation sidesteps this rather than relying on library behaviour.
- expo-image-manipulator's tvOS/Web support is listed but I did not check whether resize/saveAsync behave identically on react-native-web, which this project has as a dependency.


## followup:4

*Clean-clone install integrity for the combined Expo SDK 57 e-consult dependency set: does `git clone && npm install && npx expo start` survive the union of all recommended packages under npm 11 without --legacy-peer-deps*

### Recommendation

VERDICT: the combined set DOES install cleanly under npm 11 with no --legacy-peer-deps — but only if the devDependency block is hand-written. Every documented `npx expo install` path for the test stack produces a different, broken tree. Split the install into two rules.

RULE 1 — use `npx expo install` for the five Expo-versioned packages ONLY. `npx expo install expo-image-picker expo-image-manipulator expo-network` and `npx expo install jest-expo eslint-config-expo --dev`. These are the only packages in Expo's SDK 57 endpoint, so these are the only ones `npx expo install --check` / `expo-doctor` can validate. Expected pins: expo-image-picker ~57.0.17, expo-image-manipulator ~57.0.17, expo-network ~57.0.2, jest-expo ~57.0.5, eslint-config-expo ~57.0.2.

RULE 2 — hand-write everything else into package.json, never `npx expo install` it. The three that MUST be pinned by hand, with the exact failure each prevents:
  - "jest": "~29.7.0" — jest-expo 57.0.5 declares no `jest` peer, so npm will NOT error; Expo's own documented `npx expo install jest @types/jest --dev` fetches jest 30.5.1 / @types/jest 30.0.0 against jest-expo's jest-29 internals. Silent split runtime. If you want @types/jest, pin "^29.5.14", not latest.
  - "@testing-library/react-native": "13.3.3" — exact, no caret. `npx expo install ... --dev` resolves npm latest 14.0.1, where `render()` and `fireEvent()` became async and react-test-renderer was swapped for the separate `test-renderer` package. That change invalidates every test you write without failing the install.
  - "@react-native/jest-preset": "^0.86.3" — safe as written (caret on 0.x is <0.87.0, resolves to 0.86.3); the landmine is only a bare `npm i -D`, which grabs `latest` 0.87.1 and violates jest-expo's peer. If you want belt-and-braces, write the exact "0.86.3".
  - "react-test-renderer": "19.2.3" is optional — jest-expo already ships it as a direct dep — but keep it explicit so RTL 13.3.3's peer is satisfied at top level.
  - @tanstack/react-query 5.102.8, zod 4.6.2, prettier ^3.9.6, eslint ^9, typescript ~6.0.3 are unconstrained and fine as plain `npm i -D`.

GOOD NEWS worth not re-litigating: TypeScript 6.0.3 clears the lint chain. eslint-config-expo 57.0.2 pulls @typescript-eslint/parser 8.70.0 whose peer is `typescript >=4.8.4 <6.1.0` — satisfied. That was the most plausible remaining ERESOLVE and it does not fire.

CI GATE for the take-home: add a clean-clone job that runs `rm -rf node_modules package-lock.json && npm install --strict-peer-deps` (defaults are legacy-peer-deps=false, strict-peer-deps=false; forcing strict turns silent nearest-non-peer resolution into a hard failure), then `npx expo install --check`, then `npx expo-doctor`. Call out in the README that `--check` covers only the five Expo-versioned packages, so the jest/RTL pins are enforced by the lockfile and exact versions, not by Expo tooling.

HONESTY CAVEAT to carry forward: this is a manually resolved peer graph, not an executed install. I am read-only this run and could not write a package.json to run `npm install --dry-run` against the union. Run the clean-clone job once before submitting.

**Packages involved:** expo, expo-constants, expo-device, expo-font, expo-image, expo-linking, expo-router, expo-splash-screen, expo-status-bar, expo-system-ui, react, react-dom, react-native, react-native-gesture-handler, react-native-reanimated, react-native-safe-area-context, react-native-screens, react-native-web, react-native-worklets, expo-image-picker, expo-image-manipulator, expo-network, @tanstack/react-query, zod, jest, jest-expo, @react-native/jest-preset, @testing-library/react-native, react-test-renderer, @types/jest, @types/react, eslint, eslint-config-expo, prettier, typescript, test-renderer, @typescript-eslint/parser, @typescript-eslint/eslint-plugin, eslint-plugin-react-hooks, babel-jest, jest-environment-jsdom, @jest/globals, react-server-dom-webpack

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (npm CLI 11.19.1, registry read 2026-09-11) — The combined set installs cleanly with no --legacy-peer-deps: every peer edge I resolved is satisfied (jest-expo 57.0.5 -> @react-native/jest-preset ^0.86.3, react-native *, expo *, react-server-dom-webpack optional; @react-native/jest-preset 0.86.3 -> react ^19.2.3; RTL 13.3.3 -> react >=18.2.0, react-native >=0.71, react-test-renderer >=18.2.0, jest optional; react-test-renderer 19.2.3 -> react ^19.2.3; @tanstack/react-query 5.102.8 -> react ^18 || ^19; eslint-config-expo 57.0.2 -> eslint >=8.10).  
   Source: npm registry via `npm view <pkg> peerDependencies --json` for jest-expo@57.0.5, @react-native/jest-preset@0.86.3, @testing-library/react-native@13.3.3, react-test-renderer@19.2.3, @tanstack/react-query@5.102.8, eslint-config-expo@57.0.2, zod@4.6.2, prettier@3.9.6; npm 11.19.1 / Node v26.8.2
2. **verified** (@react-native/jest-preset 0.86.3) — The critic's ERESOLVE fear for @react-native/jest-preset does NOT materialize from a hand-written manifest, because caret on a 0.x version is range-restricted: `^0.86.3` means >=0.86.3 <0.87.0, and `npm view '@react-native/jest-preset@^0.86.3' version` resolves to exactly 0.86.3.  
   Source: `npm view '@react-native/jest-preset@^0.86.3' version --json` -> "0.86.3"
3. **verified** (dist-tags read 2026-09-11) — The hazard is real but lives in the ADD COMMAND, not the manifest: @react-native/jest-preset dist-tags are {"0.86-stable":"0.86.3","latest":"0.87.1","next":"0.88.0-rc.0"}, so a bare `npm i -D @react-native/jest-preset` writes ^0.87.1 and breaks jest-expo 57.0.5's ^0.86.3 peer.  
   Source: `npm view @react-native/jest-preset dist-tags --json`
4. **verified** (Expo SDK 57.0.0 endpoint, expo 57.0.22 bundledNativeModules.json) — @react-native/jest-preset is absent from Expo's SDK 57 version endpoint and from the installed bundledNativeModules.json, so `npx expo install @react-native/jest-preset` offers no protection — it must be hand-written as `^0.86.3` (or the `0.86-stable` tag) in devDependencies.  
   Source: `curl https://api.expo.dev/v2/sdks/57.0.0/native-modules` (no match) and grep of node_modules/expo/bundledNativeModules.json (no match)
5. **verified** (@testing-library/react-native 14.0.1 vs 13.3.3) — Confirmed: @testing-library/react-native is in neither bundledNativeModules.json nor the SDK 57 endpoint, and its npm `latest` is 14.0.1 — so Expo's own documented `npx expo install @testing-library/react-native --dev` silently installs 14.0.1, not 13.3.3.  
   Source: `npm view @testing-library/react-native dist-tags --json` -> {"next":"14.0.0-rc.3","latest":"14.0.1"}; bundledNativeModules.json grep; docs.expo.dev/develop/unit-testing/
6. **verified** (@testing-library/react-native 14.0.0/14.0.1) — RTL 14 is a hard API break that would silently invalidate every written test, which is the real reason to pin 13.3.3: v14 release notes state `render()` now returns `Promise<RenderResult>`, `fireEvent()` returns `Promise<void>`, React 18 support dropped, minimum RN raised to 0.78, and React Test Renderer replaced by the separate `test-renderer` package.  
   Source: https://github.com/callstack/react-native-testing-library/releases (v14.0.0 notes)
7. **verified** (RTL 14.0.1, test-renderer 1.2.0) — RTL 14.0.1 declares a NON-optional peer on a different package, `test-renderer: ^1.0.0` (latest 1.2.0, peer react ^19.0.0), whereas 13.3.3 peers on `react-test-renderer >=18.2.0` — so the two versions need structurally different devDependency blocks.  
   Source: `npm view @testing-library/react-native@14.0.1 peerDependencies peerDependenciesMeta --json` and `npm view test-renderer@1.2.0 peerDependencies --json`
8. **verified** (jest-expo 57.0.5) — The jest pin is the most dangerous item because npm will NOT error: jest-expo 57.0.5 declares NO `jest` peer dependency at all, yet pins its internals to the 29 line (babel-jest ^29.2.1, @jest/globals ^29.2.1, jest-snapshot ^29.2.1, jest-environment-jsdom ^29.2.1, @jest/create-cache-key-function ^29.2.1).  
   Source: `npm view jest-expo@57.0.5 peerDependencies peerDependenciesMeta dependencies --json`
9. **verified** (Expo SDK 57 docs, jest 30.5.1) — Expo's own documented command is the trap: docs.expo.dev/develop/unit-testing/ says `npx expo install jest-expo jest @types/jest --dev`, but jest is not in the SDK 57 endpoint and npm `latest` for jest is 30.5.1 (and @types/jest is 30.0.0), producing a split jest-30-runner / jest-29-internals tree with no install error.  
   Source: docs.expo.dev/develop/unit-testing/; `npm view jest dist-tags --json` -> latest 30.5.1; `npm view @types/jest version` -> 30.0.0
10. **verified** (@typescript-eslint/parser 8.70.0, eslint 9.39.5, eslint-config-expo 57.0.2) — TypeScript ~6.0.3 is safe against the lint chain — a plausible landmine that does not fire: eslint-config-expo 57.0.2 pulls @typescript-eslint/parser ^8.59.0 (resolving to 8.70.0) whose peer is `typescript: ">=4.8.4 <6.1.0"` and `eslint: ^8.57.0 || ^9.0.0 || ^10.0.0`, both satisfied by TS 6.0.3 and eslint ^9 (9.39.5).  
   Source: `npm view '@typescript-eslint/parser@^8.59.0' peerDependencies --json`, `npm view eslint-config-expo@57.0.2 peerDependencies dependencies --json`, `npm view 'eslint@^9' version`
11. **verified** (expo 57.0.22 bundledNativeModules.json) — The three runtime Expo modules ARE Expo-versioned and must be added with `npx expo install`, which pins them to expo 57.0.22's bundled ranges: expo-image-picker ~57.0.17, expo-image-manipulator ~57.0.17, expo-network ~57.0.2 — all three declare only trivial peers (expo "*", and react "*" for expo-network).  
   Source: node_modules/expo/bundledNativeModules.json; `npm view expo-image-picker@57.0.17 peerDependencies --json` etc.
12. **verified** (Expo SDK 57.0.0 version endpoint / Expo CLI docs) — `npx expo install --check` will only validate the five Expo-versioned packages in the set (expo-image-picker, expo-image-manipulator, expo-network, jest-expo ~57.0.5, eslint-config-expo ~57.0.2) and will report nothing at all about jest, @react-native/jest-preset, @testing-library/react-native, react-test-renderer, @tanstack/react-query, or zod — so a green `--check` does not mean the test stack is correct.  
   Source: https://api.expo.dev/v2/sdks/57.0.0/native-modules (grep showed only those packages present); docs.expo.dev/more/expo-cli/ ("Check which installed packages need to be updated", "a best-effort tool ... using a list of popular packages")
13. **verified** (npm CLI v11 docs / local npm 11.19.1) — npm's own config defaults make the conflict behaviour asymmetric and worth knowing: legacy-peer-deps defaults to false, strict-peer-deps defaults to false, and npm v7+ resolves conflicting peers *deep in the graph* via the nearest non-peer spec with only a warning — direct top-level peer conflicts are what actually produce a hard ERESOLVE.  
   Source: https://docs.npmjs.com/cli/v11/using-npm/config; confirmed locally with `npm config get legacy-peer-deps` -> false and `npm config get strict-peer-deps` -> false
14. **verified** (jest-expo 57.0.5 / Expo SDK 57 docs) — Listing react-test-renderer 19.2.3 in devDependencies is redundant-but-harmless: jest-expo 57.0.5 already has `react-test-renderer: 19.2.3` as a direct (non-peer) dependency, even though Expo's docs simultaneously claim react-test-renderer "does not support React 19 and above" — an unresolved contradiction in Expo's own SDK 57 material.  
   Source: `npm view jest-expo@57.0.5 dependencies --json`; docs.expo.dev/develop/unit-testing/

### Open uncertainties

- I could not execute `npm install` or `npm install --dry-run` on the combined set — this run is read-only and I am barred from creating even a scratch package.json, so the 'install succeeds' verdict is a manually resolved peer graph (every first-order peer edge walked via `npm view`), not an observed resolution. Deep transitive conflicts below the first peer layer were not exhaustively enumerated.
- `npx expo install --check` and `npx expo-doctor` were never actually run against the combined set; my claim about what they report is inferred from which packages appear in https://api.expo.dev/v2/sdks/57.0.0/native-modules plus the Expo CLI docs' description, not from observed CLI output.
- Whether the jest-expo 57.0.5 preset actually runs green with @testing-library/react-native 13.3.3 under React 19.2.3 is untested. Expo's SDK 57 docs assert react-test-renderer 'does not support React 19 and above', yet jest-expo 57.0.5 ships react-test-renderer 19.2.3 as a direct dependency and RTL 13.3.3 peers on it. If RTL 13 turns out to be genuinely broken on React 19, the only supported path is RTL 14.0.1 with async `render()`, which changes how every test is written — decide this by running one smoke test before committing to the 13.3.3 pin.
- Expo Go compatibility of expo-image-picker / expo-image-manipulator / expo-network was not re-verified this run (it was other digests' scope); I only confirmed their version ranges and that their peers are trivial.
- npm's documented conflict behaviour distinguishes deep-graph peers (resolved with a warning) from direct top-level peers (hard ERESOLVE); I did not find docs stating the precise boundary, so `--strict-peer-deps` in CI is recommended precisely because I cannot guarantee which class a future conflict would land in.
- jest-expo 57.0.5 lists `react-server-dom-webpack` as an optional peer; I assumed it stays uninstalled, which is correct for a non-RSC app but would need rechecking if any Expo Router RSC feature is enabled.


## followup:5

*Draft survival and in-flight send across backgrounding / process death (Expo SDK 57, RN 0.86.3): AppState semantics, expo/fetch under iOS suspension, photo cache-URI durability, and a minimal AsyncStorage draft-persistence pattern*

### Recommendation

Keep the Context on the flow's group layout as the single source of truth, but back it with a write-through AsyncStorage draft and treat the send as resumable rather than abortable.

1) Draft persistence (small, boring, ~40 lines). One key, `econsult.draft.v1`, holding `{ schemaVersion, recipientId, answers, message, photo: { uri, mimeType, width, height } | null, econsultId: string | null, idempotencyKey, updatedAt }`. Write it from the reducer with a ~500ms debounce (AsyncStorage is string-only, so JSON.stringify) AND flush immediately on `AppState.addEventListener('change', ...)` when the next state is `inactive` or `background` — iOS documents ~5 seconds before it "is terminated and purged from memory", and Android documents that `onDestroy()` is not guaranteed, so debounce-only would lose the last keystrokes. Hydrate in the group layout: render a `hydrating` state (you already have loading), then either resume on the step the user left or start clean. Clear the key on confirmation and on explicit discard.

2) Photo. Do not trust the picker's cache URI across a relaunch — Expo defines `Paths.cache` as deletable by the system and Android says outright to "check for the existence of your cache files before reading them". Right after the picker resolves, `await new File(asset.uri).copy(new File(Paths.document, 'econsult', name))` (expo-file-system is already a dep of `expo`, nothing to install), store the document-dir URI in the draft, and on restore check `file.exists` before showing the preview — if it's gone, restore the text and show "the photo could not be recovered, add it again" rather than a broken thumbnail. Android's `getPendingResultAsync()` still belongs in the same restore path.

3) In-flight send. Do NOT abort on `background`/`inactive` — backgrounding to read an SMS code is the exact case you're protecting, and cancelling guarantees the failure you're trying to avoid. Reserve `AbortController` (which `expo/fetch` genuinely supports) for the user's own Cancel button and a hard timeout. Make call 1 idempotent: generate the `Idempotency-Key` once, persist it in the draft before the request, and send it on every retry, so replaying after a process death cannot create a duplicate e-consult. Persist `econsultId` the moment call 1 resolves. On the way back to `active`, reconcile: draft with an `idempotencyKey` but no `econsultId` -> re-issue call 1 (safe, idempotent); draft with `econsultId` but no attachment ack -> jump straight to call 2. Show an honest "we're checking whether your message was sent" state rather than a spinner that lies.

4) Use the right transport per call. Call 1 (JSON create) via `expo/fetch`, which is a plain `URLSessionConfiguration.default` session on iOS — no documented continuation while suspended, so it must be resumable, not trusted. Call 2 (attachment) via expo-file-system's `file.upload(url, { uploadType: UploadType.MULTIPART })`, whose iOS `sessionType` already defaults to `'background'` (a real `URLSessionConfiguration.background`) — Apple documents those transfers as continuing while the app is suspended, so the big, slow, flaky-connection part of the send is the part that survives backgrounding for free. Call this out in the write-up as a deliberate asymmetry, not an accident.

5) Cost and honesty for the case study. Zero custom-dev-build cost: AsyncStorage 2.2.0 is Included in Expo Go, expo-file-system is already installed via `expo`. What you cannot claim is true background *completion* of the JSON call or a promise that survives process death — the recoverable-state design (idempotency key + persisted econsultId + resume on `active`) is what makes that acceptable, and saying so explicitly is stronger than pretending the send is atomic.

**Packages involved:** @react-native-async-storage/async-storage, expo-file-system, expo-image-picker, expo, react-native

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (React Native 0.86 docs) — RN 0.86's AppState documents only `active`, `background` and iOS-only `inactive` ("transitioning between foreground & background, and during periods of inactivity such as entering the multitasking view, opening the Notification Center or in the event of an incoming call"), plus Android-only `focus`/`blur` and iOS-only `memoryWarning` events, and says nothing at all about network requests or timers.  
   Source: https://reactnative.dev/docs/0.86/appstate
2. **verified** (React Native 0.86 docs) — `AppState.currentState` is documented as a static property holding the current status, with the note that under the legacy architecture it "will be `null` at launch until it is retrieved asynchronously from the native side" — on the New Architecture default in SDK 57 it is readable synchronously at mount.  
   Source: https://reactnative.dev/docs/0.86/appstate
3. **verified** (Apple UIKit docs, fetched 2026-09-11) — Apple documents that on entering background you have "approximately five seconds to perform any tasks and return. If the method doesn't return before time runs out, your app is terminated and purged from memory", and explicitly tells you to "Invalidate timers" and "Store enough app state information to restore your app to its current state in case it's terminated later".  
   Source: https://developer.apple.com/tutorials/data/documentation/uikit/uiapplicationdelegate/applicationdidenterbackground(_:).json
4. **verified** (Apple UIKit docs, fetched 2026-09-11) — Apple also documents that "UIKit can disconnect a background or suspended scene at any time to reclaim its resources", i.e. iOS can tear the app down with no further JS running and no second chance to save.  
   Source: https://developer.apple.com/tutorials/data/documentation/uikit/managing-your-app-s-life-cycle.json
5. **verified** (expo 57.0.22 (installed)) — `expo/fetch` on iOS creates its session with `URLSessionConfiguration.default` (plus cookie config) — a plain foreground session, not a background one.  
   Source: node_modules/expo/ios/Fetch/ExpoFetchModule.swift:119-123 (createURLSession)
6. **verified** (Apple Foundation docs, fetched 2026-09-11) — Apple documents that only a background configuration "makes it possible for transfers to continue even when the app itself is suspended or terminated" by handing control to a separate process — a default-configuration session gets no such guarantee, so the create-e-consult POST issued through `expo/fetch` has no documented promise of completing once iOS suspends the app and dies outright if iOS reclaims the process.  
   Source: https://developer.apple.com/tutorials/data/documentation/foundation/urlsessionconfiguration/background(withidentifier:).json
7. **verified** (expo 57.0.22 (installed)) — `expo/fetch` does honour `AbortSignal`: the JS wrapper subscribes to the signal and calls `response.abort(reason)` then `request.cancel()`, so a user-initiated cancel or timeout is a first-class, supported operation.  
   Source: node_modules/expo/src/winter/fetch/fetch.ts (addAbortSignalListener)
8. **verified** (expo 57.0.22 (installed)) — On Android `expo/fetch` runs through React Native's shared `OkHttpClientProvider.createClient(reactContext)` on a module-owned `CoroutineScope` inside the app process, so a request is not suspended by backgrounding — it simply dies if Android reclaims the process.  
   Source: node_modules/expo/android/src/main/java/expo/modules/fetch/ExpoFetchModule.kt:27,39-42
9. **verified** (Android developer docs, fetched 2026-09-11) — Android documents that a backgrounded app is a "cached process" the system "is free to kill … as needed when resources like memory are needed elsewhere" and that "`onDestroy()` is not guaranteed to be called in the case that a process is killed by the system" — so a draft must be written eagerly, never on a teardown hook.  
   Source: https://developer.android.com/guide/components/activities/process-lifecycle
10. **verified** (expo-file-system 57.0.7 / SDK 57 docs) — expo-file-system 57.0.7 is already installed as a direct dependency of `expo` 57.0.22 (so `import { File, Paths } from 'expo-file-system'` costs nothing and is Included in Expo Go), and it defines `Paths.cache` as "a place to store files that can be deleted by the system when the device runs low on storage" versus `Paths.document` as "a place to store files that are safe from being deleted by the system".  
   Source: node_modules/expo/package.json:85 + node_modules/expo-file-system/build/Paths.d.ts:6,14 + https://docs.expo.dev/versions/v57.0.0/sdk/filesystem/
11. **verified** (expo-image-picker ~57.0.17 (pinned in expo/bundledNativeModules.json)) — expo-image-picker's documented asset `uri` is a cache path (`file:///data/user/0/host.exp.exponent/cache/cropped1814158652.jpg` in the docs' own example) and the docs warn that "Android system sometimes kills the MainActivity after the ImagePicker finishes … you can retrieve the lost data by calling getPendingResultAsync" — the docs make no persistence promise for that URI.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/imagepicker/
12. **verified** (Android docs + expo-file-system 57.0.7) — Android explicitly states "When the device is low on internal storage space, Android may delete these cache files to recover space. So check for the existence of your cache files before reading them", which — with `Paths.cache`'s identical Expo wording — means a picked photo URI is NOT guaranteed readable after relaunch and should be copied via `file.copy(destination)` into `Paths.document` and re-validated with `file.exists` on restore.  
   Source: https://developer.android.com/training/data-storage/app-specific + node_modules/expo-file-system/build/internal/NativeFileSystem.types.d.ts:172,182
13. **verified** (expo-file-system 57.0.7 (installed)) — expo-file-system 57.0.7 ships `UploadTask` / `file.upload(url, options)` whose iOS `sessionType` field defaults to `'background'` in both the TS types (`@platform ios`, `@default 'background'`) and the Swift record (`@Field var sessionType: NetworkTaskSessionType = .background` resolving to `URLSessionConfiguration.background`), so the attachment upload — unlike `expo/fetch` — runs on a session Apple documents as continuing while the app is suspended.  
   Source: node_modules/expo-file-system/build/NetworkTasks.types.d.ts:135-138 + ios/FileSystemUploadTask.swift:21,39,52 + ios/FileSystemDownloadTask.swift:177,214
14. **verified** (async-storage 2.2.0) — AsyncStorage is zero-cost here — SDK 57 docs list "Android, iOS, macOS, tvOS, Web" and Included in Expo Go with `npx expo install @react-native-async-storage/async-storage`, expo 57.0.22 pins 2.2.0, whose npm peerDependency `^0.0.0-0 || >=0.65 <1.0` covers RN 0.86.3 — but it "can only store `string` data", so the draft must be `JSON.stringify`/`JSON.parse`d.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/async-storage/ + https://registry.npmjs.org/@react-native-async-storage/async-storage/2.2.0 + https://react-native-async-storage.github.io/2.0/Usage/

### Open uncertainties

- Neither RN 0.86's AppState page nor any Expo SDK 57 page I opened states that the JS thread stops executing or that JS timers stop firing while iOS suspends the app — that conclusion is inferred from Apple's lifecycle docs (5-second window, scene disconnection) and is NOT a claim I read in React Native or Expo documentation; treat 'setTimeout-based retry loops silently freeze in background on iOS' as unverified.
- Whether an iOS `expo/fetch` request is actively cancelled by the system or merely stalled during suspension is documented nowhere I opened; what IS documented is only the absence of a continuation guarantee for default sessions. The safe design (idempotent replay) does not depend on resolving this.
- expo-file-system's background upload session has no identifier-based resume exposed in its JS API, so an upload that outlives the JS context has no documented way to report its result after a relaunch — the app must re-ask the server, not re-await the promise. I read the Swift/TS surface but did not find a documented Expo Go end-to-end guarantee for `handleEventsForBackgroundURLSession` (the hook exists in ExpoAppDelegate.swift:126).
- expo-image-picker is not installed in this repo yet (only pinned at ~57.0.17 in bundledNativeModules.json), and the docs' only concrete cache-path example is Android — the iOS temp/cache path shape is unverified.
- AsyncStorage size limits are unverified: https://react-native-async-storage.github.io/2.0/Limits/ returned 404 and the Usage page documents no limit. The often-cited Android 6MB default SQLite size (AsyncStorage_db_size_in_MB) is recalled from training, not opened — irrelevant for a text draft, but do not store base64 image data there.
- I did not verify behaviour on a device or simulator; every claim here is from documentation and installed package sources, not from an observed backgrounding test.


## followup:6

*Dutch app language + screen-reader pronunciation (VoiceOver/TalkBack) in Expo SDK 57 / RN 0.86.3, and what works in Expo Go vs only in a prebuild/EAS binary*

### Recommendation

Do the JS-only fix and explicitly scope the rest as out-of-scope-for-Expo-Go in your README.

1) iOS, works in Expo Go today, zero dependencies: set `accessibilityLanguage="nl-NL"` on every element that carries Dutch `accessibilityLabel`/`accessibilityHint`/`accessibilityValue`. It is a core RN view prop registered in BaseViewConfig.ios.js (no native module), so Expo Go's prebuilt binary already supports it. Because RCTViewComponentView.mm applies it only to the view's own accessibility element and nothing propagates from ancestors, wrapping the app root in one `<View accessibilityLanguage="nl-NL">` does NOT work — put it on each accessible node. Practical way to not scatter it: build thin wrappers (`<AppText>`, `<AppButton>`, `<AppTouchable>`) that default `accessibilityLanguage={Platform.OS === 'ios' ? 'nl-NL' : undefined}`, and set it once per screen-level accessible element — recipient rows, question options, the send button, error/offline banners, the confirmation summary. Same for `AccessibilityInfo.announceForAccessibility` targets (the announcement string itself has no language param, so the wrapper trick is the only lever).

2) Android/TalkBack: state plainly in the README that there is no per-view language API — `LocaleSpan` is absent from react-native 0.86.3 and RN#35813 was closed without an Android equivalent. TalkBack reads Dutch using whatever TTS locale the device is set to, so the only mitigation is setting the emulator's system language to Nederlands when demoing, which you should note as a manual test step rather than code.

3) Do NOT add expo-localization for this. It only reads the locale; it cannot set the app language, and its `supportedLocales` per-app-language feature is a config plugin evaluated only during `npx expo prebuild`. Same for `locales` + `ios.infoPlist.CFBundleAllowMixedLocalizations` — those write InfoPlist.strings at prebuild and are invisible in Expo Go, which is also why your Dutch camera/library permission copy will not appear in the reviewer's Expo Go prompts.

4) Turn the gap into a credible README line instead of hiding it: "App is nl-NL only. iOS VoiceOver pronunciation is forced via accessibilityLanguage; Android has no equivalent API, so TalkBack follows the device TTS locale. Localized system permission dialogs and per-app language selection require `locales` + `ios.infoPlist.CFBundleAllowMixedLocalizations` and the expo-localization config plugin's `supportedLocales`, all of which only take effect in a prebuild/EAS build — deliberately skipped to keep `npx expo start` in Expo Go working from a clean clone." That sentence demonstrates exactly the Expo Go-cost awareness the brief asks for.

5) Demo hygiene: because the iOS Simulator defaults to English, record/demo with VoiceOver on a Simulator left in English to prove the accessibilityLanguage fix actually changes the voice — that is the visible payoff.

**Packages involved:** react-native, expo

### Facts

Each fact is marked **verified** only when the researcher opened the cited source in that run.

1. **verified** (React Native 0.86) — RN 0.86 docs mark `accessibilityLanguage` as iOS-only: "By using the accessibilityLanguage property, the screen reader will understand which language to use while reading the element's label, value, and hint. The provided string value must follow the BCP 47 specification."  
   Source: https://reactnative.dev/docs/0.86/accessibility (opened; page banner confirms "This is documentation for React Native 0.86")
2. **verified** (RN 0.86 and 0.87 doc pages) — `accessibilityLanguage` is the only language/locale accessibility prop on the RN accessibility page, and no Android-equivalent prop is documented there (0.86 and current 0.87 pages both).  
   Source: https://reactnative.dev/docs/0.86/accessibility and https://reactnative.dev/docs/accessibility (both opened)
3. **verified** (react-native 0.86.3 (installed)) — In the installed RN source, `accessibilityLanguage` is registered only in Libraries/NativeComponent/BaseViewConfig.ios.js:196 and has zero occurrences anywhere under node_modules/react-native/ReactAndroid, so on Android the prop is silently dropped rather than mapped to any native API.  
   Source: grep over node_modules/react-native (version 0.86.3 from its package.json)
4. **verified** (react-native 0.86.3) — The TypeScript surface itself marks it iOS-only: ViewAccessibility.d.ts:318-328 carries `@platform ios` above `accessibilityLanguage?: string | undefined;`.  
   Source: node_modules/react-native/Libraries/Components/View/ViewAccessibility.d.ts
5. **verified** (react-native 0.86.3) — There is no inheritance to descendants: RCTViewComponentView.mm:406-410 assigns the prop only to that view's own `self.accessibilityElement`, and RCTParagraphComponentAccessibilityProvider.mm:69 copies `_view.accessibilityLanguage` onto the same Text view's generated sub-elements — nothing walks from an ancestor View down to child components.  
   Source: node_modules/react-native/React/Fabric/Mounting/ComponentViews/View/RCTViewComponentView.mm and .../Text/RCTParagraphComponentAccessibilityProvider.mm
6. **verified** (react-native 0.86.3) — Android has no per-view language mechanism in RN at all: the string "LocaleSpan" (the Android API that would tell TalkBack a text run's language) appears nowhere in react-native 0.86.3.  
   Source: grep -rl "LocaleSpan" over node_modules/react-native (no matches)
7. **verified** (issue closed, not fixed in 0.86.3 per the greps above) — facebook/react-native#35813 "Android: Lack of control of Nested Text locale (Android Span)" (opened 2023-01-12, now Closed) states "Can not specify the locale (language) of a Nested Text (Android Span). TalkBack uses the information when announcing the text" and that iOS's accessibilityLanguage has no Android equivalent.  
   Source: https://github.com/facebook/react-native/issues/35813 (opened)
8. **verified** (Expo SDK 57 (v57.0.0 docs)) — expo-localization for SDK 57 reads "Android, iOS, tvOS, Web, Included in Expo Go", so it costs nothing to add, but its API (getLocales() returning languageTag/languageCode/textDirection/regionCode etc.) only *reports* the user's locale — the SDK page documents no way to *set* the app's language.  
   Source: https://docs.expo.dev/versions/v57.0.0/sdk/localization/ and its .md variant (both opened)
9. **verified** (Expo SDK 57 app config reference) — The app-config `locales` key is documented as: "Provide per-locale values for System Dialog prompts such as Permissions Boxes, and create Localizable.strings file to localize (for example) push notifications" — i.e. it targets OS dialogs, not in-app strings.  
   Source: https://docs.expo.dev/versions/v57.0.0/config/app/ (opened)
10. **verified** (Expo SDK 57 docs (current guide)) — Using it requires `ios.infoPlist.CFBundleAllowMixedLocalizations: true` plus a list of locale JSON file paths in `locales`, and the guide says "During prebuild, Expo writes those values to InfoPlist.strings for the corresponding locale" — so it is a prebuild-time native write with no effect in Expo Go.  
   Source: https://docs.expo.dev/guides/localization/ and /guides/localization.md (both opened)
11. **verified** (Expo SDK 57 docs) — Per-app language selection in system settings requires declaring supported locales via "the expo-localization config plugin and pass the supportedLocales property", and config plugin mods "are only evaluated during the syncing phase of npx expo prebuild" — so this is dev-build/EAS-only too.  
   Source: https://docs.expo.dev/guides/localization.md and https://docs.expo.dev/config-plugins/introduction/ (both opened)
12. **verified** (Expo SDK 57 docs) — The permissions guide confirms the demo-visible consequence for image picking: native usage-description configuration "is not required when testing projects in the Expo Go app", so Dutch camera/photo permission copy in app.json never reaches the reviewer's Expo Go prompts.  
   Source: https://docs.expo.dev/guides/permissions/ (opened)
13. **verified** (repo state 2026-09-11, expo ~57.0.22 / react-native 0.86.3) — The project is currently unconfigured for any locale: app.json has no `locales`, no `ios.infoPlist` (ios only sets `icon`), and package.json has no expo-localization or i18n dependency.  
   Source: app.json and package.json (read)
14. **verified** (Expo SDK 57 docs) — Expo Go also resets RTL state — "This does not work in Expo Go, as Expo Go resets RTL preferences when opening the launcher or individual projects" — irrelevant for Dutch but confirms Expo Go overrides app-level locale/direction settings generally.  
   Source: https://docs.expo.dev/guides/localization/ (opened)
15. *unverified recall* — Apple's own reference for accessibilityLanguage (fallback behaviour when unset, and whether VoiceOver derives a default from the app bundle's development region) could not be read this run — developer.apple.com returned 404/JS-only content for both URLs tried.  
   Source: https://developer.apple.com/documentation/objectivec/nsobject/accessibilitylanguage (404) and .../1615192-accessibilitylanguage (no body returned)

### Open uncertainties

- Apple's documentation on accessibilityLanguage's fallback when unset could not be opened this run (404 / JS-rendered), so the precise claim 'VoiceOver falls back to the host binary's development region, i.e. Expo Go's English' is not verified here — what IS verified is that RN sets the property only when you pass it, so unset elements get whatever the system default is.
- 'accessibilityLanguage works inside Expo Go' is an inference (it is a core RN view prop in BaseViewConfig.ios.js with no native module or config plugin, therefore already compiled into the Expo Go binary); it was not confirmed by running the app on a simulator this run.
- Whether TalkBack does any automatic language detection on Dutch text when the device is set to English is not established by any source opened here; RN#35813 only says the span-level locale cannot be specified.
- The app config reference page also carried a line under `ios` reading 'To add or change language and localization information in your iOS app, you need to use Xcode.' — I could not tell from the fetched rendering which specific key that note is attached to, so I did not build a claim on it.
- expo-localization's `supportedLocales` option is documented in the localization guide, not on the v57 SDK page I opened; the exact option shape/keys for SDK 57 was not read verbatim.


